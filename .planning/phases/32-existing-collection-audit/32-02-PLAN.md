---
phase: 32-existing-collection-audit
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - frontend/src/pages/admin/DuplicateReviewPage.tsx
  - frontend/src/pages/admin/components/DuplicateClusterCard.tsx
  - frontend/src/pages/admin/components/ClusterQuestionItem.tsx
  - frontend/src/pages/admin/AdminLayout.tsx
  - frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can see all duplicate clusters with similarity scores in the review UI"
    - "Admin can select which question to keep per cluster using radio buttons"
    - "Admin can auto-resolve all 90%+ similarity clusters with one click"
    - "Resolved clusters show confirmation with undo option (30-second window)"
    - "Duplicate review page is accessible via admin sidebar navigation"
  artifacts:
    - path: "frontend/src/pages/admin/DuplicateReviewPage.tsx"
      provides: "Main page with cluster list, filters, auto-resolve button, summary stats"
      min_lines: 150
    - path: "frontend/src/pages/admin/components/DuplicateClusterCard.tsx"
      provides: "Single cluster card with radio button keep selection, similarity display, resolve button"
      min_lines: 80
    - path: "frontend/src/pages/admin/components/ClusterQuestionItem.tsx"
      provides: "Question display within cluster showing text, options, correct answer, collection, quality score"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/admin/DuplicateReviewPage.tsx"
      to: "/api/admin/duplicates"
      via: "fetch in useEffect on mount"
      pattern: "fetch.*api/admin/duplicates"
    - from: "frontend/src/pages/admin/DuplicateReviewPage.tsx"
      to: "/api/admin/duplicates/resolve"
      via: "fetch POST on resolve button click"
      pattern: "fetch.*duplicates/resolve"
    - from: "frontend/src/pages/admin/DuplicateReviewPage.tsx"
      to: "/api/admin/duplicates/auto-resolve"
      via: "fetch POST on auto-resolve all button"
      pattern: "fetch.*auto-resolve"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/admin/DuplicateReviewPage.tsx"
      via: "React Router route registration"
      pattern: "Route.*duplicates.*DuplicateReviewPage"
    - from: "frontend/src/pages/admin/AdminLayout.tsx"
      to: "/admin/duplicates"
      via: "Sidebar navigation link"
      pattern: "href.*admin/duplicates"
---

<objective>
Build the admin UI for reviewing and resolving duplicate question clusters. Displays clustered groups with similarity scores, allows selecting which question to keep per cluster, supports auto-resolve for high-confidence duplicates, and provides undo capability.

Purpose: Without the UI, the admin cannot review scanner findings and make keep/archive decisions. This is the human-in-the-loop step of the deduplication workflow.
Output: DuplicateReviewPage.tsx, DuplicateClusterCard.tsx, ClusterQuestionItem.tsx, and routing/navigation updates.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-existing-collection-audit/32-CONTEXT.md
@.planning/phases/32-existing-collection-audit/32-RESEARCH.md
@.planning/phases/32-existing-collection-audit/32-01-SUMMARY.md

# Key source files for patterns
@frontend/src/pages/admin/FlagReviewPage.tsx
@frontend/src/pages/admin/components/FlagDetailPanel.tsx
@frontend/src/pages/admin/AdminLayout.tsx
@frontend/src/App.tsx
@backend/src/services/embeddings/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DuplicateReviewPage and cluster display components</name>
  <files>
    frontend/src/pages/admin/DuplicateReviewPage.tsx
    frontend/src/pages/admin/components/DuplicateClusterCard.tsx
    frontend/src/pages/admin/components/ClusterQuestionItem.tsx
  </files>
  <action>
Create `frontend/src/pages/admin/components/ClusterQuestionItem.tsx`:
- Props: `{ question: QuestionForDedup, isSelected: boolean, onSelect: () => void, isKept?: boolean, isArchived?: boolean }`
- Display: question text (full), all 4 options with checkmark on correct answer, collection badges (colored pills), quality score badge, difficulty badge
- Radio button on the left for keep selection (controlled by isSelected prop)
- If isKept: green border/background tint with "Kept" badge
- If isArchived: red strikethrough/faded with "Archived" badge
- Use Tailwind classes following existing admin component patterns (red theme accents)

Create `frontend/src/pages/admin/components/DuplicateClusterCard.tsx`:
- Props: `{ cluster: EnrichedCluster, onResolve: (clusterId: string, keepExternalId: string) => Promise<void>, onUndo: (clusterId: string) => Promise<void> }`
- EnrichedCluster type: extends DuplicateCluster from embeddings/types.ts with { autoResolvable: boolean, resolved: boolean, resolution?: { keepId: string, archivedIds: string[], resolvedAt: string } }
- Header: cluster ID, tier badge (color-coded: red for exact, orange for near-duplicate, yellow for possible), similarity score range, question count
- Body: list of ClusterQuestionItem components with radio buttons for keep selection
- Pre-select the recommended keep question (from cluster.recommendation.keep)
- Show pairwise similarity table below questions (pairs with scores, color-coded by tier)
- Footer: "Resolve" button (disabled until a question is selected), shows recommendation reason text
- If autoResolvable: show "Auto-recommended" badge in header
- If resolved: show green "Resolved" state with kept question highlighted, undo button (visible for 30 seconds after resolution, then hidden)
- Use `useState` for local selectedKeepId, initialized to cluster.recommendation.keep

Create `frontend/src/pages/admin/DuplicateReviewPage.tsx`:
- Follow FlagReviewPage.tsx pattern for layout structure (header, filters, content area)
- State: clusters array, loading, error, summary stats
- On mount: fetch GET /api/admin/duplicates, populate state
- Summary bar at top: total clusters, pending review, resolved, auto-resolvable count
- Filter controls: tier dropdown (All / Exact / Near-duplicate / Possible), status dropdown (All / Pending / Resolved)
- "Auto-resolve All" button (prominent, in summary bar): POST /api/admin/duplicates/auto-resolve, then refresh cluster list. Show confirmation with count before executing.
- Cluster list: map clusters to DuplicateClusterCard components
- Resolve handler: POST /api/admin/duplicates/resolve with { clusterId, keepExternalId }, update local state to mark cluster as resolved, show undo toast (custom hook pattern from project - NOT Sonner library, follow existing custom undo toast pattern)
- Undo handler: POST /api/admin/duplicates/undo with { clusterId }, update local state to mark cluster as pending again
- Empty state: "No duplicate clusters found" with icon (follow FlagReviewPage empty state pattern)
- Loading state: skeleton or "Loading clusters..." text

Type definitions (inline in DuplicateReviewPage.tsx or in a local types file):
```typescript
interface ClusterQuestion {
  externalId: string;
  text: string;
  options: string[];
  correctAnswer: number;
  collections: string[];
  qualityScore: number | null;
}

interface SimilarityPair {
  questionA: string;
  questionB: string;
  score: number;
  tier: 'exact' | 'near-duplicate' | 'possible';
}

interface ClusterRecommendation {
  keep: string;
  archive: string[];
  reason: string;
}

interface EnrichedCluster {
  clusterId: string;
  tier: 'exact' | 'near-duplicate' | 'possible';
  questions: ClusterQuestion[];
  similarities: SimilarityPair[];
  recommendation: ClusterRecommendation;
  autoResolvable: boolean;
  resolved: boolean;
  resolution?: {
    keepId: string;
    archivedIds: string[];
    resolvedAt: string;
  };
}
```

Use `useAuthStore` for accessToken (same pattern as FlagReviewPage). Use `API_URL` from services/api.
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit` to confirm all components compile. Verify no import errors. Check that the component hierarchy renders: DuplicateReviewPage > DuplicateClusterCard > ClusterQuestionItem.
  </verify>
  <done>
DuplicateReviewPage renders cluster list with filter controls and auto-resolve button. DuplicateClusterCard shows cluster details with radio-button keep selection and resolve/undo buttons. ClusterQuestionItem displays full question context with selection state. All components compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire routing, sidebar navigation, and undo toast</name>
  <files>
    frontend/src/pages/admin/AdminLayout.tsx
    frontend/src/App.tsx
  </files>
  <action>
Update `frontend/src/pages/admin/AdminLayout.tsx`:
1. Add "Duplicate Review" to the navigation array, positioned after "Flag Review":
   ```
   { name: 'Duplicate Review', href: '/admin/duplicates', icon: DuplicateIcon }
   ```
2. Create a DuplicateIcon component at the bottom of the file (following existing icon patterns like FlagIcon). Use a "layers" or "copy" style SVG icon that suggests duplicates — two overlapping rectangles or similar. Keep it simple with stroke-based SVG matching the existing icon style.

Update `frontend/src/App.tsx`:
1. Import DuplicateReviewPage: `import { DuplicateReviewPage } from './pages/admin/DuplicateReviewPage';`
2. Add route inside the admin routes section (after the flags route):
   ```
   <Route path="duplicates" element={<DuplicateReviewPage />} />
   ```

For the undo toast in DuplicateReviewPage: This project uses a custom undo toast hook (decision: "Custom undo toast over Sonner library"). Check if there's an existing `useUndoToast` hook in the codebase. If yes, reuse it. If not, implement a minimal undo toast inline in DuplicateReviewPage using a fixed-position div at the bottom of the screen with a 30-second countdown, "Undo" button, and dismiss. The toast should:
- Appear when a cluster is resolved
- Show "Cluster resolved. {N} questions archived." with an "Undo" button
- Auto-dismiss after 30 seconds
- On "Undo" click: call the undo API, refresh the cluster, dismiss toast
- Only show one toast at a time (new resolution replaces old toast)
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit` to confirm compilation. Verify the route exists by searching for "duplicates" in App.tsx. Verify the nav item exists by searching for "Duplicate Review" in AdminLayout.tsx.
  </verify>
  <done>
Admin sidebar shows "Duplicate Review" navigation item. Route /admin/duplicates renders DuplicateReviewPage. Undo toast appears on cluster resolution with 30-second window. Full frontend compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with no errors
2. `cd frontend && npm run build` succeeds
3. AdminLayout.tsx has "Duplicate Review" in navigation array
4. App.tsx has route for "/admin/duplicates" pointing to DuplicateReviewPage
5. DuplicateReviewPage fetches from /api/admin/duplicates on mount
6. DuplicateClusterCard has radio buttons for keep selection and resolve/undo buttons
7. ClusterQuestionItem shows question text, options, collection badges, quality score
</verification>

<success_criteria>
- Admin can navigate to /admin/duplicates from sidebar
- Page loads and displays duplicate clusters (or empty state if no report exists)
- Each cluster shows questions with radio buttons, similarity scores, and tier badges
- Resolve button archives non-kept questions and shows undo toast
- Auto-resolve button processes all 90%+ clusters
- Undo toast allows reversal within 30 seconds
- Frontend builds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-existing-collection-audit/32-02-SUMMARY.md`
</output>
