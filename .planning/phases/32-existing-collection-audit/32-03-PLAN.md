---
phase: 32-existing-collection-audit
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - backend/src/data/questions.json
  - backend/src/data/bloomington-in-questions.json
  - backend/src/data/los-angeles-ca-questions.json
  - backend/src/data/indiana-state-questions.json
  - backend/src/data/california-state-questions.json
  - backend/src/data/fremont-ca-questions.json
autonomous: false

must_haves:
  truths:
    - "Scanner has been run against all 639 questions and a report exists"
    - "All duplicate clusters have been reviewed (auto-resolved or manually resolved)"
    - "Confirmed duplicates are archived in the database"
    - "JSON source files no longer contain archived duplicate questions"
    - "Federal collection has been verified for uniqueness"
    - "All remaining active questions are unique (no duplicates above threshold)"
  artifacts:
    - path: ".planning/dedup-reports/duplicates-*.json"
      provides: "Scanner output with all duplicate clusters"
    - path: ".planning/dedup-reports/duplicates-*.md"
      provides: "Human-readable markdown report of findings"
  key_links:
    - from: "backend/src/scripts/scan-duplicates.ts"
      to: ".planning/dedup-reports/"
      via: "CLI execution generates reports"
      pattern: "writeFileSync.*dedup-reports"
    - from: "backend/src/services/DuplicateReviewService.ts"
      to: ".planning/dedup-reports/*.json"
      via: "loadReport reads scanner output"
      pattern: "readFileSync.*dedup-reports"
    - from: "backend/src/services/JSONSyncService.ts"
      to: "backend/src/data/*.json"
      via: "syncAfterArchive removes archived questions"
      pattern: "writeFileSync.*\\.json"
---

<objective>
Execute the full duplicate audit: run the scanner against all 639 questions, then use the admin UI to review and resolve all duplicate clusters. This plan produces the actual audit results and data changes.

Purpose: This is the operational execution that makes the deduplication real. Plans 01 and 02 built the tools; this plan uses them to audit and clean the question bank.
Output: Duplicate report, archived duplicates in DB, cleaned JSON source files.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-existing-collection-audit/32-CONTEXT.md
@.planning/phases/32-existing-collection-audit/32-01-SUMMARY.md
@.planning/phases/32-existing-collection-audit/32-02-SUMMARY.md
@.planning/phases/31-semantic-deduplication-infrastructure/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run duplicate scanner against all 639 questions</name>
  <files>
    (no files created/modified - generates reports in .planning/dedup-reports/)
  </files>
  <action>
1. Verify OPENAI_API_KEY is set in backend/.env (required for embedding generation). If not set, this task cannot proceed — document the blocker and skip to checkpoint.

2. Run the scanner from the backend directory:
   ```
   cd backend && npm run scan-duplicates
   ```
   This runs in default dry-run mode, generating JSON and markdown reports in `.planning/dedup-reports/`.

3. Analyze the generated report:
   - Read the JSON report to count total clusters, questions involved, tier distribution
   - Check the markdown report for readability
   - Note the similarity score distribution to calibrate the lower flagging threshold
   - Verify all 6 collections are represented in the scan

4. Report findings:
   - Total clusters found
   - Breakdown by tier (exact, near-duplicate, possible)
   - Breakdown by within-collection vs cross-collection
   - Number of auto-resolvable clusters (90%+ similarity)
   - Any Federal collection duplicates specifically

5. If the scanner fails (API key missing, database connection issue, etc.), document the error and proceed to the checkpoint with whatever information is available.
  </action>
  <verify>
Verify `.planning/dedup-reports/duplicates-*.json` exists and contains valid JSON with a clusters array. Verify `.planning/dedup-reports/duplicates-*.md` exists and is readable. Verify the report covers all 6 collections by checking the collections mentioned in cluster questions.
  </verify>
  <done>
Scanner report generated covering all 639 questions across 6 collections. Report contains duplicate clusters with similarity scores, tier classifications, and keep/archive recommendations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Duplicate scanner report generated. The admin UI at /admin/duplicates should now display all found duplicate clusters for review. The auto-resolve function can handle 90%+ similarity clusters automatically.

The following has been built and is ready for review:
- Scanner report in .planning/dedup-reports/ (both JSON and markdown)
- Backend API endpoints at /api/admin/duplicates
- Admin UI at /admin/duplicates with cluster review, resolve, auto-resolve, and undo
  </what-built>
  <how-to-verify>
1. Review the markdown report at `.planning/dedup-reports/duplicates-*.md` to understand the duplicate landscape
2. Start the backend: `cd backend && npm run dev`
3. Start the frontend: `cd frontend && npm run dev`
4. Log in as admin and navigate to /admin/duplicates
5. Verify clusters display with similarity scores and tier badges
6. Test auto-resolve: click "Auto-resolve All" for 90%+ clusters, verify undo toast appears
7. For remaining clusters: review each one, select the question to keep, click "Resolve"
8. After all clusters resolved, verify:
   - Check the JSON source files in backend/src/data/ — archived questions should be removed
   - Check the database — archived questions should have status='archived'
   - Verify Federal collection is clean (no duplicates remaining)

If any clusters look wrong (good questions being flagged, or true duplicates being missed), describe the issues for adjustment.
  </how-to-verify>
  <resume-signal>Type "approved" after reviewing all clusters and confirming the audit is complete, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. Scanner report exists in .planning/dedup-reports/ (JSON + markdown)
2. All 639 questions were included in the scan
3. All duplicate clusters have been resolved (either auto-resolved or manually)
4. Database shows archived status on all confirmed duplicates
5. JSON source files have archived questions removed
6. Federal collection (questions.json) verified — all remaining questions are unique
7. No active questions remain that are duplicates of each other above the flagging threshold
</verification>

<success_criteria>
- Scanner report covers all 6 collections (639 questions)
- All duplicate clusters reviewed and resolved
- Database reflects archival (status='archived' with duplicate-review history entry)
- JSON source files updated (archived questions removed)
- Federal collection verified unique
- Admin confirms audit is complete via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/32-existing-collection-audit/32-03-SUMMARY.md`
</output>
