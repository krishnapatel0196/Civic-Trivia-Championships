---
phase: 51-plano-tx-collection
plan: "03"
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - frontend/public/images/collections/plano-tx.jpg
autonomous: false

must_haves:
  truths:
    - "plano-tx.jpg exists at frontend/public/images/collections/ with correct aspect ratio (landscape ~2:1)"
    - "activate-collection.ts runs successfully for plano-tx (dry-run then live)"
    - "The Plano collection card appears in the collection picker at ctc.empowered.vote"
    - "A player can start and complete a full 10-question game using Plano questions"
    - "verify-post-activation.ts confirms questionCount >= 50 via the production API"
  artifacts:
    - path: "frontend/public/images/collections/plano-tx.jpg"
      provides: "Plano collection banner image (hot air balloon — Balloon Capital of Texas)"
      min_lines: 1
  key_links:
    - from: "CollectionCard.tsx"
      to: "frontend/public/images/collections/plano-tx.jpg"
      via: "/images/collections/${collection.slug}.jpg"
      pattern: "plano-tx.jpg"
    - from: "verify-post-activation.ts"
      to: "https://civic-trivia-backend.onrender.com/api/collections"
      via: "--slugs plano-tx --api-url"
      pattern: "questionCount.*[5-9][0-9]"
---

<objective>
Add the Plano banner image, activate the collection, and verify end-to-end playability in production.

Purpose: Activation is the final gate — it flips isActive and promotes draft questions to active. The banner image must exist before the collection card will render correctly. Post-activation verification confirms the production API returns the collection with >= 50 questions.

Output: Plano, TX is live in production — collection card visible, full game playable.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-plano-tx-collection/51-CONTEXT.md
@.planning/phases/51-plano-tx-collection/51-RESEARCH.md
@.planning/phases/51-plano-tx-collection/51-02-SUMMARY.md
@backend/src/scripts/activate-collection.ts
@backend/src/scripts/verify-post-activation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source and place Plano banner image</name>
  <files>
    frontend/public/images/collections/plano-tx.jpg
  </files>
  <action>
Source a high-quality hot air balloon photograph for the Plano collection banner image.

**About the image choice:** The user decided on a hot air balloon image because it ties directly to Plano's "Balloon Capital of Texas" historical identity — the annual Plano Balloon Festival and Governor Clements' 1980 proclamation. This is colorful and distinctive, contrasting well with the skyline/civic-building approach of other collections.

**Source options (in priority order):**

1. **Unsplash (free, no attribution required for most):**
   - Search "hot air balloon" or "balloon festival"
   - Look for a colorful, landscape-oriented shot with multiple balloons if possible
   - Unsplash license allows use without attribution

2. **Pexels (free, no attribution required):**
   - Search "hot air balloon festival"
   - Similar terms, similar free-use license

3. **Plano Balloon Festival official imagery:**
   - Check https://www.planoballoonfest.org/ for any press/media images
   - Verify usage rights before downloading

**Image requirements:**
- **Aspect ratio:** Landscape, approximately 2:1 (check other banners: bloomington-in.jpg, fremont-ca.jpg, cambridge-ma.jpg are reference — use the same pixel dimensions)
- **Format:** JPG
- **File name:** `plano-tx.jpg`
- **Destination:** `frontend/public/images/collections/plano-tx.jpg`
- The image should show colorful hot air balloons — ideally in flight or at a festival
- Crop or resize to match the landscape aspect ratio used by other banners

**Steps:**
1. Check existing banner dimensions for reference: `ls -la frontend/public/images/collections/` and optionally check pixel dimensions of an existing banner
2. Download a suitable hot air balloon image from Unsplash or Pexels
3. Crop/resize to match other banner dimensions (landscape 2:1 ratio)
4. Save as `frontend/public/images/collections/plano-tx.jpg`
  </action>
  <verify>
`ls -la frontend/public/images/collections/plano-tx.jpg` confirms file exists. File size > 50KB (too small = likely corrupt or wrong file). Image is visually a hot air balloon photograph.
  </verify>
  <done>
plano-tx.jpg exists in frontend/public/images/collections/, is a landscape hot air balloon photograph, and matches the aspect ratio of other collection banners.
  </done>
</task>

<task type="auto">
  <name>Task 2: Activate the Plano collection and verify production</name>
  <files>
    backend/src/scripts/activate-collection.ts (executed — activates collection in DB)
    backend/src/scripts/verify-post-activation.ts (executed — verifies production API)
  </files>
  <action>
Run the activation sequence for the Plano collection:

```bash
cd backend

# Dry run first — confirm what will be activated
npx tsx src/scripts/activate-collection.ts --slug plano-tx --prefix pla --dry-run

# Review dry-run output: confirm question count matches expectations
# Proceed only if dry-run shows >= 50 questions to be activated

# Live activation
npx tsx src/scripts/activate-collection.ts --slug plano-tx --prefix pla
```

**Expected dry-run output:** Script lists draft questions matching prefix `pla` and the collection row with slug `plano-tx`. Confirm:
- Question count >= 50
- Collection isActive will flip from false -> true
- Questions status will flip from draft -> active

**Expected live activation output:** Script reports success, collection is now active, questions are now active.

**After activation, deploy the frontend (banner image must be in production):**

```bash
cd ../frontend
# If Render auto-deploys from git push, just commit and push the banner image
# Otherwise trigger a deploy as needed
```

Commit the banner image to git and push so the frontend deployment includes it.

**After deployment, run post-activation verification against production:**

```bash
cd ../backend
npx tsx src/scripts/verify-post-activation.ts \
  --slugs plano-tx \
  --api-url https://civic-trivia-backend.onrender.com
```

**Expected output:** Script returns PASS for plano-tx with questionCount >= 50.

**If verify-post-activation fails:**
- Check if the Render deployment is serving the latest code (banner image needs to be deployed)
- If the collections API doesn't return plano-tx, check that isActive = true in the DB for the collection row
- The backend reads live from Supabase, so no backend deployment is needed for the DB changes
  </action>
  <verify>
`activate-collection.ts` exits 0. `verify-post-activation.ts` exits 0 with PASS status showing questionCount >= 50 for plano-tx.
  </verify>
  <done>
Plano collection is active. Production API returns plano-tx collection with questionCount >= 50. Activation and verification scripts both exited 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Claude has placed the hot air balloon banner image, activated the Plano collection (flipping all draft questions to active and marking the collection isActive), and run post-activation verification confirming >= 50 questions are live via the production API.
  </what-built>
  <how-to-verify>
1. Visit https://ctc.empowered.vote (or https://civic-trivia-frontend.onrender.com)
2. Open the collection picker — you should see a Plano, TX collection card with a hot air balloon banner image
3. Verify the card shows the correct theme color (amber/burnt orange, #B45309)
4. Start a game with Plano, TX selected
5. Play through at least 5 questions — confirm:
   - Questions are about Plano civic topics (city government, history, growth story, economic development, community)
   - Corporate questions have civic hooks (not brand promotion)
   - No question has an address or phone number as its answer
   - The game completes normally (scoring, wager at Q10, results screen)
6. Verify the Plano card appears in the local collections section of the collection picker
  </how-to-verify>
  <resume-signal>Type "approved" if Plano is live and playable, or describe any issues (wrong image, card not appearing, questions failing, game not completing)</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
1. `frontend/public/images/collections/plano-tx.jpg` exists and is a hot air balloon photo
2. `verify-post-activation.ts --slugs plano-tx` exits 0 with PASS
3. Plano collection card visible at https://ctc.empowered.vote
4. Full game completable with Plano questions
5. Human reviewer confirmed collection is live and playable
</verification>

<success_criteria>
- plano-tx.jpg exists as a landscape hot air balloon photograph
- activate-collection.ts completed successfully (isActive = true, all questions active)
- verify-post-activation.ts reports PASS with questionCount >= 50
- Plano collection card is visible in the production collection picker with amber theme
- Human reviewer completed a full game and confirmed playability
</success_criteria>

<output>
After completion, create `.planning/phases/51-plano-tx-collection/51-03-SUMMARY.md` following the summary template.
</output>
