---
phase: 47-collection-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260302000001_add_collections_tier.sql
  - backend/src/db/schema.ts
  - backend/src/routes/game.ts
  - frontend/src/features/collections/types.ts
autonomous: true

must_haves:
  truths:
    - "The collections table has a tier column with correct values for all 7 existing collections"
    - "GET /collections API response includes tier field for each collection"
    - "Frontend CollectionSummary type includes tier field"
  artifacts:
    - path: "supabase/migrations/20260302000001_add_collections_tier.sql"
      provides: "DB migration adding tier column to trivia.collections"
      contains: "ADD COLUMN"
    - path: "backend/src/db/schema.ts"
      provides: "Drizzle schema with tier column on collections table"
      contains: "tier"
    - path: "backend/src/routes/game.ts"
      provides: "GET /collections endpoint returning tier in response"
      contains: "tier"
    - path: "frontend/src/features/collections/types.ts"
      provides: "CollectionSummary interface with tier field"
      contains: "tier"
  key_links:
    - from: "supabase/migrations/20260302000001_add_collections_tier.sql"
      to: "backend/src/db/schema.ts"
      via: "migration adds column that schema declares"
      pattern: "tier.*text"
    - from: "backend/src/db/schema.ts"
      to: "backend/src/routes/game.ts"
      via: "collections.tier selected in GET /collections query"
      pattern: "collections\\.tier"
---

<objective>
Add tier column to the collections database table, update the Drizzle schema, include tier in the GET /collections API response, and update the frontend type.

Purpose: This is the foundation for INFRA-01 — making the collection hierarchy database-driven. Once tier lives in the DB, the hardcoded COLLECTION_HIERARCHY map can be replaced (Plan 02). Adding tier to the API now means the frontend and all consumers get tier data at runtime.

Output: DB migration applied, Drizzle schema updated, API returns tier, frontend type updated.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-collection-infrastructure/47-RESEARCH.md
@backend/src/db/schema.ts
@backend/src/routes/game.ts
@frontend/src/features/collections/types.ts
@supabase/migrations/20260301000001_add_player_stats_columns.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier column to collections table (migration + Drizzle schema)</name>
  <files>
    supabase/migrations/20260302000001_add_collections_tier.sql
    backend/src/db/schema.ts
  </files>
  <action>
    1. Create SQL migration file `supabase/migrations/20260302000001_add_collections_tier.sql`:
       ```sql
       -- Phase 47: Add tier column to trivia.collections for DB-driven hierarchy (INFRA-01)
       ALTER TABLE trivia.collections
         ADD COLUMN IF NOT EXISTS tier text NOT NULL DEFAULT 'city';

       -- Set correct tier values for existing collections
       UPDATE trivia.collections SET tier = 'federal' WHERE slug = 'federal';
       UPDATE trivia.collections SET tier = 'state' WHERE slug IN ('indiana-state', 'california-state');
       -- All other collections default to 'city' which is correct
       ```

    2. Update `backend/src/db/schema.ts` — add `tier` column to the `collections` table definition:
       - Add after the `themeColor` field: `tier: text('tier').notNull().default('city'),`
       - This is a text column (not enum) matching the `CollectionTier` type ('federal' | 'state' | 'city')

    3. Apply the migration to the Supabase database. Use `psql` or the Supabase CLI:
       - Read DATABASE_URL from `backend/.env`
       - Run: `psql "$DATABASE_URL" -f supabase/migrations/20260302000001_add_collections_tier.sql`
       - If psql is not available, use the pg client approach from prior phases (create a small script that reads the SQL and runs it via pg Pool)

    4. Verify the migration applied correctly:
       - Query: `SELECT slug, tier FROM trivia.collections ORDER BY sort_order;`
       - Expected: federal -> 'federal', indiana-state -> 'state', california-state -> 'state', all others -> 'city'
  </action>
  <verify>
    Run a SQL query against the database confirming all 7 collections have correct tier values:
    - federal: 'federal'
    - indiana-state: 'state'
    - california-state: 'state'
    - bloomington-in, los-angeles-ca, fremont-ca, norwich-uk: 'city'

    Run `cd backend && npx tsc --noEmit` to verify schema.ts compiles.
  </verify>
  <done>
    The trivia.collections table has a tier column with NOT NULL DEFAULT 'city'. All 7 existing collections have correct tier values. Drizzle schema declares the tier column.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tier to GET /collections API response and frontend type</name>
  <files>
    backend/src/routes/game.ts
    frontend/src/features/collections/types.ts
  </files>
  <action>
    1. In `backend/src/routes/game.ts`, update the `GET /collections` endpoint (around line 54):
       - Add `tier: collections.tier` to the `.select()` object (after the `themeColor` field)
       - The tier value will be automatically included in the JSON response since `res.status(200).json({ collections: filtered })` serializes all selected fields

    2. In `frontend/src/features/collections/types.ts`, add `tier` to the `CollectionSummary` interface:
       ```typescript
       export interface CollectionSummary {
         id: number;
         name: string;
         slug: string;
         description: string;
         themeColor: string;
         questionCount: number;
         tier: 'federal' | 'state' | 'city';
       }
       ```

    3. Verify both backend and frontend compile:
       - `cd backend && npx tsc --noEmit`
       - `cd frontend && npx tsc --noEmit`
  </action>
  <verify>
    `cd backend && npx tsc --noEmit` passes.
    `cd frontend && npx tsc --noEmit` passes.
    Start the backend dev server and `curl http://localhost:3001/collections` — each collection in the response should include a `tier` field.
  </verify>
  <done>
    The GET /collections API returns tier for each collection. The frontend CollectionSummary type includes tier as 'federal' | 'state' | 'city'. Both projects compile without errors.
  </done>
</task>

</tasks>

<verification>
1. SQL: `SELECT slug, tier FROM trivia.collections ORDER BY sort_order;` returns correct tiers for all 7 collections
2. TypeScript: `cd backend && npx tsc --noEmit` passes
3. TypeScript: `cd frontend && npx tsc --noEmit` passes
4. API: GET /collections response includes tier field for each collection
</verification>

<success_criteria>
- tier column exists in trivia.collections with correct values for all existing collections
- Drizzle schema declares tier column
- GET /collections API includes tier in response
- Frontend CollectionSummary type includes tier field
- Both projects compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/47-collection-infrastructure/47-01-SUMMARY.md`
</output>
