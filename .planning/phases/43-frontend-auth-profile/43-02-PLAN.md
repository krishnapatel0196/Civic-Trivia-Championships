---
phase: 43-frontend-auth-profile
plan: "02"
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/Signup.tsx
  - frontend/src/components/ProtectedRoute.tsx
autonomous: true

must_haves:
  truths:
    - "Login form submits to accounts API and stores access_token + refresh_token correctly"
    - "Signup form has no name field, calls accounts API signup then auto-login"
    - "After login, user returns to the page they were on (return-to via ?from= param)"
    - "After signup, user always navigates to / (not from param, to avoid Inform-tier redirect loops)"
    - "ProtectedRoute redirects unauthenticated users to /login?from=<current path>"
  artifacts:
    - path: "frontend/src/pages/Login.tsx"
      provides: "Login page calling accounts API with return-to"
      contains: "from"
    - path: "frontend/src/pages/Signup.tsx"
      provides: "Signup page with no name field, accounts API"
      min_lines: 30
    - path: "frontend/src/components/ProtectedRoute.tsx"
      provides: "Protected route with return-to redirect"
      contains: "from"
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "frontend/src/services/authService.ts"
      via: "authService.login -> accountsApiFetch"
      pattern: "authService.login"
    - from: "frontend/src/pages/Login.tsx"
      to: "localStorage"
      via: "stores refresh_token after login"
      pattern: "ev_refresh_token"
    - from: "frontend/src/components/ProtectedRoute.tsx"
      to: "frontend/src/pages/Login.tsx"
      via: "redirects with ?from= param"
      pattern: "from="
---

<objective>
Rewire Login and Signup pages to call the Empowered Accounts API, add return-to navigation, and update ProtectedRoute to pass the return-to URL.

Purpose: Login/Signup currently submit to local backend /auth/* routes and use the old User type (id: number, name). This plan switches them to the accounts API response shape (access_token snake_case, AccountsUser with string UUID and tier), removes the name field from signup, and adds return-to navigation so users land back where they were after authenticating.

Output: Updated Login.tsx, Signup.tsx, ProtectedRoute.tsx — all using new auth types and accounts API.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-frontend-auth-profile/43-CONTEXT.md
@.planning/phases/43-frontend-auth-profile/43-RESEARCH.md
@.planning/phases/43-frontend-auth-profile/43-01-SUMMARY.md

Key source files:
@frontend/src/pages/Login.tsx
@frontend/src/pages/Signup.tsx
@frontend/src/components/ProtectedRoute.tsx
@frontend/src/types/auth.ts
@frontend/src/store/authStore.ts
@frontend/src/services/authService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Login.tsx and Signup.tsx for accounts API with return-to</name>
  <files>
    frontend/src/pages/Login.tsx
    frontend/src/pages/Signup.tsx
  </files>
  <action>
**frontend/src/pages/Login.tsx** — Update to use accounts API response shape and add return-to navigation:
- Add `useSearchParams` import from `react-router-dom` (alongside existing useNavigate, Link).
- Read `from` param: `const [searchParams] = useSearchParams(); const from = searchParams.get('from');`
- In `handleSubmit`:
  - After `authService.login({ email, password })` succeeds, the response now has `access_token` and `refresh_token` (snake_case from accounts API).
  - Store refresh token: `localStorage.setItem('ev_refresh_token', response.refresh_token)`.
  - Call `setAuth(response.access_token, response.user)` (not `response.accessToken`).
  - Navigate: if `from` exists and starts with `/`, navigate to `from`. Otherwise navigate to `/`.
  - Validate `from` starts with `/` to prevent open redirect attacks.
- Keep all existing error handling, form layout, visual design unchanged.
- Keep the existing red error banner pattern.

**frontend/src/pages/Signup.tsx** — Remove name field, use accounts API, add return-to for login link:
- Remove the `name` state variable and its Input field entirely. The accounts API signup accepts only `{ email, password }`.
- In `handleSubmit`:
  - Call `authService.signup({ email, password })` — note SignupData no longer has name field.
  - After successful signup, auto-login: call `authService.login({ email, password })`.
  - Store refresh token: `localStorage.setItem('ev_refresh_token', loginResponse.refresh_token)`.
  - Call `setAuth(loginResponse.access_token, loginResponse.user)`.
  - Navigate to `/` (NOT to `from` param — Inform-tier users can't access /profile, so navigating there would create a redirect loop).
- Remove the password hint text "Must be at least 8 characters with uppercase, lowercase, and number" — the accounts API may have different password requirements and will return its own validation errors.
- Keep all existing error handling, visual design unchanged.
- Update the "Already have an account? Sign in" link: if there's a `from` param in the current URL, pass it through to `/login?from=...` so the return-to is preserved when switching between login/signup pages. Read from param: `const [searchParams] = useSearchParams();` and `const from = searchParams.get('from');`. Link: `to={from ? \`/login?from=\${encodeURIComponent(from)}\` : '/login'}`.
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit 2>&1 | grep -E "(Login|Signup)" | head -10`. Login.tsx and Signup.tsx should have zero type errors. Check that no reference to `response.accessToken` (camelCase) remains — all should be `response.access_token`.
  </verify>
  <done>
Login.tsx calls accounts API login, stores refresh_token in localStorage, navigates to `from` param or `/`. Signup.tsx has no name field, calls accounts API signup then auto-login, navigates to `/`. Both pages use snake_case access_token/refresh_token from AuthResponse.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ProtectedRoute with return-to redirect</name>
  <files>
    frontend/src/components/ProtectedRoute.tsx
  </files>
  <action>
**frontend/src/components/ProtectedRoute.tsx** — Add return-to URL to login redirect:
- Import `useLocation` from `react-router-dom` (alongside Navigate, Outlet).
- Get current location: `const location = useLocation();`
- When redirecting unauthenticated users, pass current path as `from` param:
  ```
  <Navigate to={`/login?from=${encodeURIComponent(location.pathname)}`} replace />
  ```
- Keep the `isLoading` early return and `isAuthenticated` check unchanged.
- Do NOT add tier checking to ProtectedRoute itself — the Profile page will handle its own Connected-tier check after loading account data (per CONTEXT.md decision: tier check happens after fetchAccountProfile resolves, not before).
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit 2>&1 | grep "ProtectedRoute"`. Should have zero type errors. Confirm the Navigate element includes `from=` in the redirect URL.
  </verify>
  <done>
ProtectedRoute redirects unauthenticated users to `/login?from=<current path>` so they return to their original destination after login.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error"` — errors should only remain in Profile.tsx (still using old ProfileStats shape, old identity management). Login.tsx, Signup.tsx, ProtectedRoute.tsx should be error-free.
2. Confirm Login.tsx stores `ev_refresh_token` in localStorage on successful login.
3. Confirm Signup.tsx has no name field and no name state variable.
4. Confirm ProtectedRoute.tsx redirect includes `?from=` param.
5. Confirm no `response.accessToken` (camelCase) remains in Login.tsx or Signup.tsx.
</verification>

<success_criteria>
- Login submits to accounts API, stores both tokens correctly, navigates with return-to
- Signup removes name field, calls accounts API signup then auto-login, navigates to /
- ProtectedRoute passes return-to URL when redirecting to login
- All three files compile with zero TypeScript errors
- Existing visual design preserved (dark slate gradient, card layout, red error banners)
</success_criteria>

<output>
After completion, create `.planning/phases/43-frontend-auth-profile/43-02-SUMMARY.md`
</output>
