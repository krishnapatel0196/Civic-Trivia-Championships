---
phase: 30-admin-integration-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/admin.ts
  - backend/src/env.ts
autonomous: true

must_haves:
  truths:
    - "Explore endpoint returns flagCount for each question"
    - "Explore endpoint supports sort by flag_count"
    - "Explore endpoint supports filtering to flagged-only questions"
    - "Detail endpoint returns flagCount for the question"
    - "ADMIN_EMAIL env var is validated at startup (optional)"
  artifacts:
    - path: "backend/src/routes/admin.ts"
      provides: "flagCount in explore SELECT, flag_count sort, flagged filter, flagCount in detail SELECT"
      contains: "flagCount: questions.flagCount"
    - path: "backend/src/env.ts"
      provides: "ADMIN_EMAIL environment variable validation"
      contains: "ADMIN_EMAIL"
  key_links:
    - from: "backend/src/routes/admin.ts (explore)"
      to: "backend/src/db/schema.ts (questions.flagCount)"
      via: "Drizzle select"
      pattern: "flagCount.*questions\\.flagCount"
    - from: "backend/src/routes/admin.ts (detail)"
      to: "backend/src/db/schema.ts (questions.flagCount)"
      via: "Drizzle select"
      pattern: "flagCount.*questions\\.flagCount"
---

<objective>
Add flag count data to the question explore and detail API endpoints, add flag_count sorting and flagged-only filtering to the explore endpoint, and configure ADMIN_EMAIL environment variable validation.

Purpose: The frontend needs flagCount data from both endpoints to display the Flags column in the question table (ADMN-05) and the flag badge in the detail panel (ADMN-06). ADMIN_EMAIL satisfies DEBT-02.
Output: Updated backend API endpoints serving flagCount, plus ADMIN_EMAIL env var support.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/routes/admin.ts
@backend/src/env.ts
@backend/src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flagCount to explore and detail endpoints with sort/filter support</name>
  <files>backend/src/routes/admin.ts</files>
  <action>
  In the GET /questions/explore endpoint (around line 411):

  1. Add `flagCount: questions.flagCount` to the select object (after violationCount, around line 463).

  2. Add 'flag_count' to the validSortColumns array (line 425):
     `const validSortColumns = ['quality_score', 'difficulty', 'encounter_count', 'correct_count', 'created_at', 'flag_count'];`

  3. Add flag_count sort handler after the created_at handler (around line 494):
     ```
     } else if (sortColumn === 'flag_count') {
       query = query.orderBy(order === 'desc' ? desc(questions.flagCount) : asc(questions.flagCount)) as any;
     }
     ```

  4. Add flagged-only filter support. Parse a new query param `flagged`:
     ```
     const flaggedFilter = req.query.flagged as string;
     ```
     If `flaggedFilter === 'true'`, add filter: `filters.push(gt(questions.flagCount, 0));`
     Import `gt` from drizzle-orm if not already imported (check existing imports at top of file).

  In the GET /questions/:id/detail endpoint (around line 538):

  5. Add `flagCount: questions.flagCount` to the select object (after violationCount, around line 567).

  Make sure the `gt` import exists. The flags endpoint (around line 725) already uses `gt(questions.flagCount, 0)` so it should already be imported. Verify this.
  </action>
  <verify>
  Run `cd "C:/Project Test/backend" && npx tsc --noEmit` to check for TypeScript errors.
  Test the endpoint manually if dev server available: the explore response should include flagCount field for each question.
  </verify>
  <done>
  - GET /api/admin/questions/explore returns flagCount in each question object
  - sort=flag_count&order=desc sorts questions by flag count descending
  - flagged=true filters to only questions with flag_count > 0
  - GET /api/admin/questions/:id/detail returns flagCount in the question object
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ADMIN_EMAIL environment variable validation</name>
  <files>backend/src/env.ts</files>
  <action>
  Update backend/src/env.ts to validate ADMIN_EMAIL as an optional email string. The current file only loads dotenv without any validation schema. Add a simple validation:

  After the existing dotenv.config() call, add:

  ```typescript
  // Validate optional ADMIN_EMAIL if present
  const adminEmail = process.env.ADMIN_EMAIL;
  if (adminEmail && !adminEmail.match(/^[^\s@]+@[^\s@]+\.[^\s@]+$/)) {
    console.warn('WARNING: ADMIN_EMAIL is set but does not look like a valid email address:', adminEmail);
  }

  export const ADMIN_EMAIL = adminEmail || null;
  ```

  Do NOT use Zod here — the env.ts file is minimal (just dotenv loading) and adding Zod would change the import pattern for the entire backend. Keep it simple with a regex warning. The env var is optional so missing is fine.

  Also add ADMIN_EMAIL to the backend's .env.example file if one exists. If not, skip this step.
  </action>
  <verify>
  Run `cd "C:/Project Test/backend" && npx tsc --noEmit` to verify no TypeScript errors.
  Check that `import { ADMIN_EMAIL } from '../env.js'` would resolve correctly.
  </verify>
  <done>
  - ADMIN_EMAIL is exported from backend/src/env.ts
  - Invalid email format triggers a console warning at startup
  - Missing ADMIN_EMAIL results in null (no error)
  </done>
</task>

</tasks>

<verification>
1. `cd "C:/Project Test/backend" && npx tsc --noEmit` passes with no errors
2. The explore endpoint SELECT includes flagCount
3. The detail endpoint SELECT includes flagCount
4. validSortColumns includes 'flag_count'
5. flagged=true filter logic exists
6. ADMIN_EMAIL is exported from env.ts
</verification>

<success_criteria>
- Backend explore endpoint returns flagCount for every question row
- Backend explore endpoint accepts sort=flag_count and flagged=true query params
- Backend detail endpoint returns flagCount
- ADMIN_EMAIL env var is validated and exported
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-admin-integration-tech-debt/30-01-SUMMARY.md`
</output>
