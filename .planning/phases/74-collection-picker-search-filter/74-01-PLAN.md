---
phase: 74-collection-picker-search-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/collections/components/CollectionPicker.tsx
autonomous: true

must_haves:
  truths:
    - "Search input is visible at top of collection picker before any interaction"
    - "Typing a partial name filters to only matching collections in a flat list"
    - "Clearing the input restores the full grouped view (Federal / State / City)"
    - "Filter is case-insensitive"
    - "Empty state shown when no collections match the query"
  artifacts:
    - path: "frontend/src/features/collections/components/CollectionPicker.tsx"
      provides: "Search/filter functionality for collection picker"
      contains: "useDebounce"
  key_links:
    - from: "CollectionPicker search input"
      to: "useDebounce hook"
      via: "import from frontend/src/hooks/useDebounce.ts"
      pattern: "useDebounce"
    - from: "debouncedQuery state"
      to: "filtered collections rendering"
      via: "isFiltering conditional"
      pattern: "isFiltering.*debouncedQuery"
---

<objective>
Add search/filter input to the collection picker so players can find any collection by typing instead of scrolling.

Purpose: With 26+ collections across three tiers, scrolling to find a specific collection is slow. A search input lets players jump straight to what they want.
Output: Updated CollectionPicker.tsx with search input and conditional grouped/flat rendering.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/features/collections/components/CollectionPicker.tsx
@frontend/src/hooks/useDebounce.ts
@frontend/src/features/collections/components/CollectionCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search input and filter logic to CollectionPicker</name>
  <files>frontend/src/features/collections/components/CollectionPicker.tsx</files>
  <action>
Modify CollectionPicker.tsx to add collection search/filter:

1. **Add imports:** Add `useState` from 'react' and `useDebounce` from '../../../hooks/useDebounce'.

2. **Add state inside component function** (before the early return and before `grouped` computation):
   - `const [query, setQuery] = useState('')`
   - `const debouncedQuery = useDebounce(query, 150)`
   - `const isFiltering = debouncedQuery.trim().length > 0`
   - `const filtered = isFiltering ? collections.filter(c => c.name.toLowerCase().includes(debouncedQuery.trim().toLowerCase())) : []`

3. **Add search input** inside the non-loading branch, AFTER the "CHOOSE YOUR COLLECTION" heading and BEFORE the grouped/filtered content. Render only when `!loading`. Use a native `<input type="search">` element with inline styles only (NO Tailwind classes, NO shared Input component). Styles:
   - `width: '100%'`, `boxSizing: 'border-box'` as const, `padding: '8px 12px'`
   - `fontFamily: "'Lora', Georgia, serif"`, `fontSize: '14px'`, `color: '#17120E'`
   - `background: '#F5EDD8'`, `border: '1px solid #C8BAA6'`, `borderRadius: '3px'`
   - `outline: 'none'`, `marginBottom: '20px'`
   - `placeholder="Search collections..."`, `aria-label="Search collections"`
   - Do NOT auto-focus the input (mobile keyboard concern)
   - `value={query}`, `onChange={(e) => setQuery(e.target.value)}`

4. **Conditional rendering** below the search input:
   - When `isFiltering` is true: render a FLAT grid (same grid styles as the existing per-category grid: `display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(148px, 1fr))', gap: '12px'`) with NO category headers. Map `filtered` to CollectionCard components, passing `isSelected={selectedId === collection.id}` and `onSelect={onSelect}`. If `filtered.length === 0`, render a paragraph inside the grid with `gridColumn: '1 / -1'`, `textAlign: 'center'`, `color: '#9A8878'`, `fontFamily: "'Lora', Georgia, serif"`, `fontSize: '14px'`, `padding: '20px 0'` showing: `No collections match "{debouncedQuery}"`
   - When `isFiltering` is false: render the existing grouped view unchanged (the existing `grouped.map(...)` block)

5. **Structure the JSX** so the search input and the conditional grouped/flat content are siblings inside the existing non-loading container div. The "CHOOSE YOUR COLLECTION" heading stays outside and above both loading and non-loading branches (it already is).

Important: Do NOT move the search input into the loading branch. It should only appear when loading is false.
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit` to confirm no type errors. Then run `npm run build` from the frontend directory to confirm the build succeeds. Visually inspect the diff to confirm:
- useState and useDebounce are imported
- Search input uses inline styles only (no className on the input)
- Flat filtered view has NO category labels
- Empty state message is present
- isSelected prop is passed in the flat view
  </verify>
  <done>
CollectionPicker renders a search input above collections when not loading. Typing filters to a flat list of matching collections (case-insensitive). Clearing restores grouped view. Empty state shown when no matches. Build passes with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero type errors
2. `cd frontend && npm run build` — build succeeds
3. Manual spot-check: search input element present in JSX, uses inline styles, no Tailwind classes on new elements
4. Confirm useDebounce imported from existing hook (not reimplemented)
5. Confirm flat view does not render category headers
6. Confirm empty state renders when filtered.length === 0
</verification>

<success_criteria>
- Search input visible at top of collection picker (in non-loading state)
- Typing partial name filters collections into flat list (no tier groupings)
- Clearing input restores full grouped view
- Case-insensitive filtering works
- Empty state displayed for zero-match queries
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/74-collection-picker-search-filter/74-01-SUMMARY.md`
</output>
