---
phase: 67-leaderboard
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - frontend/src/features/leaderboard/types.ts
  - frontend/src/features/leaderboard/hooks/useLeaderboard.ts
  - frontend/src/features/leaderboard/components/LeaderboardTabs.tsx
  - frontend/src/features/leaderboard/components/LeaderboardRow.tsx
  - frontend/src/pages/Leaderboard.tsx
  - frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /leaderboard shows a ranked list of players"
    - "All Time tab shows players ranked by total XP"
    - "This Week tab shows players ranked by rolling 7-day XP"
    - "Switching tabs loads the correct data without full page reload"
    - "Loading state shows skeleton rows while data fetches"
    - "Error state shows a subdued message if API is unavailable"
    - "Empty state shows motivational CTA with Play button"
  artifacts:
    - path: "frontend/src/features/leaderboard/types.ts"
      provides: "LeaderboardEntry and LeaderboardResponse type definitions"
      exports: ["LeaderboardEntry", "LeaderboardResponse"]
    - path: "frontend/src/features/leaderboard/hooks/useLeaderboard.ts"
      provides: "Data fetching hook for leaderboard API"
      exports: ["useLeaderboard"]
    - path: "frontend/src/pages/Leaderboard.tsx"
      provides: "Leaderboard page component"
      exports: ["Leaderboard"]
    - path: "frontend/src/App.tsx"
      provides: "Route registration for /leaderboard"
      contains: "leaderboard"
  key_links:
    - from: "frontend/src/features/leaderboard/hooks/useLeaderboard.ts"
      to: "/api/leaderboard"
      via: "fetch call to backend API"
      pattern: "api/leaderboard"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Leaderboard.tsx"
      via: "Route element"
      pattern: "path.*leaderboard"
    - from: "frontend/src/pages/Leaderboard.tsx"
      to: "frontend/src/features/leaderboard/hooks/useLeaderboard.ts"
      via: "hook import"
      pattern: "useLeaderboard"
---

<objective>
Build the core leaderboard frontend page with tabs, ranked list (positions 4-25), loading/error/empty states, and data fetching hook.

Purpose: This plan creates the foundational leaderboard page that displays ranked player data from the backend API built in Plan 01. It establishes the page structure, tab switching, and row rendering. Plan 03 adds the podium (top 3), sticky "you" row, and navigation entry points.

Output: A working /leaderboard page showing ranked players in a list with tab switching between All Time and This Week.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-leaderboard/67-RESEARCH.md
@.planning/phases/67-leaderboard/67-CONTEXT.md
@.planning/phases/67-leaderboard/67-01-SUMMARY.md

Key source files:
@frontend/src/App.tsx — Route registration pattern
@frontend/src/hooks/usePlayerXp.ts — Data fetching hook pattern (fetch + useEffect + loading/error states)
@frontend/src/hooks/useTheme.ts — Theme hook pattern (C.* color tokens)
@frontend/src/components/Avatar.tsx — Avatar component for initial circles
@frontend/src/pages/Profile.tsx — Page layout pattern with sections, Bebas Neue headers
@frontend/src/components/layout/Header.tsx — Header for layout context
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, data hook, and tab component</name>
  <files>
    frontend/src/features/leaderboard/types.ts
    frontend/src/features/leaderboard/hooks/useLeaderboard.ts
    frontend/src/features/leaderboard/components/LeaderboardTabs.tsx
  </files>
  <action>
Create the leaderboard feature directory structure and foundational modules.

**1. `frontend/src/features/leaderboard/types.ts`:**
```typescript
export interface LeaderboardEntry {
  rank: number;
  user_id: string;
  username: string;
  tier: 'inform' | 'connected' | 'empowered';
  level: number;
  total_xp: number;
}

export interface UserRank {
  rank: number;
  username: string;
  tier: 'inform' | 'connected' | 'empowered';
  level: number;
  total_xp: number;
  gap_to_next: number;
}

export interface LeaderboardResponse {
  tab: 'all_time' | 'this_week';
  entries: LeaderboardEntry[];
  userRank: UserRank | null;
  generatedAt: string;
}

export type LeaderboardTab = 'all_time' | 'this_week';
```

**2. `frontend/src/features/leaderboard/hooks/useLeaderboard.ts`:**
- Follow the `usePlayerXp` pattern exactly: plain `fetch` in `useEffect`, `useState` for data/loading/error
- Import API_BASE from the same location usePlayerXp uses (check `accountsApi.ts` or env config)
- Fetch from `${API_BASE}/api/leaderboard?tab=${tab}${userId ? '&userId=' + userId : ''}`
- Cache results per tab in component state to avoid re-fetch flicker on tab switch (see Research Pitfall 4):
  ```typescript
  const [cache, setCache] = useState<Partial<Record<LeaderboardTab, LeaderboardResponse>>>({});
  ```
- On tab change: if cache[tab] exists and is <5 min old, use it; otherwise fetch
- Return `{ data: LeaderboardResponse | null, isLoading: boolean, error: string | null }`
- Dependencies: `[tab, userId]` — refetch when tab or userId changes (but check cache first)

**3. `frontend/src/features/leaderboard/components/LeaderboardTabs.tsx`:**
- Simple inline-styled tab strip matching Bebas Neue + border-bottom active indicator
- Props: `{ active: LeaderboardTab; onChange: (tab: LeaderboardTab) => void }`
- Two tabs: "ALL TIME" and "THIS WEEK"
- Use `useTheme()` for colors: active tab gets `C.accent` color + `2px solid C.accent` bottom border, inactive gets `C.muted` color + transparent bottom border
- `fontFamily: "'Bebas Neue', sans-serif"`, `fontSize: '15px'`, `letterSpacing: '0.16em'`
- Buttons have `background: 'none'`, `border: 'none'` except the bottom border indicator
- Container: `display: 'flex'`, `borderBottom: 1px solid ${C.rule}`, `marginBottom: '24px'`
- Do NOT use @headlessui Tab — the app uses inline style patterns consistently
  </action>
  <verify>
`cd frontend && npx tsc --noEmit` — TypeScript compiles. Types are exported correctly. Hook and tab component are importable.
  </verify>
  <done>Types, data fetching hook with client-side tab cache, and tab component are created and type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Leaderboard page with ranked list, states, and route</name>
  <files>
    frontend/src/features/leaderboard/components/LeaderboardRow.tsx
    frontend/src/pages/Leaderboard.tsx
    frontend/src/App.tsx
  </files>
  <action>
Build the main leaderboard page and register the route.

**1. `frontend/src/features/leaderboard/components/LeaderboardRow.tsx`:**
- Renders a single row for positions 4-25 (top 3 handled by podium in Plan 03)
- Props: `{ entry: LeaderboardEntry; isYou: boolean }` (isYou from comparing entry.user_id to auth store userId)
- Layout: horizontal flex row with:
  - Rank number (Bebas Neue, 16px, C.muted, right-aligned in 28px width)
  - Tier badge: small 8px colored circle — use tier color map: `inform` = C.muted, `connected` = '#03B9D2', `empowered` = '#FF5740'
  - Avatar initial circle: use `<Avatar name={entry.username} size={28} />` from `frontend/src/components/Avatar.tsx`
  - Username (Lora, 14px, C.ink, with `overflow: hidden; textOverflow: ellipsis; whiteSpace: nowrap` in a flex:1 container)
  - Level label below username (Bebas Neue, 11px, C.muted, "LV {level}")
  - Total XP (Bebas Neue, 14px, `#E8A020` amber, right-aligned, `{xp.toLocaleString()} XP`)
- isYou row: add `background: ${C.accent}14` (accent at ~8% opacity) — this is the hex alpha suffix trick
- Each row has `borderBottom: 1px solid ${C.ruleLight}`, padding `10px 16px`
- See Research code example for LeaderboardRow — match that structure

**2. `frontend/src/pages/Leaderboard.tsx`:**
- Full page component with Header (imported from layout/Header) at top
- Page title: "LEADERBOARD" in Bebas Neue, centered, large (28px), with C.ink color
- Contains LeaderboardTabs for tab switching
- State: `const [tab, setTab] = useState<LeaderboardTab>('all_time')`
- Get userId from `useAuthStore(s => s.user?.id ?? null)`
- Use `useLeaderboard(tab, userId)` hook for data
- **Ranked list (positions 4-25):** Filter `data.entries.filter(e => e.rank > 3)` and render LeaderboardRow for each. If entry.user_id matches userId, pass `isYou={true}`.
- **Top 3 placeholder:** For now, render entries with rank <= 3 as regular LeaderboardRow components (Plan 03 replaces with podium). Add a comment: `{/* Plan 03: Replace with LeaderboardPodium */}`
- **Loading state:** Show 5 skeleton rows using `animate-pulse` Tailwind class with `C.ruleLight` background rectangles. Match the research recommendation.
- **Error state:** Show subdued message: "Leaderboard temporarily unavailable. Try again later." in C.muted. Do NOT use error boundary — this is non-critical. Add a "Try Again" button that calls the hook's refetch.
- **Empty state:** If data.entries is empty, show: "No players yet — be the first!" with a "PLAY NOW" button linking to /play. Style the button like the primary CTA (C.accent background, white text, Bebas Neue).
- Page wrapper: match Dashboard page layout — max-width container, centered, padding

**3. Route in `frontend/src/App.tsx`:**
- Import: `import { Leaderboard } from './pages/Leaderboard';`
- Add route in the public routes section (NOT inside ProtectedRoute):
  ```tsx
  <Route path="/leaderboard" element={<Leaderboard />} />
  ```
- Place after `/play` route, before the ProtectedRoute block
  </action>
  <verify>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles
2. `cd frontend && npm run build` — Vite builds successfully
3. Start frontend dev server, navigate to `http://localhost:5173/leaderboard` — page renders with tabs, shows loading then data (or empty state if no users have XP)
4. Tab switching works without page reload
5. Page is accessible without being logged in (no redirect to /login)
  </verify>
  <done>
Leaderboard page renders at /leaderboard with tab switching, ranked list for all positions, loading/error/empty states. Page is publicly accessible. Logged-in user's row is highlighted with accent background.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` succeeds
- `/leaderboard` route loads without auth
- Tab switching between All Time and This Week works
- Ranked rows display with correct data from backend API
- Loading state shows skeleton rows
- Error state shows graceful message
- Empty state shows motivational CTA
</verification>

<success_criteria>
- Leaderboard page accessible at /leaderboard without authentication
- All Time and This Week tabs fetch and display correct data
- Each row shows rank, tier badge, avatar, username, level, total XP
- Logged-in user's row has accent background highlight
- Loading, error, and empty states are handled gracefully
- Client-side tab cache prevents re-fetch flicker on tab switch
</success_criteria>

<output>
After completion, create `.planning/phases/67-leaderboard/67-02-SUMMARY.md`
</output>
