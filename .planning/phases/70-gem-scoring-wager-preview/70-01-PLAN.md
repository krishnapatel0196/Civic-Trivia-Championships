---
phase: 70-gem-scoring-wager-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/progressionService.ts
  - backend/src/routes/game.ts
autonomous: true

must_haves:
  truths:
    - "A player with finalScore >= 1000 earns 1 gem"
    - "A player with 8/8 correct earns 2 gems regardless of score"
    - "A player with <1000 score and <8/8 correct earns 0 gems"
    - "The old accuracy-based gem rule (>= totalQuestions - 2) is fully removed"
  artifacts:
    - path: "backend/src/services/progressionService.ts"
      provides: "Updated calculateProgression with score-based gem logic"
      exports: ["calculateProgression", "GEM_SCORE_THRESHOLD"]
      contains: "GEM_SCORE_THRESHOLD"
    - path: "backend/src/routes/game.ts"
      provides: "Call site passes totalScore to calculateProgression"
      contains: "results.totalScore"
  key_links:
    - from: "backend/src/routes/game.ts"
      to: "backend/src/services/progressionService.ts"
      via: "calculateProgression(correctAnswers, totalQuestions, finalScore)"
      pattern: "calculateProgression.*totalScore"
---

<objective>
Update the backend gem calculation to award gems based on final score instead of accuracy.

Purpose: The current gem rule (1 gem for >= 6/8 correct) doesn't reward wager strategy. The new rule (1 gem for score >= 1000) makes the wager question meaningful for gem earning.
Output: Updated calculateProgression() that accepts finalScore, exported GEM_SCORE_THRESHOLD constant, updated call site in game.ts.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/services/progressionService.ts
@backend/src/routes/game.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update calculateProgression with score-based gem logic</name>
  <files>backend/src/services/progressionService.ts</files>
  <action>
1. Add an exported named constant at the top of the file (after imports, before the function):
   ```
   export const GEM_SCORE_THRESHOLD = 1000;
   ```

2. Update the `calculateProgression` function signature to accept a third parameter `finalScore`:
   ```
   export function calculateProgression(
     correctAnswers: number,
     totalQuestions: number,
     finalScore: number
   ): { xpEarned: number; gemsEarned: number }
   ```

3. Replace the gem calculation logic inside the function. CRITICAL: Check perfect game FIRST, then score threshold:
   ```
   const isPerfect = correctAnswers === totalQuestions;
   const gemsEarned = isPerfect ? 2 : finalScore >= GEM_SCORE_THRESHOLD ? 1 : 0;
   ```

4. The XP calculation (`xpEarned`) remains unchanged.

5. Remove all old accuracy-based gem code (the `correctAnswers >= totalQuestions - 2` ternary). No dead code or commented-out lines.

Note: The existing `calculateXpAmount` function is separate and unchanged — that handles platform XP awards.
  </action>
  <verify>Run `npx tsc --noEmit` from the backend directory to confirm no type errors. Grep for "totalQuestions - 2" in progressionService.ts to confirm old rule is gone.</verify>
  <done>calculateProgression accepts (correctAnswers, totalQuestions, finalScore), exports GEM_SCORE_THRESHOLD = 1000, awards 2 gems for perfect (8/8), 1 gem for finalScore >= 1000, 0 otherwise. Old accuracy rule fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update game.ts call site to pass totalScore</name>
  <files>backend/src/routes/game.ts</files>
  <action>
1. Find the call to `calculateProgression` around line 397:
   ```
   const { gemsEarned } = calculateProgression(results.totalCorrect, results.totalQuestions);
   ```

2. Add `results.totalScore` as the third argument:
   ```
   const { gemsEarned } = calculateProgression(results.totalCorrect, results.totalQuestions, results.totalScore);
   ```

That is the only change needed. `results.totalScore` is already in scope at that point in the code.
  </action>
  <verify>Run `npx tsc --noEmit` from the backend directory. The build must pass with zero errors. Run `npx tsx -e "import { calculateProgression, GEM_SCORE_THRESHOLD } from './src/services/progressionService.js'; console.log(GEM_SCORE_THRESHOLD); console.log(calculateProgression(5, 8, 1050)); console.log(calculateProgression(8, 8, 800)); console.log(calculateProgression(4, 8, 500));"` from the backend directory to verify: threshold is 1000, (5,8,1050) gives 1 gem, (8,8,800) gives 2 gems (perfect), (4,8,500) gives 0 gems.</verify>
  <done>game.ts passes results.totalScore to calculateProgression. TypeScript compiles cleanly. Score-based gem logic works: perfect=2, >=1000=1, else 0.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in backend/
- No references to `totalQuestions - 2` remain in progressionService.ts
- GEM_SCORE_THRESHOLD is exported and equals 1000
- calculateProgression(5, 8, 1050) returns { xpEarned: 55, gemsEarned: 1 }
- calculateProgression(8, 8, 800) returns { xpEarned: 58, gemsEarned: 2 }
- calculateProgression(4, 8, 500) returns { xpEarned: 54, gemsEarned: 0 }
</verification>

<success_criteria>
- Backend gem logic uses score-based threshold (GEM_SCORE_THRESHOLD = 1000) instead of accuracy-based rule
- Perfect game (8/8) still awards 2 gems (checked FIRST)
- Score >= 1000 awards 1 gem
- Score < 1000 with imperfect game awards 0 gems
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/70-gem-scoring-wager-preview/70-01-SUMMARY.md`
</output>
