---
phase: 45-auth-state-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-frontend-auth-profile/43-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "A VERIFICATION.md exists for Phase 43 that confirms AUTH-04, AUTH-05, AUTH-06, AUTH-07, PROF-01, PROF-02, PROF-03, PROF-04 are satisfied by the committed codebase"
    - "REQUIREMENTS.md ADMIN-01 text references public.admin_users (not public.user_roles)"
  artifacts:
    - path: ".planning/phases/43-frontend-auth-profile/43-VERIFICATION.md"
      provides: "Goal-backward verification of Phase 43 frontend auth and profile requirements"
      contains: "AUTH-04"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected ADMIN-01 requirement text"
      contains: "admin_users"
  key_links:
    - from: ".planning/phases/43-frontend-auth-profile/43-VERIFICATION.md"
      to: "frontend/src/services/accountsApi.ts"
      via: "Evidence for AUTH-04, AUTH-05, AUTH-06, AUTH-07"
      pattern: "accountsApi"
    - from: ".planning/phases/43-frontend-auth-profile/43-VERIFICATION.md"
      to: "frontend/src/pages/Profile.tsx"
      via: "Evidence for PROF-01, PROF-02, PROF-03, PROF-04"
      pattern: "Profile"
---

<objective>
Write the missing Phase 43 VERIFICATION.md (goal-backward verification of AUTH-04-07 and PROF-01-04) and update the stale ADMIN-01 requirement text from "public.user_roles" to "public.admin_users".

Purpose: Closes two audit gaps — the missing Phase 43 formal verification and the stale requirement text. All verification is structural (file inspection), no live environment needed.
Output: 43-VERIFICATION.md created, REQUIREMENTS.md updated.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-auth-state-hardening/45-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/v1.8-MILESTONE-AUDIT.md
@.planning/phases/43-frontend-auth-profile/43-01-SUMMARY.md
@.planning/phases/43-frontend-auth-profile/43-02-SUMMARY.md
@.planning/phases/43-frontend-auth-profile/43-03-SUMMARY.md
@.planning/phases/44-deprecation-cleanup/44-VERIFICATION.md
@frontend/src/services/accountsApi.ts
@frontend/src/services/authService.ts
@frontend/src/pages/Login.tsx
@frontend/src/pages/Signup.tsx
@frontend/src/pages/Profile.tsx
@frontend/src/components/AuthInitializer.tsx
@frontend/src/store/authStore.ts
@frontend/src/types/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 43 VERIFICATION.md</name>
  <files>
    .planning/phases/43-frontend-auth-profile/43-VERIFICATION.md
  </files>
  <action>
    Create a goal-backward verification report for Phase 43: Frontend Auth and Profile. Follow the exact format used in `.planning/phases/44-deprecation-cleanup/44-VERIFICATION.md` as the template.

    **Phase 43 goal (from roadmap):** "Frontend login, signup, logout, and token refresh all call the Empowered Accounts API; the profile page shows trivia-specific stats plus accounts-sourced gem balance, tier badge, and display name, with identity management actions linking out to the accounts platform."

    **Frontmatter:**
    ```yaml
    ---
    phase: 43-frontend-auth-profile
    verified: [current timestamp]
    status: passed
    score: 8/8 must-haves verified
    ---
    ```

    **Verify each of these 8 requirements by reading the actual source files** (not by trusting summaries — read the files directly):

    **AUTH-04: Frontend login calls Empowered Accounts POST /api/auth/login**
    - Read `frontend/src/services/authService.ts` — find the login function
    - Confirm it calls `accountsApiFetch('/api/auth/login', ...)` with POST method
    - Read `frontend/src/pages/Login.tsx` — confirm it calls the auth service login
    - Evidence: file paths, line numbers, function names

    **AUTH-05: Frontend signup calls Empowered Accounts POST /api/auth/signup**
    - Read `frontend/src/services/authService.ts` — find the signup function
    - Confirm it calls `accountsApiFetch('/api/auth/signup', ...)` with POST method
    - Read `frontend/src/pages/Signup.tsx` — confirm it calls the auth service signup
    - Evidence: file paths, line numbers, function names

    **AUTH-06: Logout calls Empowered Accounts POST /api/auth/logout**
    - Read `frontend/src/services/authService.ts` — find the logout function
    - Confirm it calls the accounts API logout endpoint with Bearer token
    - Evidence: file paths, line numbers, function names

    **AUTH-07: Token refresh uses Empowered Accounts refresh token flow**
    - Read `frontend/src/services/accountsApi.ts` — confirm `exchangeRefreshToken` calls Supabase `/auth/v1/token?grant_type=refresh_token`
    - Read `frontend/src/components/AuthInitializer.tsx` — confirm it calls `exchangeRefreshToken` on startup
    - Evidence: file paths, line numbers, endpoint URL

    **PROF-01: Profile page displays trivia stats (games played, accuracy, best score)**
    - Read `frontend/src/pages/Profile.tsx` — confirm it calls `fetchTriviaStats()` and renders games played, best score, accuracy
    - Evidence: JSX elements, state variables, service call

    **PROF-02: Profile page displays gem balance and tier badge from accounts API**
    - Read `frontend/src/pages/Profile.tsx` — confirm it calls `fetchAccountProfile` and renders gem balance + TierBadge
    - Evidence: JSX elements showing gem_balance, TierBadge component usage

    **PROF-03: Display name from accounts API shown**
    - Read `frontend/src/pages/Profile.tsx` — confirm `display_name` is rendered in the hero section
    - Evidence: JSX showing `accountData.display_name`

    **PROF-04: Identity management links out to accounts platform**
    - Read `frontend/src/pages/Profile.tsx` — confirm "Manage your Empowered account" link exists pointing to ACCOUNTS_WEB_URL
    - Confirm NO name change, password change, or avatar upload forms exist in Profile.tsx
    - Evidence: anchor tag with external link, absence of form elements

    **Format each truth as a table row:**
    ```
    | # | Truth | Status | Evidence |
    ```

    **Required Artifacts section:** List the key files that must exist with their expected contents. Verify each by reading:
    - `frontend/src/services/accountsApi.ts` — exports `accountsApiFetch`, `exchangeRefreshToken`, `fetchAccountProfile`
    - `frontend/src/services/authService.ts` — exports login, signup, logout functions
    - `frontend/src/pages/Login.tsx` — Login component with form
    - `frontend/src/pages/Signup.tsx` — Signup component with form
    - `frontend/src/pages/Profile.tsx` — Profile component with dual-API fetch
    - `frontend/src/components/AuthInitializer.tsx` — token refresh on mount
    - `frontend/src/store/authStore.ts` — Zustand store with setAuth, clearAuth, setDisplayName
    - `frontend/src/types/auth.ts` — AccountsUser, AccountProfile, Tier types

    **Key Link Verification section:** Verify critical wiring:
    - Login.tsx -> authService.login -> accountsApiFetch('/api/auth/login')
    - Signup.tsx -> authService.signup -> accountsApiFetch('/api/auth/signup')
    - AuthInitializer -> exchangeRefreshToken -> Supabase /auth/v1/token
    - Profile.tsx -> fetchAccountProfile -> accountsApiFetch('/api/account/me')
    - Profile.tsx -> fetchTriviaStats -> apiRequest (trivia backend)

    **Build Check section:** Note that `npx tsc --noEmit` was confirmed passing in Phase 43-03 summary and re-confirmed in Phase 44 verification.

    **Anti-Patterns Found section:** Document the three bugs that Phase 45 Plan 01 fixes:
    - Profile.tsx does not sync tier to store (setDisplayName called but not setTier)
    - AuthInitializer does not resolve authoritative tier after token refresh
    - AdminGuard uses user.tier which may be stale after refresh
    - accessToken ?? '' sends malformed Authorization header

    Mark these as "known gaps addressed by Phase 45 Plan 01" — they do NOT cause Phase 43 verification to fail because the requirements (AUTH-04-07, PROF-01-04) are about the integration wiring, not tier correctness.

    **Score: 8/8** — all 8 requirements are structurally satisfied by the committed code.
  </action>
  <verify>
    - File exists at `.planning/phases/43-frontend-auth-profile/43-VERIFICATION.md`
    - File contains all 8 requirement IDs: AUTH-04, AUTH-05, AUTH-06, AUTH-07, PROF-01, PROF-02, PROF-03, PROF-04
    - File has YAML frontmatter with `status: passed` and `score: 8/8`
    - Each truth row has Status = VERIFIED with specific file/line evidence
  </verify>
  <done>
    43-VERIFICATION.md exists with goal-backward verification of all 8 Phase 43 requirements. Each requirement is VERIFIED with specific file path and code evidence. Anti-patterns section documents known tier-sync gaps addressed by Phase 45 Plan 01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ADMIN-01 requirement text in REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
    In `.planning/REQUIREMENTS.md`, find line 51 (the ADMIN-01 requirement):

    **Current text:**
    ```
    - [ ] **ADMIN-01**: `requireAdmin` middleware checks `public.user_roles` table via service role client (not `is_admin` boolean)
    ```

    **Replace with:**
    ```
    - [ ] **ADMIN-01**: `requireAdmin` middleware checks `public.admin_users` table via service role client (not `is_admin` boolean)
    ```

    The only change is `public.user_roles` -> `public.admin_users`. This reflects the actual Empowered Accounts platform schema that was implemented in Phase 41 and confirmed in the Phase 41 VERIFICATION.md.

    Do NOT change any other requirement text. Do NOT mark ADMIN-01 as checked ([x]) — it remains unchecked until the full milestone is verified.
  </action>
  <verify>
    - `grep "ADMIN-01" .planning/REQUIREMENTS.md` shows `admin_users` (not `user_roles`)
    - `grep "user_roles" .planning/REQUIREMENTS.md` returns empty (no remaining references)
    - No other lines in REQUIREMENTS.md were modified (diff shows exactly one line changed)
  </verify>
  <done>
    REQUIREMENTS.md ADMIN-01 text correctly references `public.admin_users`. No other requirements were modified. The stale `user_roles` reference is gone.
  </done>
</task>

</tasks>

<verification>
1. `.planning/phases/43-frontend-auth-profile/43-VERIFICATION.md` exists with frontmatter `status: passed`, `score: 8/8`
2. All 8 requirements (AUTH-04-07, PROF-01-04) appear in the verification report as VERIFIED
3. `grep "user_roles" .planning/REQUIREMENTS.md` returns empty
4. `grep "admin_users" .planning/REQUIREMENTS.md` shows the updated ADMIN-01 line
</verification>

<success_criteria>
- 43-VERIFICATION.md exists and follows the Phase 44 verification format
- All 8 Phase 43 requirements are structurally verified with file-level evidence
- Anti-patterns section documents the tier-sync gaps (for Phase 45 Plan 01 to close)
- ADMIN-01 requirement text says "public.admin_users" (not "public.user_roles")
- No other requirements or verification reports were modified
</success_criteria>

<output>
After completion, create `.planning/phases/45-auth-state-hardening/45-02-SUMMARY.md`
</output>
