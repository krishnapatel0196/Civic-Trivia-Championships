---
phase: 66-gem-award-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/progressionService.ts
  - backend/src/routes/game.ts
  - backend/src/env.ts
  - backend/.env.example
autonomous: true

must_haves:
  truths:
    - "awardPlatformGems() calls POST /api/gems/award via fetch — no Supabase RPC remains"
    - "TRIVIA_GEMS_KEY is validated at startup with a warning if missing"
    - "Game session completes normally even if gem award fails (never-throw pattern)"
    - "connect.credit_gems RPC call is fully removed from TypeScript"
  artifacts:
    - path: "backend/src/services/progressionService.ts"
      provides: "Migrated awardPlatformGems() using fetch to accounts API"
      contains: "api/gems/award"
    - path: "backend/src/env.ts"
      provides: "TRIVIA_GEMS_KEY startup validation warning"
      contains: "TRIVIA_GEMS_KEY"
    - path: "backend/src/routes/game.ts"
      provides: "Updated call-site with idempotencyKey parameter"
      contains: "ctc-gems-"
    - path: "backend/.env.example"
      provides: "TRIVIA_GEMS_KEY entry for developer setup"
      contains: "TRIVIA_GEMS_KEY"
  key_links:
    - from: "backend/src/routes/game.ts"
      to: "backend/src/services/progressionService.ts"
      via: "awardPlatformGems(userId, gemsEarned, gemIdempotencyKey)"
      pattern: "awardPlatformGems.*ctc-gems"
    - from: "backend/src/services/progressionService.ts"
      to: "EMPOWERED_ACCOUNTS_API_URL/api/gems/award"
      via: "fetch POST with X-Service-Key header"
      pattern: "fetch.*api/gems/award"
    - from: "backend/src/env.ts"
      to: "TRIVIA_GEMS_KEY"
      via: "startup warning if missing"
      pattern: "TRIVIA_GEMS_KEY"
---

<objective>
Migrate awardPlatformGems() from the deprecated connect.credit_gems Supabase RPC to POST /api/gems/award on the Empowered Accounts API, secured with TRIVIA_GEMS_KEY. Add startup env validation. Update the game.ts call-site to pass an idempotency key.

Purpose: Removes direct DB coupling for gem awards. CTC gem awards now route through the accounts API like XP awards do, gaining idempotency and audit trail.
Output: Four files updated — progressionService.ts (new implementation), game.ts (call-site), env.ts (startup check), .env.example (new var).
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-gem-award-migration/66-CONTEXT.md
@.planning/phases/66-gem-award-migration/66-RESEARCH.md
@backend/src/services/progressionService.ts
@backend/src/routes/game.ts
@backend/src/env.ts
@backend/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace awardPlatformGems() body and add idempotencyKey parameter</name>
  <files>backend/src/services/progressionService.ts, backend/src/routes/game.ts</files>
  <action>
In `progressionService.ts`:

1. Delete the TODO comment block at lines 101-103 (the "Route through accounts API" comment).

2. Replace the entire `awardPlatformGems()` function (lines 104-124) with a new implementation that mirrors `awardPlatformXp()` structurally. The new function:
   - Adds `idempotencyKey: string` as a third parameter
   - Checks `EMPOWERED_ACCOUNTS_API_URL` and `TRIVIA_GEMS_KEY` env vars at entry — returns `{ confirmed: false, error: 'Missing env vars' }` with a console.warn if either is missing
   - Calls `fetch(accountsUrl + '/api/gems/award', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Service-Key': gemsKey }, body: JSON.stringify({ userId, gemType: 'yellow', amount, idempotencyKey }) })`
   - On non-ok response: reads `resp.text()`, logs warning, returns `{ confirmed: false, error }`
   - On success: returns `{ confirmed: true }`
   - Wraps entire fetch in try/catch — catch logs warning and returns `{ confirmed: false, error: err?.message }`
   - Does NOT use `withRetry` — plain try/catch only (no retry per CONTEXT decision)
   - Return type stays `Promise<{ confirmed: boolean; error?: string }>` — unchanged

3. Update the JSDoc comment above the function to reflect the new implementation (accounts API call, not Supabase RPC). Remove the "Retries up to 3 times" line.

4. If `withRetry` import is no longer used by any other function in the file, leave it (other functions may use it). Do NOT remove imports that other functions depend on.

In `game.ts`:

5. Find the call to `awardPlatformGems(session.userId, gemsEarned)` (around line 405). Add the idempotency key:
   ```typescript
   const gemIdempotencyKey = `ctc-gems-${session.sessionId}-${session.userId}`;
   const gemResult = await awardPlatformGems(session.userId, gemsEarned, gemIdempotencyKey);
   ```
   Follow the exact same pattern used for XP idempotency key in the same file (search for `ctc-game-` to see the pattern).

Anti-patterns to avoid:
- Do NOT use `withRetry` — CONTEXT says no retry for gem awards
- Do NOT use `Authorization: Bearer` header — use `X-Service-Key` to match `awardPlatformXp()`
- Do NOT touch the Supabase DB function — code-only removal
- Do NOT change the return type of awardPlatformGems — callers expect `{ confirmed: boolean; error?: string }`
  </action>
  <verify>Run `npx tsc --noEmit` from the backend directory — zero type errors. Grep for `credit_gems` in backend/src/ — zero matches (RPC call fully removed). Grep for `withRetry` in progressionService.ts — should only appear in functions OTHER than awardPlatformGems.</verify>
  <done>awardPlatformGems() calls POST /api/gems/award with X-Service-Key header and idempotencyKey. No Supabase RPC call remains. game.ts passes 3 args including ctc-gems- idempotency key. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add TRIVIA_GEMS_KEY startup validation and .env.example entry</name>
  <files>backend/src/env.ts, backend/.env.example</files>
  <action>
In `env.ts`:

1. After the existing `_requiredForAccounts` block (lines 17-21), add a new validation block for the gems key:
   ```typescript
   const _requiredForGems = ['TRIVIA_GEMS_KEY'];
   const _missingGems = _requiredForGems.filter(k => !process.env[k]);
   if (_missingGems.length > 0) {
     console.warn(`[env] Missing env vars (gem awards will be skipped): ${_missingGems.join(', ')}`);
   }
   ```
   This is warning-only — server boots normally without the key. Gem awards silently fail at runtime (the env var check inside awardPlatformGems handles runtime safety).

In `.env.example`:

2. Add the new variable after the `TRIVIA_SERVICE_KEY` entry, grouped with the other Empowered Accounts vars:
   ```
   # Service-to-service auth key for gem awards — scope: ["yellow"]
   TRIVIA_GEMS_KEY=your-trivia-gems-key-here
   ```
  </action>
  <verify>Run `npx tsc --noEmit` from the backend directory — zero type errors. Read env.ts and confirm three validation blocks exist (XP, accounts, gems). Read .env.example and confirm TRIVIA_GEMS_KEY is present.</verify>
  <done>TRIVIA_GEMS_KEY validated at startup with warning-only behavior. .env.example documents the new variable for other developers.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end compilation check and codebase cleanup verification</name>
  <files>backend/src/services/progressionService.ts</files>
  <action>
Run a full verification sweep:

1. `npx tsc --noEmit` from backend/ — must pass with zero errors
2. Grep entire backend/src/ for `credit_gems` — must return zero matches (RPC fully removed)
3. Grep entire backend/src/ for `schema('connect')` — if the only remaining uses are in OTHER files (not progressionService.ts), that is fine. If progressionService.ts still references it, something was missed.
4. Grep progressionService.ts for `TODO.*gems\|TODO.*award\|TODO.*accounts` (case insensitive) — the old TODO comment must be gone
5. Verify awardPlatformGems function signature has 3 parameters (userId, amount, idempotencyKey)
6. Verify game.ts contains `ctc-gems-` idempotency key pattern

If any check fails, fix the issue before proceeding.
  </action>
  <verify>All 6 checks above pass. `npx tsc --noEmit` returns exit code 0.</verify>
  <done>Codebase is clean: no RPC references, no stale TODOs, TypeScript compiles, all call-sites updated. Phase 66 code changes are complete and ready for deploy + manual production verification.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — zero type errors
2. `grep -r "credit_gems" backend/src/` — zero matches
3. `grep -r "schema('connect')" backend/src/services/progressionService.ts` — zero matches
4. `grep "TRIVIA_GEMS_KEY" backend/src/env.ts` — present
5. `grep "TRIVIA_GEMS_KEY" backend/.env.example` — present
6. `grep "ctc-gems-" backend/src/routes/game.ts` — present
7. `grep "api/gems/award" backend/src/services/progressionService.ts` — present
</verification>

<success_criteria>
- awardPlatformGems() uses fetch to POST /api/gems/award with X-Service-Key header
- connect.credit_gems RPC call completely removed from TypeScript
- withRetry NOT used in awardPlatformGems (plain try/catch only)
- game.ts passes idempotencyKey as third argument
- TRIVIA_GEMS_KEY validated at startup (warning-only)
- .env.example documents TRIVIA_GEMS_KEY
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/66-gem-award-migration/66-01-SUMMARY.md`
</output>
