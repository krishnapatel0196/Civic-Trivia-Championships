---
phase: 71-leaderboard-cache-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/routes/leaderboard.ts]
autonomous: false

must_haves:
  truths:
    - "Leaderboard cache TTL is 60 seconds, not 300"
    - "Both cache key patterns (entries and rank) use the reduced TTL"
    - "A player who earns XP sees updated leaderboard within ~1 minute"
  artifacts:
    - path: "backend/src/routes/leaderboard.ts"
      provides: "Leaderboard route with 60-second cache"
      contains: "CACHE_TTL = 60"
  key_links:
    - from: "CACHE_TTL constant"
      to: "cacheSet calls (lines ~367, ~375)"
      via: "module-level constant reference"
      pattern: "cacheSet\\(.*CACHE_TTL\\)"
---

<objective>
Reduce leaderboard cache TTL from 300 seconds (5 minutes) to 60 seconds so players see updated XP rankings within approximately one minute of earning XP.

Purpose: Players currently wait up to 5 minutes to see their earned XP reflected on the leaderboard. This causes confusion — they finish a game, check the leaderboard, and see stale data.
Output: Updated `backend/src/routes/leaderboard.ts` with 60-second cache TTL.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/routes/leaderboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce CACHE_TTL to 60 seconds</name>
  <files>backend/src/routes/leaderboard.ts</files>
  <action>
Two changes in backend/src/routes/leaderboard.ts:

1. Line 2 of the file header comment: change "5-minute cache" to "60-second cache"
   - Before: `GET /api/leaderboard — Public leaderboard route with 5-minute cache.`
   - After: `GET /api/leaderboard — Public leaderboard route with 60-second cache.`

2. Line 29: change the CACHE_TTL constant from 300 to 60
   - Before: `const CACHE_TTL = 300; // 5 minutes in seconds`
   - After: `const CACHE_TTL = 60; // 60 seconds`

Do NOT:
- Add an environment variable override for TTL (over-engineering, explicitly called out in research as anti-pattern)
- Create separate TTLs for entries vs rank keys (both must use the same constant)
- Add any cache invalidation logic (LEAD-F01 is deferred)
- Touch any other files
  </action>
  <verify>
Run: `grep -n "CACHE_TTL" backend/src/routes/leaderboard.ts` — should show `const CACHE_TTL = 60;` on line 29 and two cacheSet references using CACHE_TTL.
Run: `grep "60-second cache" backend/src/routes/leaderboard.ts` — should match the header comment.
Run: `npx tsc --noEmit --project backend/tsconfig.json` — TypeScript compiles without errors.
  </verify>
  <done>CACHE_TTL is 60 (not 300), header comment says "60-second cache", TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reduced leaderboard cache TTL from 300s to 60s. After deploying to Render, the leaderboard will refresh within ~1 minute instead of ~5 minutes.</what-built>
  <how-to-verify>
1. Deploy backend to Render (push to master or trigger manual deploy)
2. Wait 5 minutes for any existing Redis cache entries with old 300s TTL to expire
3. Play a full game as a Connected account and finish (earning XP)
4. Navigate to /leaderboard
5. Verify your XP total and rank reflect the XP you just earned (within ~1 minute of game completion)
6. If rank is stale, wait 60 seconds and refresh — it should update
  </how-to-verify>
  <resume-signal>Type "approved" if leaderboard reflects recent XP within ~1 minute, or describe what you observe</resume-signal>
</task>

</tasks>

<verification>
- `grep "CACHE_TTL = 60" backend/src/routes/leaderboard.ts` returns a match
- `grep "60-second cache" backend/src/routes/leaderboard.ts` returns a match
- TypeScript compiles without errors
- No other files modified
</verification>

<success_criteria>
- LEAD-01: CACHE_TTL changed from 300 to 60
- LEAD-02: After deploy, leaderboard reflects XP earned within ~1 minute (manual verification)
</success_criteria>

<output>
After completion, create `.planning/phases/71-leaderboard-cache-fix/71-01-SUMMARY.md`
</output>
