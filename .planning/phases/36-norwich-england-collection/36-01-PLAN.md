---
phase: 36-norwich-england-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/scripts/content-generation/locale-configs/norwich-uk.ts
  - backend/src/scripts/content-generation/prompts/system-prompt.ts
  - backend/src/scripts/content-generation/generate-locale-questions.ts
  - backend/src/db/seed/collections.ts
  - backend/src/db/seed/seed-community.ts
  - backend/src/scripts/merge-generated-questions.ts
autonomous: true

must_haves:
  truths:
    - "norwich-uk locale config exists with UK civic terminology, two-tier governance fields, and forbidden US terms"
    - "Generation pipeline builds Norwich voice guidance into system prompt when locale is norwich-uk"
    - "Norwich collection is registered in seed data so db:seed creates the collection row"
    - "seed-community.ts knows about norwich-uk and can seed its questions"
    - "merge-generated-questions.ts accepts norwich-uk as a valid collection slug"
  artifacts:
    - path: "backend/src/scripts/content-generation/locale-configs/norwich-uk.ts"
      provides: "Norwich locale config with topic distribution, source URLs, two-tier governance fields"
      exports: ["norwichConfig"]
    - path: "backend/src/scripts/content-generation/prompts/system-prompt.ts"
      provides: "buildNorwichVoiceGuidance function gated on norwich-uk slug"
      contains: "buildNorwichVoiceGuidance"
    - path: "backend/src/db/seed/collections.ts"
      provides: "Norwich collection entry in collectionsData array"
      contains: "norwich-uk"
  key_links:
    - from: "backend/src/scripts/content-generation/generate-locale-questions.ts"
      to: "locale-configs/norwich-uk.ts"
      via: "dynamic import in loadLocaleConfig"
      pattern: "norwich-uk.*import.*norwich-uk"
    - from: "backend/src/scripts/content-generation/prompts/system-prompt.ts"
      to: "buildNorwichVoiceGuidance"
      via: "conditional call gated on localeSlug"
      pattern: "norwich-uk.*buildNorwichVoiceGuidance"
---

<objective>
Create all code infrastructure needed to generate and seed Norwich, England questions.

Purpose: This plan adds the locale config, voice guidance prompt, collection registry entry, and updates to the generation/seed/merge scripts so that Plan 02 can run the generation pipeline and seed questions.

Output: All source files ready for `npx tsx src/scripts/content-generation/generate-locale-questions.ts --locale norwich-uk`
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-norwich-england-collection/36-CONTEXT.md
@.planning/phases/36-norwich-england-collection/36-RESEARCH.md

Key source files to study before writing:
@backend/src/scripts/content-generation/locale-configs/fremont-ca.ts (pattern for locale config)
@backend/src/scripts/content-generation/locale-configs/bloomington-in.ts (LocaleConfig interface definition)
@backend/src/scripts/content-generation/prompts/system-prompt.ts (where to add voice guidance)
@backend/src/scripts/content-generation/generate-locale-questions.ts (loadLocaleConfig function to update)
@backend/src/db/seed/collections.ts (collectionsData array to add entry)
@backend/src/db/seed/seed-community.ts (LOCALES array to add entry)
@backend/src/scripts/merge-generated-questions.ts (JSON_FILE_MAP to add entry)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Norwich locale config + voice guidance prompt</name>
  <files>
    backend/src/scripts/content-generation/locale-configs/norwich-uk.ts
    backend/src/scripts/content-generation/prompts/system-prompt.ts
  </files>
  <action>
**1a. Create `backend/src/scripts/content-generation/locale-configs/norwich-uk.ts`**

Model on fremont-ca.ts. Import `LocaleConfig` from `./bloomington-in.js`. Export `norwichConfig: LocaleConfig`.

Fields:
- `locale: 'norwich-uk'`
- `name: 'Norwich, England'`
- `externalIdPrefix: 'nor'`
- `collectionSlug: 'norwich-uk'`
- `targetQuestions: 90`
- `batchSize: 15` (smaller batches for topic-based generation per CONTEXT.md decision)
- `overshootFactor: 1.2` (generate ~108, curate to ~90)

Topic categories (8 topics, per RESEARCH.md batch structure):
1. `city-council-mechanics` — "Norwich City Council structure, procedures, elections, and wards"
2. `city-council-services` — "Norwich City Council services: housing, planning, leisure, bin collections, council tax"
3. `civic-history` — "Norwich civic history: medieval charter, notable milestones, past Lord Mayors"
4. `landmarks-institutions` — "Norwich Cathedral, Norwich Castle, Guildhall, Elm Hill, and cultural institutions"
5. `economy-culture` — "Norwich economy, Norwich Market, UEA, Norwich Research Park, creative industries"
6. `norfolk-county` — "Norfolk County Council responsibilities: roads, schools, social care, libraries"
7. `sports-community` — "Norwich City FC, community events, UNESCO City of Literature, City of Sanctuary"
8. `elections-democracy` — "UK local elections, wards, by-elections, MPs for Norwich, voter participation"

Topic distribution (sums to 90, heavy local government per CONTEXT.md "50%+ dedicated to council mechanics"):
- `city-council-mechanics`: 15
- `city-council-services`: 15
- `civic-history`: 10
- `landmarks-institutions`: 10
- `economy-culture`: 10
- `norfolk-county`: 10 (80/20 split means ~10 county questions)
- `sports-community`: 10
- `elections-democracy`: 10

Source URLs (authoritative Norwich/Norfolk sources):
```
'https://www.norwich.gov.uk',
'https://www.norwich.gov.uk/info/20003/your_council',
'https://www.norwich.gov.uk/info/20004/councillors_democracy_and_elections',
'https://www.norfolk.gov.uk',
'https://www.norfolk.gov.uk/what-we-do-and-how-we-work',
'https://en.wikipedia.org/wiki/Norwich',
'https://en.wikipedia.org/wiki/Norwich_City_Council',
'https://en.wikipedia.org/wiki/Norfolk_County_Council',
'https://www.bbc.co.uk/news/england/norfolk',
'https://www.cathedral.org.uk',
```

Add a JSDoc header comment explaining this is the first non-US collection and documenting the two-tier governance context (City Council vs County Council responsibilities), similar to fremont-ca.ts's consolidation context.

**1b. Add `buildNorwichVoiceGuidance()` to `system-prompt.ts`**

In `backend/src/scripts/content-generation/prompts/system-prompt.ts`:

1. Add `buildNorwichVoiceGuidance()` function (private, not exported) modeled on `buildFremontSensitivityInstructions()`.

Content (from RESEARCH.md, verbatim):
```
VOICE AND PERSPECTIVE:
This question is written by and for the people of Norwich, England.
Write as a knowledgeable local, not as an outside observer.
The tone should feel like it comes from a proud Norfolk resident -- direct, unpretentious, and civic-minded.

REQUIRED TERMINOLOGY (use these, not American equivalents):
- "councillor" (not "council member")
- "ward" (not "district" or "precinct")
- "by-election" (not "special election")
- "MP" or "Member of Parliament" (not "congressman" or "representative")
- "parish council" (not "town board")
- "constituency" (not "district")
- "postcode" (not "zip code")
- "unitary authority" (when discussing local government reorganisation)
- "devolution" (when discussing power transferred from central government)

FORBIDDEN TERMS (never use):
mayor, city hall (use "Norwich City Council" or "Guildhall"), zip code, congressman, senator, precinct, special election, alderman

TWO-TIER GOVERNMENT (critical accuracy requirement):
Norwich operates under a two-tier local government system:
- NORWICH CITY COUNCIL is responsible for: housing, planning, leisure, bin collections, council tax billing, environmental health
- NORFOLK COUNTY COUNCIL is responsible for: roads, schools, education, social care, libraries, public health, fire service

When writing a question about local services or governance, ensure the responsible authority matches the actual responsibility. Do NOT attribute county responsibilities to the city council or vice versa.

NORWICH CITY COUNCIL LORD MAYOR:
Norwich has a Lord Mayor (ceremonial role), not an elected mayor. Do not confuse with executive mayors in other cities.
```

2. Update the return string in `buildSystemPrompt()` to append Norwich guidance. Currently line ~104 has:
```typescript
${localeSlug === 'fremont-ca' ? buildFremontSensitivityInstructions() : ''}
```
Change to:
```typescript
${localeSlug === 'fremont-ca' ? buildFremontSensitivityInstructions() : ''}${localeSlug === 'norwich-uk' ? buildNorwichVoiceGuidance() : ''}
```
  </action>
  <verify>
    Run `npx tsx --eval "import('./backend/src/scripts/content-generation/locale-configs/norwich-uk.js')"` from project root to confirm the module loads without errors. Verify the system-prompt.ts file compiles by running `cd backend && npx tsc --noEmit src/scripts/content-generation/prompts/system-prompt.ts` (or equivalent TypeScript check).
  </verify>
  <done>
    norwich-uk.ts exports a valid LocaleConfig with 8 topics, 90 target questions, 'nor' prefix, and Norwich-specific source URLs. system-prompt.ts includes buildNorwichVoiceGuidance() gated on 'norwich-uk' slug with full UK terminology, forbidden terms, and two-tier governance instructions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Norwich in collection seed, community seed, merge script, and generation loader</name>
  <files>
    backend/src/db/seed/collections.ts
    backend/src/db/seed/seed-community.ts
    backend/src/scripts/merge-generated-questions.ts
    backend/src/scripts/content-generation/generate-locale-questions.ts
  </files>
  <action>
**2a. Add Norwich to `backend/src/db/seed/collections.ts`**

Add a new entry to the `collectionsData` array:
```typescript
{
  name: 'Norwich, England',
  slug: 'norwich-uk',
  description: 'Local democracy, history & culture in the heart of Norfolk',
  localeCode: 'en-GB',
  localeName: 'Norwich, England',
  iconIdentifier: 'flag-gb',
  themeColor: '#1B4332',  // deep forest green (visually distinct from all existing colors)
  isActive: false,        // activated after questions are seeded
  sortOrder: 7
}
```

Note: `localeCode: 'en-GB'` -- this is the first non-US collection. All existing entries use `en-US`.

**2b. Add Norwich to `backend/src/db/seed/seed-community.ts`**

Add to the `LOCALES` array:
```typescript
{ slug: 'norwich-uk', file: 'norwich-uk-questions.json' },
```

**2c. Add Norwich to `backend/src/scripts/merge-generated-questions.ts`**

Add to the `JSON_FILE_MAP`:
```typescript
'norwich-uk': 'norwich-uk-questions.json',
```

**2d. Register Norwich in `generate-locale-questions.ts` loadLocaleConfig()**

In the `supportedLocales` map inside `loadLocaleConfig()`, add:
```typescript
'norwich-uk': () => import('./locale-configs/norwich-uk.js') as Promise<{ norwichConfig: LocaleConfig }>,
```

And add `'norwichConfig'` to the `configKeys` array:
```typescript
const configKeys = ['bloomingtonConfig', 'losAngelesConfig', 'fremontConfig', 'norwichConfig'];
```
  </action>
  <verify>
    Grep for 'norwich-uk' across all four modified files to confirm the slug is registered in each. Run `cd backend && npx tsc --noEmit` to verify no TypeScript errors across the project.
  </verify>
  <done>
    Norwich is registered in all four lookup tables: collectionsData (seed), LOCALES (community seed), JSON_FILE_MAP (merge), and supportedLocales (generation loader). Running `--locale norwich-uk` will be recognized by the generation script. Running `db:seed` will create the norwich-uk collection row (inactive). Running `db:seed:community` will attempt to seed norwich-uk-questions.json (once the file exists).
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` -- full TypeScript compilation passes
2. `grep -r 'norwich-uk' backend/src/` -- confirms slug appears in all expected files (locale config, system prompt, collections seed, community seed, merge script, generate-locale-questions)
3. `grep -r 'buildNorwichVoiceGuidance' backend/src/` -- confirms function exists and is called
4. `grep -r 'norwichConfig' backend/src/` -- confirms config is exported and imported
</verification>

<success_criteria>
- norwich-uk locale config file exists with 8 topics, 90 target, 'nor' prefix, en-GB source URLs
- buildNorwichVoiceGuidance() is defined and gated on 'norwich-uk' slug in system-prompt.ts
- Norwich collection entry in seed/collections.ts with en-GB locale, deep forest green theme, sortOrder 7
- All four registration points (seed, community-seed, merge, generate) recognize 'norwich-uk'
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-norwich-england-collection/36-01-SUMMARY.md`
</output>
