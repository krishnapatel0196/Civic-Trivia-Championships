---
phase: 34-scale-to-90-questions
plan: 05
type: execute
wave: 2
depends_on: ["34-04"]
files_modified:
  - backend/src/data/bloomington-in-questions.json
  - backend/src/data/california-state-questions.json
  - backend/src/data/fremont-ca-questions.json
  - backend/src/data/indiana-state-questions.json
  - backend/src/data/los-angeles-ca-questions.json
  - backend/src/scripts/verify-phase34-final.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 5 non-Federal collections have 90+ active questions in the database"
    - "Total question bank is 540+ unique active questions across 6 collections"
    - "No newly generated or re-activated question has a blocking quality violation"
    - "Zero active duplicates across all collections"
  artifacts:
    - path: "backend/src/scripts/verify-phase34-final.ts"
      provides: "Final verification script confirming all 5 collections at 90+ active"
      min_lines: 50
  key_links:
    - from: "backend/src/scripts/generateQuestions.ts"
      to: "Anthropic API"
      via: "claude-sonnet-4-6 model"
      pattern: "claude-sonnet-4-6"
    - from: "backend/src/scripts/merge-generated-questions.ts"
      to: "backend/src/data/*-questions.json"
      via: "JSON_FILE_MAP + writeFileSync"
      pattern: "writeFileSync"
    - from: "backend/src/db/seed/seed-community.ts"
      to: "civic_trivia.questions"
      via: "INSERT with ON CONFLICT DO NOTHING"
      pattern: "onConflictDoNothing"
---

<objective>
Generate additional questions for collections still below 90 active after re-activation, seed them, and verify all collections meet the 90-question target.

Purpose: Plan 34-04 re-activated archived questions, but some collections may still be below 90 (especially Fremont, LA, Bloomington where archived count was less than the deficit). This plan generates the remaining questions using the existing pipeline, merges and seeds them, then runs a final verification confirming all success criteria are met.

Output: All 5 non-Federal collections at 90+ active questions. Final verification script with PASS on all checks.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-scale-to-90-questions/34-VERIFICATION.md
@.planning/phases/34-scale-to-90-questions/34-04-SUMMARY.md

Key files:
@backend/src/scripts/generateQuestions.ts (generation pipeline: --collection X --target N)
@backend/src/scripts/merge-generated-questions.ts (merge: --input FILE --collection SLUG)
@backend/src/db/seed/seed-community.ts (seed: npm run db:seed:community)
@backend/src/scripts/verify-no-active-dups.ts (duplicate check)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate, merge, and seed remaining questions for deficit collections</name>
  <files>
    backend/src/data/bloomington-in-questions.json
    backend/src/data/california-state-questions.json
    backend/src/data/fremont-ca-questions.json
    backend/src/data/indiana-state-questions.json
    backend/src/data/los-angeles-ca-questions.json
  </files>
  <action>
Read the 34-04-SUMMARY.md to get the exact remaining deficit per collection after re-activation.

For EACH collection that is still below 90 active questions, run the existing generation pipeline:

1. **Calculate generation target.** For each deficit collection, target = 90 + 15% buffer (overshoot to ~104). The pipeline's quality filter and semantic dedup will reduce the accepted count, so overshooting is necessary.

2. **Generate questions.** Run from the backend/ directory:
   ```bash
   npx tsx src/scripts/generateQuestions.ts --collection {slug} --target {target}
   ```
   Where {slug} uses the pipeline's slug format:
   - bloomington-in -> bloomington-in
   - los-angeles-ca -> los-angeles-ca
   - fremont-ca -> fremont-ca
   - indiana-state -> indiana (check the COLLECTION_PREFIXES in generateQuestions.ts)
   - california-state -> california (check the COLLECTION_PREFIXES in generateQuestions.ts)

   IMPORTANT: The `--collection` flag uses the slug as defined in generateQuestions.ts COLLECTION_NAMES/COLLECTION_PREFIXES, NOT the DB slug. Check the mapping at the top of generateQuestions.ts. The DB slugs are indiana-state and california-state but the pipeline may use indiana and california.

   If a collection's generation run produces insufficient questions (source exhaustion), accept what the pipeline produces per the CONTEXT.md decision: "accept what the pipeline produces and ship it."

3. **Merge generated questions into source JSON files.** For each generated file:
   ```bash
   npx tsx src/scripts/merge-generated-questions.ts --input generated-questions-{slug}.json --collection {slug}
   ```

4. **Seed all collections to database:**
   ```bash
   npm run db:seed:community
   ```
   (This is safe to re-run — ON CONFLICT DO NOTHING means existing questions are untouched. Only net-new questions from the merge step get inserted.)

5. **Verify no active duplicates:**
   ```bash
   npx tsx src/scripts/verify-no-active-dups.ts
   ```

Skip generation for any collection that already has 90+ active after Plan 34-04's re-activation. Only generate for collections that are still below 90.

If ALL collections are already at 90+ after Plan 34-04 (re-activation alone closed all gaps), skip generation entirely and proceed directly to Task 2.
  </action>
  <verify>
1. Each generation run exits 0 (or reports source exhaustion gracefully)
2. Merge runs complete without collision errors on new questions
3. Seed completes with "Questions seeded: N" output per collection
4. verify-no-active-dups.ts reports zero active duplicates
  </verify>
  <done>
- All deficit collections have had additional questions generated, merged, and seeded
- OR: All collections were already at 90+ after re-activation (no generation needed)
- Zero active duplicates confirmed
  </done>
</task>

<task type="auto">
  <name>Task 2: Create and run final verification script</name>
  <files>backend/src/scripts/verify-phase34-final.ts</files>
  <action>
Create `backend/src/scripts/verify-phase34-final.ts` that performs the definitive Phase 34 verification:

1. **Check 1: Per-collection active counts.** Query:
   ```sql
   SELECT c.slug, c.name, COUNT(q.id) as active_count
   FROM civic_trivia.collections c
   JOIN civic_trivia.collection_questions cq ON c.id = cq.collection_id
   JOIN civic_trivia.questions q ON cq.question_id = q.id AND q.status = 'active'
   GROUP BY c.slug, c.name
   ORDER BY c.slug
   ```
   For each of the 5 non-Federal collections, check active_count >= 90. Print PASS/FAIL per collection.

2. **Check 2: Total bank.** Sum all active questions. Must be >= 540. Print PASS/FAIL.

3. **Check 3: Quality sanity.** Query all active questions and verify none have quality_score below 50 (the blocking threshold). This confirms no bad questions slipped through.

4. **Check 4: Federal unchanged.** Federal collection must still have 114 active. Print PASS/FAIL.

5. **Print final summary table:**
   ```
   Collection              Active    Target    Result
   --------------------------------------------------
   bloomington-in          NN        90        PASS/FAIL
   california-state        NN        90        PASS/FAIL
   fremont-ca              NN        90        PASS/FAIL
   indiana-state           NN        90        PASS/FAIL
   los-angeles-ca          NN        90        PASS/FAIL
   federal                 114       (N/A)     PASS
   --------------------------------------------------
   TOTAL                   NNN       540       PASS/FAIL

   PHASE 34 FINAL STATUS: PASS / FAIL
   ```

Follow the same import patterns as verify-phase34.ts:
- `import '../env.js';` first
- `import { db } from '../db/index.js';`
- `import { sql } from 'drizzle-orm';`
- `import { pool } from '../config/database.js';`

Run the script:
```bash
cd backend && npx tsx src/scripts/verify-phase34-final.ts
```

If any collection is STILL below 90 despite re-activation + generation (source truly exhausted), document the final state honestly. The CONTEXT.md decision applies: accept what the pipeline produces.
  </action>
  <verify>
1. Script exits 0
2. Output shows per-collection PASS/FAIL
3. All 5 non-Federal collections show PASS (>= 90 active) — or document the accepted shortfall
4. Total bank shows PASS (>= 540) — or document the accepted shortfall
5. Federal unchanged at 114
  </verify>
  <done>
- verify-phase34-final.ts created and executed
- Output shows all collections at 90+ active (ideal) or documents accepted shortfalls
- Phase 34 gap closure is complete — VERIFICATION.md gaps are addressed
  </done>
</task>

</tasks>

<verification>
- verify-phase34-final.ts shows PASS for all checks (or documents accepted shortfalls for exhausted sources)
- verify-no-active-dups.ts confirms zero duplicates
- Total active question count >= 540 (or maximum achievable)
- Federal collection unchanged at 114
</verification>

<success_criteria>
- All 5 non-Federal collections have 90+ active questions in database (primary goal)
- Total active bank reaches 540+ across 6 collections
- Zero active duplicates
- Zero blocking quality violations in any active question
- Federal collection unchanged
- If any collection cannot reach 90 due to source exhaustion, the shortfall is documented with the CONTEXT.md decision rationale
</success_criteria>

<output>
After completion, create `.planning/phases/34-scale-to-90-questions/34-05-SUMMARY.md`

The SUMMARY must include:
1. Final active counts for all 6 collections
2. Whether each collection meets the 90 target
3. Total active bank
4. Phase 34 final status: PASS or ACCEPTED SHORTFALL
</output>
