---
phase: 60-washington-dc-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/seed/collections.ts
  - backend/src/scripts/content-generation/locale-configs/washington-dc.ts
  - backend/src/scripts/content-generation/generate-locale-questions.ts
autonomous: false

must_haves:
  truths:
    - "Washington, DC collection exists in the database with slug 'washington-dc' and prefix 'wdc'"
    - "Locale config uses city tier shape (single named export, NOT state two-export pattern) with district framing in voice guidance"
    - "At least 80 draft questions are in 'active' status after generation and curation"
    - "All current-officeholder questions carry correct expiresAt values (2027-01-02 for Mayor/AG/Chair, 2027-01-03 for Delegate); durable civic facts carry null"
    - "No federal institution questions (Congress, Supreme Court, White House, national monuments) remain in the active set"
    - "Questions never call DC a 'city' or 'state' — always 'the District' or 'Washington, DC'"
  artifacts:
    - path: "backend/src/scripts/content-generation/locale-configs/washington-dc.ts"
      provides: "Washington, DC locale config — topic categories, distribution, source URLs, voice guidance"
      exports: ["washingtonDcConfig"]
    - path: "backend/src/db/seed/collections.ts"
      provides: "Washington, DC seed entry (sortOrder: 14, isActive: false, themeColor: #C41E3A)"
      contains: "washington-dc"
  key_links:
    - from: "washington-dc.ts"
      to: "generate-locale-questions.ts city config registration"
      via: "scaffold registers city configs in generate-locale-questions.ts"
      pattern: "washingtonDcConfig"
    - from: "generate-locale-questions.ts"
      to: "Washington DC questions in DB"
      via: "--locale washington-dc --fetch-sources"
      pattern: "wdc-[0-9]+"
---

<objective>
Scaffold the Washington, DC collection, author a complete locale config with district-framing voice guidance, run content generation, and curate the draft question set to at least 80 active questions.

Purpose: Establishes DC as a playable collection with questions that make DC's unique constitutional status legible and interesting. DC uses the `city` tier (no district tier exists in the schema) with district framing expressed entirely through voice guidance.

Output: Washington, DC locale config file, seed entry in collections.ts, and at least 80 curated active questions in the database.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-washington-dc-collection/60-RESEARCH.md
@.planning/COLLECTION-PLAYBOOK.md
@backend/src/scripts/content-generation/locale-configs/portland-or.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Washington, DC and author locale config</name>
  <files>
    backend/src/db/seed/collections.ts
    backend/src/scripts/content-generation/locale-configs/washington-dc.ts
    backend/src/scripts/content-generation/generate-locale-questions.ts
  </files>
  <action>
**Step 1 — Run scaffold command:**

```bash
cd backend
npx tsx src/scripts/scaffold-collection.ts \
  --name "Washington, DC" \
  --slug washington-dc \
  --prefix wdc \
  --theme "#C41E3A" \
  --tier city \
  --description "The District has no senators. Does that stump you?" \
  --sort-order 14
```

**Step 2 — Apply Scaffold Bug 2 workaround immediately:**

```bash
git diff backend/src/scripts/content-generation/generate-locale-questions.ts
```

If the file shows a corrupted type annotation line (string injected into a TypeScript type), revert it:

```bash
git checkout backend/src/scripts/content-generation/generate-locale-questions.ts
```

IMPORTANT: City configs DO need registration in generate-locale-questions.ts (unlike state configs which are auto-discovered). The scaffold script handles this registration automatically. Only revert if the file is corrupted (extra string in a type annotation), NOT because it was modified. Check the diff carefully — a clean import + registration is correct; a mangled type line is the bug.

**Step 3 — Run seed:**

```bash
npx tsx src/db/seed/seed.ts
```

**Step 4 — Verify seed entry in collections.ts has sortOrder: 14.** If scaffold inserted a different sortOrder, manually correct it. Also check `iconIdentifier` — if `flag-dc` is not a valid frontend identifier, change to `flag-us`.

**Step 5 — Author the locale config at `backend/src/scripts/content-generation/locale-configs/washington-dc.ts`.**

Model on `portland-or.ts` (city config shape — single named export `washingtonDcConfig`, NOT the two-export state pattern). The config must include:

`washingtonDcConfig` with:
- `locale: 'washington-dc'`
- `name: 'Washington, DC'`
- `externalIdPrefix: 'wdc'`
- `collectionSlug: 'washington-dc'`
- `targetQuestions: 100` (1.4x buffer for 80+ active target)
- `batchSize: 25`
- `overshootFactor: 1.4`

**`topicCategories` (6 topics):**

1. `dc-government-structure` — "The DC Council (13 members: Chairman elected at-large + 4 at-large members + 8 ward members, all 4-year terms, no term limits), Mayor (executive, 4-year term), DC Attorney General (elected since 2015, previously appointed by Mayor), Home Rule Charter (Congress enacted December 24, 1973; DC voters approved May 1974 by 4-to-1 margin), 103 years without elected local government (1871-1974), Board of Commissioners predecessor, Congressional override authority (30-day review period for DC legislation). Current Mayor: Muriel Bowser (not running for 4th term, expiresAt: 2027-01-02). Current Council Chair: Phil Mendelson (expiresAt: 2027-01-02). Current AG: Brian Schwalb (expiresAt: 2027-01-02). FRAMING: NEVER call DC a 'city' — always 'the District' or 'Washington, DC'. The DC Council is NOT a 'city council.' The John A. Wilson Building is where the Mayor and Council work — NOT the US Capitol."

2. `constitutional-status` — "Article I Section 8 Clause 17 (District Clause — Congress has exclusive legislative authority over the federal district). No voting representation in Congress: no senators, no voting House member. Non-voting Delegate to US House (can vote in committee, NOT on floor; Eleanor Holmes Norton serving since 1991, retiring 2026, expiresAt: 2027-01-03). 23rd Amendment (ratified 1961, DC gets electoral votes equal to least-populous state — currently 3; first presidential vote 1964). 'Taxation Without Representation' license plate motto. DC Voting Rights Amendment (1978, passed Congress but expired 1985 without ratification). DC statehood debate (proposed name: Washington, Douglass Commonwealth, honoring Frederick Douglass; House passed statehood bill 2019 and 2021, Senate has not)."

3. `dc-courts-judiciary` — "DC Superior Court (trial court, Chief Judge + 50 associate judges). DC Court of Appeals (DC's highest court, equivalent to a state supreme court, Chief Judge + 8 associate judges). CRITICAL: ALL DC judges are appointed by the President of the United States and confirmed by the US Senate — NOT elected, NOT appointed by the Mayor. This is a direct consequence of DC's federal status and is a distinguishing civic fact. Federal courts in DC (US District Court for DC, DC Circuit Court of Appeals) are NOT DC local courts — they are federal courts that happen to sit in DC."

4. `ward-neighborhood-structure` — "DC is divided into 8 wards, each represented by one Council member. Advisory Neighborhood Commissions (ANCs): 37 ANCs created by Home Rule Act, first elections 1975; each represents ~2,000 residents. ANCs are advisory — they vote on local issues but cannot pass binding laws. Ward boundaries redrawn after each decennial census. DC is also divided into quadrants (NW, NE, SE, SW) radiating from the Capitol building."

5. `dc-history` — "Capital established by Congress in 1790 (Residence Act). President Washington selected the Potomac site. Capital moved from Philadelphia in 1800. Pierre Charles L'Enfant designed the original city plan (1791). DC created from land ceded by Maryland and Virginia; Virginia's portion retroceded in 1846. Marion Barry: 4-term Mayor (1979-1991, 1995-1999), DC's most prominent Mayor. Sharon Pratt: first Black woman elected Mayor of a major US city (1991). DC budget is subject to Congressional approval."

6. `dc-civic-identity` — "John A. Wilson Building: DC City Hall at 1350 Pennsylvania Avenue NW, where the Mayor and DC Council work. NOT to be confused with the US Capitol. DC population ~700,000 (larger than Wyoming and Vermont but with no Senate representation). 'Chocolate City' historical nickname (1970s). DC Public Schools (DCPS) under DC government, not federal control. DC budget process: submitted by Mayor, passed by Council, subject to Congressional appropriation review."

**`topicDistribution`** (must sum to 100):
- `dc-government-structure`: 30
- `constitutional-status`: 25
- `dc-courts-judiciary`: 15
- `ward-neighborhood-structure`: 10
- `dc-history`: 12
- `dc-civic-identity`: 8

**`sourceUrls`** (Wikipedia preferred):
```
'https://en.wikipedia.org/wiki/Washington,_D.C.',
'https://en.wikipedia.org/wiki/District_of_Columbia_home_rule',
'https://en.wikipedia.org/wiki/District_of_Columbia_Home_Rule_Act',
'https://en.wikipedia.org/wiki/Council_of_the_District_of_Columbia',
'https://en.wikipedia.org/wiki/Government_of_the_District_of_Columbia',
'https://en.wikipedia.org/wiki/Mayor_of_the_District_of_Columbia',
'https://en.wikipedia.org/wiki/Twenty-third_Amendment_to_the_United_States_Constitution',
'https://en.wikipedia.org/wiki/District_of_Columbia_voting_rights',
'https://en.wikipedia.org/wiki/Washington_D.C._statehood_movement',
'https://en.wikipedia.org/wiki/District_of_Columbia_Court_of_Appeals',
'https://en.wikipedia.org/wiki/Superior_Court_of_the_District_of_Columbia',
'https://en.wikipedia.org/wiki/History_of_Washington,_D.C.',
'https://en.wikipedia.org/wiki/District_of_Columbia_Delegate',
'https://en.wikipedia.org/wiki/Advisory_Neighborhood_Commission',
```

Do NOT use dc.gov subdomains as primary sources — they return navigation/sitemap content (carry-forward rule from Portland Phase 58).

**FORBIDDEN content — voice guidance must explicitly state:**
- Questions about the US Congress, US Supreme Court, the White House, or the President as DC civic institutions (these belong in the Federal collection)
- Questions about national monuments (Lincoln Memorial, Washington Monument, Jefferson Memorial, Smithsonian) — these are National Park Service / Smithsonian Institution and belong in the Federal collection
- Questions calling DC a "city" or "state" — always "the District" or "Washington, DC"
- Questions about the US Capitol as DC's civic landmark (the Wilson Building is DC's seat of government)
- Confusion between the federal Attorney General and the DC Attorney General (distinct offices)
  </action>
  <verify>
    1. `backend/src/scripts/content-generation/locale-configs/washington-dc.ts` exists
    2. File exports `washingtonDcConfig` with `locale: 'washington-dc'`, `targetQuestions: 100`
    3. `generate-locale-questions.ts` has clean registration for washington-dc (no corrupted type annotations)
    4. Washington, DC seed entry appears in `backend/src/db/seed/collections.ts` with `slug: 'washington-dc'`, `themeColor: '#C41E3A'`, `tier: 'city'`, `isActive: false`
    5. TypeScript compiles: `cd backend && npx tsc --noEmit`
  </verify>
  <done>Washington, DC collection seeded and locale config complete. TypeScript compiles without errors. Scaffold Bug 2 workaround applied if triggered. City config registration intact in generate-locale-questions.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Washington, DC questions</name>
  <files>
    backend/src/scripts/content-generation/generate-locale-questions.ts
  </files>
  <action>
**Step 1 — Pre-generation source spot-check:**

Wikipedia URLs are the primary sources and are handled by the Phase 58-03 w/api.php fix. No dc.gov URLs in the primary list. Sources are safe to fetch.

**Step 2 — Run generation:**

```bash
cd backend
npx tsx src/scripts/content-generation/generate-locale-questions.ts \
  --locale washington-dc \
  --fetch-sources
```

Generation takes ~30 minutes. Semantic dedup runs automatically after all batches complete (Phase 57 feature). Watch for:
- Total generated count (expect ~130 questions before dedup — DC is content-rich)
- Dedup archive count (expect 20-40% reduction)
- Final active question count (target: 80+; if below 80, note for gap-fill)

Content saturation is less likely than Oregon (DC has very dense civic content across 6 distinct topic areas), but plan for 1-2 gap-fill passes if fewer than 60 unique questions after 2 runs.

If the active count after generation is below 80, do NOT stop. Proceed to the curation checkpoint.

**Step 3 — Log generation results:**

```bash
cd backend
npx tsx src/scripts/audit-collection-readiness.ts --slug washington-dc --prefix wdc
```

Note the output: total active count, expiring question ratio, any warnings.
  </action>
  <verify>
    Run `npx tsx src/scripts/audit-collection-readiness.ts --slug washington-dc --prefix wdc` and confirm:
    - Active question count is reported (even if below 80 — curation checkpoint handles gap-fill)
    - Expiring question ratio is reported
    - No unrecoverable errors (TypeScript errors, DB connection failures)
  </verify>
  <done>Generation complete. Audit report printed. Active question count and expiring ratio logged. Semantic dedup ran automatically during generation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Washington, DC question set — generated and semantically deduped. Collection exists in the database with draft questions ready for admin panel review.

    Current status from audit: [Claude will print the active count and expiring ratio here before pausing]
  </what-built>
  <how-to-verify>
    1. Open the admin panel: https://civic-trivia-backend.onrender.com/admin (or local: http://localhost:3001/admin)
    2. Find the Washington, DC collection
    3. Review ALL draft questions. Archive any that:
       - Are about federal institutions: US Congress, US Supreme Court, the White House, the President acting in federal (not DC-local) capacity
       - Are about national monuments: Lincoln Memorial, Washington Monument, Jefferson Memorial, WWII Memorial, Smithsonian museums
       - Reference the US Capitol as DC's civic building (the Wilson Building is DC City Hall)
       - Call DC a "city" or a "state" instead of "the District" or "Washington, DC"
       - Confuse the federal Attorney General with the DC Attorney General
       - State that DC judges are elected or appointed by the Mayor (they are presidentially appointed)
       - Reference website navigation structure ("Which section of dc.gov...")
       - Are factually wrong or misleading
       - Have weak answer distractors

    4. Confirm the active count AFTER curation:
       - Target: at least 80 active questions
       - If count is below 80 after curation: run a gap-fill generation batch targeting weak topic areas before typing "approved"

    5. Check expiring question ratio (from audit):
       - Target: 15-30% of active questions have expiresAt set
       - Expiring targets: Mayor Bowser (2027-01-02), Council Chair Mendelson (2027-01-02), AG Schwalb (2027-01-02), Delegate (2027-01-03), plus individual ward/at-large council members
       - If below 15%: check whether individual at-large or ward council member questions could boost the ratio. If not, accept the lower ratio and document in retrospective. Do NOT force artificial expiring questions.

    **Gap-fill command if needed (below 80 active questions):**
    ```bash
    cd backend
    npx tsx src/scripts/content-generation/generate-locale-questions.ts \
      --locale washington-dc \
      --fetch-sources \
      --topic [weakest-topic-slug]
    ```
    Run audit again after gap-fill to confirm count.
  </how-to-verify>
  <resume-signal>Type "approved" once the question set has at least 80 active questions and off-topic questions are archived. Or describe the final count and any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
After the curation checkpoint is approved:
1. `npx tsx src/scripts/audit-collection-readiness.ts --slug washington-dc --prefix wdc` reports 80+ active questions
2. No federal institution or national monument questions remain active
3. No questions call DC a "city" or "state"
4. `washingtonDcConfig` export exists and TypeScript compiles clean
5. Washington, DC entry in `collections.ts` has `isActive: false` (activation happens in Plan 02)
</verification>

<success_criteria>
- Washington, DC locale config exists with accurate district-framing voice guidance
- At least 80 active questions in the database
- Audit confirms expiring ratio (15-30% target, documented if below)
- District framing enforced: no "city" or "state" language, no federal institution questions in active set
- All current-officeholder questions carry correct expiresAt values
</success_criteria>

<output>
After completion, create `.planning/phases/60-washington-dc-collection/60-01-SUMMARY.md` following the summary template.
</output>
