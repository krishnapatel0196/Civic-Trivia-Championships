---
phase: 60-washington-dc-collection
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - frontend/public/images/collections/washington-dc.jpg
  - .planning/COLLECTION-PLAYBOOK.md
autonomous: true

must_haves:
  truths:
    - "Washington, DC collection is active and playable in production"
    - "Collection has at least 80 active questions visible to players"
    - "Banner image (John A. Wilson Building / DC City Hall) is present at the correct path"
    - "Post-activation verify script confirms the collection is live"
    - "Washington, DC retrospective is appended to COLLECTION-PLAYBOOK.md"
  artifacts:
    - path: "frontend/public/images/collections/washington-dc.jpg"
      provides: "John A. Wilson Building (DC City Hall) banner image for collection card"
    - path: ".planning/COLLECTION-PLAYBOOK.md"
      provides: "Retrospective appended for Washington, DC (Phase 60)"
      contains: "Retrospective: Washington, DC"
  key_links:
    - from: "activate-collection.ts"
      to: "collections table isActive flag"
      via: "--slug washington-dc --prefix wdc"
      pattern: "isActive.*true"
    - from: "frontend/public/images/collections/washington-dc.jpg"
      to: "Collection card UI"
      via: "static file serving"
      pattern: "washington-dc.jpg"
---

<objective>
Activate the Washington, DC collection in production, add the John A. Wilson Building banner image, run post-activation verification, and append the phase retrospective to COLLECTION-PLAYBOOK.md.

Purpose: Ships the DC collection to players and closes the phase with documented learnings — including DC-specific voice guidance decisions and the district-framing approach.

Output: Washington, DC live in production with banner image, post-activation verification passing, and COLLECTION-PLAYBOOK.md updated.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-washington-dc-collection/60-RESEARCH.md
@.planning/phases/60-washington-dc-collection/60-01-SUMMARY.md
@.planning/COLLECTION-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add banner image and activate Washington, DC collection</name>
  <files>
    frontend/public/images/collections/washington-dc.jpg
  </files>
  <action>
**Step 1 — Add the John A. Wilson Building banner image.**

The banner image MUST be the John A. Wilson Building (DC City Hall, 1350 Pennsylvania Avenue NW). This is DC's seat of local government where the Mayor and Council work.

The banner MUST NOT be:
- The US Capitol (federal building — appears in the Federal collection)
- The Lincoln Memorial, Washington Monument, or Jefferson Memorial (federal monuments, NPS)
- The White House (federal building)

The John A. Wilson Building is a Beaux-Arts building from 1908 at 1350 Pennsylvania Ave NW. Use WebSearch to find a suitable freely-licensed image. Place it at:
```
frontend/public/images/collections/washington-dc.jpg
```

Target dimensions: match existing collection banner images (check other `.jpg` files in `frontend/public/images/collections/` for reference dimensions). Minimum 800px wide.

**Step 2 — Dry-run activation:**

```bash
cd backend
npx tsx src/scripts/activate-collection.ts --slug washington-dc --prefix wdc --dry-run
```

Review the dry-run output. Confirm:
- Question count meets minimum (80+)
- No unexpected warnings that would block activation
- External IDs all use `wdc` prefix

**Step 3 — Activate:**

```bash
cd backend
npx tsx src/scripts/activate-collection.ts --slug washington-dc --prefix wdc
```

**Step 4 — Run post-activation verification:**

```bash
cd backend
npx tsx src/scripts/verify-post-activation.ts
```

Confirm Washington, DC collection passes all checks.

**Step 5 — Verify the collection appears in production:**

```bash
curl https://civic-trivia-backend.onrender.com/api/collections | jq '.[] | select(.slug == "washington-dc")'
```

Confirm `isActive: true` and question count is correct.
  </action>
  <verify>
    1. `frontend/public/images/collections/washington-dc.jpg` exists
    2. `npx tsx src/scripts/activate-collection.ts --slug washington-dc --prefix wdc --dry-run` passes without blockers
    3. `npx tsx src/scripts/verify-post-activation.ts` passes
    4. `curl https://civic-trivia-backend.onrender.com/api/collections | jq '.[] | select(.slug == "washington-dc") | .isActive'` returns `true`
  </verify>
  <done>Washington, DC collection is active in production. Banner image (John A. Wilson Building) present. Post-activation verify passes. Collection visible in production API response with isActive: true.</done>
</task>

<task type="auto">
  <name>Task 2: Append Washington, DC retrospective to COLLECTION-PLAYBOOK.md</name>
  <files>
    .planning/COLLECTION-PLAYBOOK.md
  </files>
  <action>
Read `.planning/COLLECTION-PLAYBOOK.md` in full. Then append a completed retrospective for Washington, DC using the Retrospective Template from Section 6 of the playbook.

Fill in the retrospective from:
- Generation stats from 60-01-SUMMARY.md (questions generated, after curation, expiring ratio)
- What actually happened during scaffold, locale config authoring, generation, and curation
- How the district-framing voice guidance performed (did the generator respect "the District" framing? Did any questions slip through calling DC a "city"?)
- Whether federal institution questions leaked into the DC collection and had to be curated out
- Any bugs encountered (Scaffold Bug 2, city registration issues, etc.)
- Carry-forward rules derived from this phase

The retrospective must be substantive and honest — not a template with placeholders. Minimum content per section:

**What went well:** At least 2 specific observations about what worked (e.g., district framing in voice guidance, Wikipedia source quality, content density vs. Oregon's saturation issues)

**What broke or was harder than expected:** Honest account of any generation issues, federal/local content bleed, dedup rate, question count shortfalls, curation effort for district framing compliance

**Bugs encountered:** Scaffold Bug 2 status (applied workaround / not triggered / something new). City config registration status. iconIdentifier validity. Any other bugs.

**Carry-forward rules:** New conventions from DC that should inform future collections:
- District framing approach (tier: city + voice guidance) as a model for other non-standard jurisdictions
- Federal/local content separation effectiveness
- Expiring ratio outcome (all 4 major officeholders change in 2026 — did this create a manageable or problematic expiring ratio?)
- Any DC-specific voice guidance decisions that generalize

**Final stats:**
- Questions generated: [from 60-01-SUMMARY]
- Questions after curation: [from audit]
- Expiring question ratio: [from audit]
- Generation cost: [if logged, otherwise "not logged"]
- Time to activate: [approximate]

Append the completed retrospective at the END of COLLECTION-PLAYBOOK.md as a new `## Retrospective: Washington, DC (Phase 60, YYYY-MM-DD)` section (use the actual date).

Do NOT modify any existing content in the file. Only append.
  </action>
  <verify>
    1. `.planning/COLLECTION-PLAYBOOK.md` contains a new `## Retrospective: Washington, DC` section at the end of the file
    2. All 5 retrospective sections are filled in (What went well, What broke, Bugs, Carry-forward rules, Final stats)
    3. Final stats include actual numbers (not placeholders)
    4. No existing playbook content was modified
  </verify>
  <done>Washington, DC retrospective appended to COLLECTION-PLAYBOOK.md with all sections filled in and actual stats from the phase execution.</done>
</task>

</tasks>

<verification>
Phase 60 complete when:
1. `curl https://civic-trivia-backend.onrender.com/api/collections | jq '.[] | select(.slug == "washington-dc")'` returns active collection with correct question count
2. `frontend/public/images/collections/washington-dc.jpg` exists (John A. Wilson Building)
3. `.planning/COLLECTION-PLAYBOOK.md` ends with a completed Washington, DC retrospective
4. `npx tsx src/scripts/audit-collection-readiness.ts --slug washington-dc --prefix wdc` reports 80+ active questions
5. DC's unique civic structure accurately represented: district framing, no federal institution leakage
</verification>

<success_criteria>
- Washington, DC collection is active and playable in production with at least 80 questions
- John A. Wilson Building (DC City Hall) banner image in place at correct path — NOT the US Capitol
- Mixed-durability pattern applied: current-officeholder questions carry expiresAt, durable facts carry null
- Expiring ratio at 15-30% (or documented below with rationale in retrospective)
- Retrospective appended to COLLECTION-PLAYBOOK.md — substantive, with actual stats and carry-forward rules
- REQUIREMENTS CONTENT-03 and PLAYBOOK-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/60-washington-dc-collection/60-02-SUMMARY.md` following the summary template.
</output>
