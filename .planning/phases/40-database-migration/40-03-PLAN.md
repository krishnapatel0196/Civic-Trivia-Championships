---
phase: 40-database-migration
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - backend/src/types/database.types.ts
autonomous: true

must_haves:
  truths:
    - "supabase gen types output includes the trivia schema with all 9 tables"
    - "The generated database.types.ts file is committed to the repo"
    - "TypeScript build passes with the generated types present"
  artifacts:
    - path: "backend/src/types/database.types.ts"
      provides: "Auto-generated Supabase TypeScript types covering all schemas including trivia"
      min_lines: 50
      contains: "trivia"
  key_links:
    - from: "backend/src/types/database.types.ts"
      to: "shared Supabase project schema"
      via: "supabase gen types --linked"
      pattern: "trivia"
---

<objective>
Generate TypeScript types from the shared Supabase project using `supabase gen types`, verify the output includes the `trivia` schema, commit the generated file, and confirm the full TypeScript build passes.

Purpose: Requirement DB-05 requires generated types committed and build passing. This proves the schema on the shared project is correctly structured and the codebase can compile against it.
Output: `backend/src/types/database.types.ts` with trivia schema types; clean TypeScript build.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-database-migration/40-RESEARCH.md
@.planning/phases/40-database-migration/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TypeScript types and verify build</name>
  <files>backend/src/types/database.types.ts</files>
  <action>
    1. Ensure `backend/src/types/` directory exists: `mkdir -p backend/src/types`

    2. Run type generation (the Supabase CLI must already be linked from 40-01):
       ```bash
       npx supabase gen types --linked --lang typescript \
         --schema public,connect,empower,inform,trivia \
         > backend/src/types/database.types.ts
       ```

       The `--schema` flag is comma-separated with NO spaces. The `--linked` flag targets the remote project linked in 40-01.

    3. Verify the generated file contains trivia schema types:
       - Open `backend/src/types/database.types.ts`
       - Confirm it contains a `trivia` key/namespace with table definitions for: collections, collection_questions, collection_topics, election_races, player_prefs, player_stats, question_flags, questions, topics
       - If the trivia section is missing, the schema was not exposed correctly. Check that 40-01 completed successfully and that "trivia" was added to Exposed Schemas in the Supabase Dashboard.

    4. Run the full TypeScript build to verify no compilation errors:
       ```bash
       cd backend && npx tsc --noEmit
       ```

    5. If the build fails due to type conflicts between the generated Supabase types and the Drizzle schema types, these are expected to be independent type systems. The generated types file is for future Supabase client usage (Phases 41+). The Drizzle schema types are for the current ORM queries. Both should coexist without conflict since they are separate exports.

    NOTE on schemas in the gen types command: The schemas `public`, `connect`, `empower`, `inform` are other schemas on the shared Supabase project (per the integration guide). If any of these do not exist yet, the command may warn but should still generate types for `trivia`. If the command fails, try with just `--schema trivia` and add others incrementally.
  </action>
  <verify>
    `grep -c "trivia" backend/src/types/database.types.ts` returns a positive number (trivia schema types present).
    `grep "player_stats" backend/src/types/database.types.ts` returns matches (player_stats table typed).
    `grep "player_prefs" backend/src/types/database.types.ts` returns matches (player_prefs table typed).
    `cd backend && npx tsc --noEmit` passes with zero errors.
  </verify>
  <done>database.types.ts generated with trivia schema types for all 9 tables. TypeScript build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `backend/src/types/database.types.ts` exists and contains trivia schema type definitions
2. The file includes type definitions for all 9 trivia tables (especially player_stats and player_prefs)
3. `cd backend && npx tsc --noEmit` passes with zero errors
4. The generated types file is ready for commit
</verification>

<success_criteria>
- `backend/src/types/database.types.ts` exists with trivia schema types
- All 9 trivia tables are represented in the generated types
- TypeScript build passes with generated types present
- File is committed to the repository
</success_criteria>

<output>
After completion, create `.planning/phases/40-database-migration/40-03-SUMMARY.md`
</output>
