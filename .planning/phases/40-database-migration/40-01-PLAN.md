---
phase: 40-database-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260228000001_create_trivia_schema.sql
  - backend/src/db/schema.ts
  - backend/drizzle.config.ts
  - backend/src/config/database.ts
  - backend/src/routes/admin.ts
  - backend/src/routes/health.ts
  - backend/src/db/seed/seed.ts
autonomous: false
user_setup:
  - service: supabase
    why: "Shared Supabase project for trivia schema"
    env_vars:
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard -> Project Settings -> General -> Reference ID"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens -> Generate New Token"
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon key"
    dashboard_config:
      - task: "Add 'trivia' to Exposed Schemas"
        location: "Supabase Dashboard -> Settings -> API -> Exposed schemas -> add 'trivia'"

must_haves:
  truths:
    - "The `trivia` schema exists on the shared Supabase project with all 9 tables"
    - "RLS is enabled on all 9 trivia tables with correct policies"
    - "PostgREST grants allow anon, authenticated, and service_role access to the trivia schema"
    - "player_stats and player_prefs tables exist with UUID user_id PKs referencing public.users(id)"
    - "question_flags.user_id is UUID (not INTEGER) referencing public.users(id)"
    - "Drizzle schema.ts uses pgSchema('trivia') with playerStats and playerPrefs table definitions"
    - "drizzle.config.ts schemaFilter references 'trivia' not 'civic_trivia'"
  artifacts:
    - path: "supabase/migrations/20260228000001_create_trivia_schema.sql"
      provides: "Complete DDL for trivia schema: 9 tables, indexes, RLS enables, RLS policies, grants"
      min_lines: 100
    - path: "supabase/config.toml"
      provides: "Supabase CLI project config"
      contains: "trivia"
    - path: "backend/src/db/schema.ts"
      provides: "Drizzle ORM schema with trivia namespace, all tables including playerStats and playerPrefs"
      exports: ["triviaSchema", "playerStats", "playerPrefs", "questions", "collections", "topics", "collectionTopics", "collectionQuestions", "questionFlags", "electionRaces"]
    - path: "backend/drizzle.config.ts"
      provides: "Drizzle config pointing to trivia schema"
      contains: "trivia"
  key_links:
    - from: "supabase/migrations/20260228000001_create_trivia_schema.sql"
      to: "shared Supabase project"
      via: "supabase db push"
      pattern: "CREATE SCHEMA.*trivia"
    - from: "backend/src/db/schema.ts"
      to: "trivia schema on shared Supabase"
      via: "pgSchema('trivia')"
      pattern: "pgSchema\\('trivia'\\)"
---

<objective>
Create the `trivia` schema on the shared Supabase project with all 9 tables, RLS policies, and grants. Update the Drizzle ORM schema from `civic_trivia` to `trivia`, add `playerStats` and `playerPrefs` table definitions, and fix `questionFlags.userId` from integer to UUID.

Purpose: This is the foundational DDL step for Phase 40. No data can be migrated and no types can be generated until the schema exists on the target database and the Drizzle ORM matches it.
Output: Working `trivia` schema on the shared Supabase project; updated Drizzle schema.ts and drizzle.config.ts; all production code references updated from `civic_trivia` to `trivia`.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-database-migration/40-RESEARCH.md
@backend/src/db/schema.ts
@backend/drizzle.config.ts
@backend/src/config/database.ts
@empowered-accounts-integration-guide.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm shared Supabase project credentials</name>
  <decision>Before any schema work can proceed, the shared Supabase project must be accessible with valid credentials.</decision>
  <context>Phase 40 requires creating a schema on a shared Supabase project. The Supabase CLI must be initialized, linked, and have push access. The user must confirm credentials are available.</context>
  <options>
    <option id="credentials-ready">
      <name>Credentials are ready</name>
      <pros>Proceed immediately with supabase init, link, and db push</pros>
      <cons>None</cons>
    </option>
    <option id="credentials-not-ready">
      <name>Credentials are NOT ready yet</name>
      <pros>Avoids wasted work</pros>
      <cons>Phase 40 is blocked until credentials are obtained</cons>
    </option>
  </options>
  <resume-signal>Select: credentials-ready (and provide SUPABASE_PROJECT_REF if not already in env), or credentials-not-ready</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create Supabase migration and push trivia schema to shared project</name>
  <files>
    supabase/config.toml
    supabase/migrations/20260228000001_create_trivia_schema.sql
  </files>
  <action>
    1. Run `npx supabase init` at repo root to create `supabase/` directory with `config.toml`.

    2. Run `npx supabase link --project-ref $SUPABASE_PROJECT_REF` to link to the shared project. If prompted for database password, ask the user.

    3. Edit `supabase/config.toml`: In the `[api]` section, ensure `schemas` includes `"trivia"` alongside any existing schemas (e.g., `schemas = ["public", "trivia"]`). Also set `extra_search_path` to include `"trivia"`.

    4. Create the migration file `supabase/migrations/20260228000001_create_trivia_schema.sql` containing the COMPLETE verified SQL from the research file (40-RESEARCH.md "Full SQL Migration File" section). This includes:
       - `CREATE SCHEMA IF NOT EXISTS trivia;`
       - GRANT statements for anon, authenticated, service_role
       - ALTER DEFAULT PRIVILEGES for future tables/sequences
       - All 9 table definitions: topics, collections, election_races, questions, collection_topics, collection_questions, question_flags (UUID user_id), player_stats (UUID PK), player_prefs (UUID PK)
       - All indexes (13 indexes)
       - `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on all 9 tables
       - All RLS policies: public read on 6 content tables, user-scoped CRUD on 3 user tables
       - Use `(SELECT auth.uid())` wrapper (not bare `auth.uid()`) in all RLS policies

    5. Run `npx supabase db push` to apply the migration to the shared project.

    6. Verify by running a quick query: `npx supabase db query "SELECT table_name FROM information_schema.tables WHERE table_schema = 'trivia' ORDER BY table_name;"` -- should return 9 tables.

    IMPORTANT: The user must also manually add "trivia" to Exposed Schemas in the Supabase Dashboard (Settings > API > Exposed schemas). The GRANT SQL alone is not sufficient for PostgREST exposure.
  </action>
  <verify>
    Run `npx supabase db query "SELECT count(*) FROM information_schema.tables WHERE table_schema = 'trivia';"` and confirm it returns 9.
    Run `npx supabase db query "SELECT tablename FROM pg_tables WHERE schemaname = 'trivia' AND rowsecurity = true ORDER BY tablename;"` and confirm all 9 tables have RLS enabled.
  </verify>
  <done>9 tables exist under the `trivia` schema on the shared Supabase project; all have RLS enabled; PostgREST grants are in place.</done>
</task>

<task type="auto">
  <name>Task 3: Update Drizzle schema and all production code from civic_trivia to trivia</name>
  <files>
    backend/src/db/schema.ts
    backend/drizzle.config.ts
    backend/src/config/database.ts
    backend/src/routes/admin.ts
    backend/src/routes/health.ts
    backend/src/db/seed/seed.ts
  </files>
  <action>
    1. Update `backend/src/db/schema.ts`:
       a. Add `uuid` and `real` to the drizzle-orm/pg-core import: `import { pgSchema, serial, text, integer, boolean, timestamp, jsonb, index, primaryKey, unique, uuid, real } from 'drizzle-orm/pg-core';`
       b. Rename `civicTriviaSchema` to `triviaSchema` and change the schema name: `export const triviaSchema = pgSchema('trivia');`
       c. Update ALL table definitions to use `triviaSchema` instead of `civicTriviaSchema` (collections, topics, collectionTopics, electionRaces, questions, collectionQuestions, questionFlags).
       d. Change `questionFlags.userId` from `integer('user_id').notNull()` to `uuid('user_id').notNull()` (UUID type for public.users FK).
       e. Add `playerStats` table definition after questionFlags:
          ```
          export const playerStats = triviaSchema.table('player_stats', {
            userId: uuid('user_id').primaryKey(),
            totalXp: integer('total_xp').notNull().default(0),
            gamesPlayed: integer('games_played').notNull().default(0),
            bestScore: integer('best_score').notNull().default(0),
            totalCorrect: integer('total_correct').notNull().default(0),
            totalQuestions: integer('total_questions').notNull().default(0),
            updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
          });
          ```
       f. Add `playerPrefs` table definition after playerStats:
          ```
          export const playerPrefs = triviaSchema.table('player_prefs', {
            userId: uuid('user_id').primaryKey(),
            timerMultiplier: real('timer_multiplier').notNull().default(1.0),
            updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
          });
          ```
       g. Add type exports: `PlayerStats`, `NewPlayerStats`, `PlayerPrefs`, `NewPlayerPrefs`.

    2. Update `backend/drizzle.config.ts`: Change `schemaFilter: ['civic_trivia']` to `schemaFilter: ['trivia']`.

    3. Search ALL non-script production files for remaining `civic_trivia` string references and update them:
       - `backend/src/config/database.ts` — if it references the schema name string, update to `trivia`
       - `backend/src/routes/admin.ts` — if it references schema name string, update to `trivia`
       - `backend/src/routes/health.ts` — if it references schema name string, update to `trivia`
       - `backend/src/db/seed/seed.ts` — update schema name references to `trivia`

    4. Search for any import of `civicTriviaSchema` across all `.ts` files and update to `triviaSchema`.

    5. Run `cd backend && npx tsc --noEmit` to verify TypeScript compilation passes with the updated schema.

    NOTE: Do NOT update files in `backend/src/scripts/` — these are one-time scripts that reference the OLD database and will not run against the new one. Leave old migration files (`backend/src/db/migrations/`) as-is for historical record.
  </action>
  <verify>
    Run `cd backend && npx tsc --noEmit` — must pass with zero errors.
    Run `grep -r "civic_trivia" backend/src/ --include="*.ts" | grep -v scripts/ | grep -v migrations/` — must return zero results (no production code references old schema name).
    Run `grep -r "civicTriviaSchema" backend/src/ --include="*.ts" | grep -v scripts/ | grep -v migrations/` — must return zero results.
  </verify>
  <done>Drizzle schema uses `triviaSchema = pgSchema('trivia')` with all 9 table definitions including playerStats and playerPrefs. All production code references updated. TypeScript build passes.</done>
</task>

</tasks>

<verification>
1. `npx supabase db query "SELECT table_name FROM information_schema.tables WHERE table_schema = 'trivia' ORDER BY table_name;"` returns: collection_questions, collection_topics, collections, election_races, player_prefs, player_stats, question_flags, questions, topics
2. `npx supabase db query "SELECT tablename FROM pg_tables WHERE schemaname = 'trivia' AND rowsecurity = true ORDER BY tablename;"` returns all 9 tables
3. `cd backend && npx tsc --noEmit` passes with zero errors
4. `grep -r "civic_trivia\|civicTriviaSchema" backend/src/ --include="*.ts" | grep -v scripts/ | grep -v migrations/` returns zero results
5. `backend/src/db/schema.ts` exports `triviaSchema`, `playerStats`, `playerPrefs`
</verification>

<success_criteria>
- 9 tables exist under `trivia` schema on shared Supabase project
- RLS enabled on all 9 tables with correct policies
- PostgREST grants allow API access to trivia schema
- Drizzle schema.ts fully updated (triviaSchema, playerStats, playerPrefs, UUID questionFlags.userId)
- drizzle.config.ts schemaFilter is ['trivia']
- All production code references civic_trivia -> trivia
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/40-database-migration/40-01-SUMMARY.md`
</output>
