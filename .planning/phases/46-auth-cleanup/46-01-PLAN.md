---
phase: 46-auth-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/profile.ts
  - backend/src/routes/admin.ts
  - backend/src/routes/feedback.ts
  - backend/src/middleware/auth.ts
  - backend/src/services/sessionService.ts
autonomous: true

must_haves:
  truths:
    - "No file in the backend imports or references authenticateToken"
    - "requireConnected has no export and no function definition in auth.ts"
    - "sessionService.ts @param userId JSDoc describes UUID string, not number"
    - "TypeScript build passes with zero errors"
  artifacts:
    - path: "backend/src/middleware/auth.ts"
      provides: "Clean auth middleware with no backward-compat alias and no dead exports"
      contains: "export async function requireAuth"
    - path: "backend/src/routes/profile.ts"
      provides: "Profile routes using requireAuth directly"
      contains: "import { requireAuth }"
    - path: "backend/src/routes/admin.ts"
      provides: "Admin routes using requireAuth directly"
      contains: "import { requireAuth, requireAdmin }"
    - path: "backend/src/routes/feedback.ts"
      provides: "Feedback routes using requireAuth directly"
      contains: "import { requireAuth }"
    - path: "backend/src/services/sessionService.ts"
      provides: "Corrected JSDoc for createSession userId param"
      contains: "UUID string for authenticated users"
  key_links:
    - from: "backend/src/routes/profile.ts"
      to: "backend/src/middleware/auth.ts"
      via: "named import requireAuth"
      pattern: "import.*requireAuth.*from.*middleware/auth"
    - from: "backend/src/routes/admin.ts"
      to: "backend/src/middleware/auth.ts"
      via: "named import requireAuth, requireAdmin"
      pattern: "import.*requireAuth.*requireAdmin.*from.*middleware/auth"
    - from: "backend/src/routes/feedback.ts"
      to: "backend/src/middleware/auth.ts"
      via: "named import requireAuth"
      pattern: "import.*requireAuth.*from.*middleware/auth"
---

<objective>
Eliminate three low-severity tech debt items from the v1.8 auth migration: rename the authenticateToken backward-compat alias to requireAuth in all callers, remove the dead requireConnected export, and fix a stale JSDoc comment.

Purpose: Leave the auth middleware layer clean and internally consistent with no dead code or misleading documentation, closing the final v1.8 milestone phase.
Output: Five modified backend files, zero TypeScript errors.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-cleanup/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename authenticateToken to requireAuth in all callers and remove dead code from auth.ts</name>
  <files>
    backend/src/routes/profile.ts
    backend/src/routes/admin.ts
    backend/src/routes/feedback.ts
    backend/src/middleware/auth.ts
    backend/src/services/sessionService.ts
  </files>
  <action>
    Make the following exact edits to five files. Order matters: update callers BEFORE removing the alias from auth.ts.

    **1. backend/src/routes/profile.ts** (2 changes)
    - Line 2: Change `import { authenticateToken } from '../middleware/auth.js';` to `import { requireAuth } from '../middleware/auth.js';`
    - Line 10: Change `router.use(authenticateToken);` to `router.use(requireAuth);`

    **2. backend/src/routes/admin.ts** (2 changes)
    - Line 2: Change `import { authenticateToken, requireAdmin } from '../middleware/auth.js';` to `import { requireAuth, requireAdmin } from '../middleware/auth.js';`
    - Line 18: Change `router.use(authenticateToken, requireAdmin);` to `router.use(requireAuth, requireAdmin);`

    **3. backend/src/routes/feedback.ts** (7 changes)
    - Line 9: Change `import { authenticateToken } from '../middleware/auth.js';` to `import { requireAuth } from '../middleware/auth.js';`
    - Line 21: Change JSDoc `Middleware: authenticateToken, flagRateLimiter` to `Middleware: requireAuth, flagRateLimiter`
    - Line 26: Change `authenticateToken,` to `requireAuth,`
    - Line 72: Change JSDoc `Middleware: authenticateToken (NO rate limiter)` to `Middleware: requireAuth (NO rate limiter)`
    - Line 77: Change `authenticateToken,` to `requireAuth,`
    - Line 120: Change JSDoc `Middleware: authenticateToken (NO rate limiter - infrequent post-game action)` to `Middleware: requireAuth (NO rate limiter - infrequent post-game action)`
    - Line 125: Change `authenticateToken,` to `requireAuth,`

    **4. backend/src/middleware/auth.ts** (2 removals)
    - Remove lines 48-50 (the backward-compat alias block):
      ```
      // Backward-compat alias — existing callers importing authenticateToken continue to work
      // until Plan 02 migrates them to requireAuth
      export { requireAuth as authenticateToken };
      ```
    - Remove lines 85-107 (the entire requireConnected function including its JSDoc):
      ```
      /**
       * Requires the authenticated user to have a verified connected_profiles row
       * in the connect schema. Must be used after requireAuth.
       * Returns 403 if no verified connected profile exists.
       */
      export async function requireConnected(
        req: Request,
        res: Response,
        next: NextFunction
      ): Promise<void> {
        ...
      }
      ```
    - IMPORTANT: Do NOT remove the `supabaseAdmin` import on line 3 — `requireAdmin` still uses it.

    **5. backend/src/services/sessionService.ts** (1 change)
    - Line 126: Change `@param userId - User identifier (number for authenticated, 'anonymous' for unauthenticated)` to `@param userId - UUID string for authenticated users, or 'anonymous' for unauthenticated`
  </action>
  <verify>
    Run three verification checks:
    1. `cd "C:/Project Test/backend" && npx tsc --noEmit` — must exit 0 with no errors
    2. `grep -r "authenticateToken" "C:/Project Test/backend/src/"` — must return zero results
    3. `grep -r "requireConnected" "C:/Project Test/backend/src/"` — must return zero results
  </verify>
  <done>
    - All three callers (profile.ts, admin.ts, feedback.ts) import and use `requireAuth` instead of `authenticateToken`
    - The `authenticateToken` alias export is removed from auth.ts
    - The `requireConnected` function and its JSDoc are removed from auth.ts
    - The `supabaseAdmin` import in auth.ts is retained (requireAdmin uses it)
    - The sessionService.ts JSDoc correctly says "UUID string for authenticated users"
    - TypeScript build passes with zero errors
    - No file in backend/src references `authenticateToken` or `requireConnected`
  </done>
</task>

<task type="auto">
  <name>Task 2: Final sweep and commit verification</name>
  <files>
    (no new files — verification only)
  </files>
  <action>
    Run a comprehensive sweep to confirm no residual tech debt remains:

    1. Grep the entire backend for any stale references: `authenticateToken`, `requireConnected`, `"number for authenticated"`, `Plan 02 migrates`
    2. Verify auth.ts exports exactly: `requireAuth`, `optionalAuth`, `requireAdmin` (three exports, no more)
    3. Confirm the build: `cd "C:/Project Test/backend" && npx tsc --noEmit`

    If any stale references are found, fix them before completing.
  </action>
  <verify>
    All four grep patterns return zero results. TypeScript build exits 0.
  </verify>
  <done>
    - Zero stale references to authenticateToken, requireConnected, "number for authenticated", or "Plan 02 migrates" anywhere in backend/src
    - auth.ts has exactly three exported functions: requireAuth, optionalAuth, requireAdmin
    - TypeScript build passes cleanly
  </done>
</task>

</tasks>

<verification>
Phase-level verification (run after all tasks complete):

1. `cd "C:/Project Test/backend" && npx tsc --noEmit` exits 0
2. `grep -rn "authenticateToken" backend/src/` returns nothing
3. `grep -rn "requireConnected" backend/src/` returns nothing
4. `grep -n "number for authenticated" backend/src/services/sessionService.ts` returns nothing
5. `grep -n "supabaseAdmin" backend/src/middleware/auth.ts` returns a hit (import retained)
6. `grep -c "export.*function\|export {" backend/src/middleware/auth.ts` shows exactly 3 exports (requireAuth, optionalAuth, requireAdmin)
</verification>

<success_criteria>
- No file in backend/src imports or references `authenticateToken`
- No file in backend/src exports or defines `requireConnected`
- sessionService.ts line ~126 JSDoc says "UUID string for authenticated users, or 'anonymous' for unauthenticated"
- `supabaseAdmin` import in auth.ts is intact
- TypeScript build (`npx tsc --noEmit`) passes with zero errors
- auth.ts exports exactly: requireAuth, optionalAuth, requireAdmin
</success_criteria>

<output>
After completion, create `.planning/phases/46-auth-cleanup/46-01-SUMMARY.md`
</output>
