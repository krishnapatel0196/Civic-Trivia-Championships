---
phase: 61-biloxi-ms-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/seed/collections.ts
  - backend/src/scripts/content-generation/locale-configs/biloxi-ms.ts
  - backend/src/scripts/content-generation/generate-locale-questions.ts
autonomous: false

must_haves:
  truths:
    - "Biloxi, MS collection exists in the database with slug 'biloxi-ms' and prefix 'bxl'"
    - "Locale config has 7 topic categories matching research distribution (city-government, founding-history, seafood-heritage, landmarks-culture, civil-rights, military-geography, casino-resilience)"
    - "At least 80 draft questions exist after generation and semantic dedup"
    - "Voice guidance warns against calling Biloxi the county seat (Gulfport is), caps casino questions at 9, and requires expiresAt on all officeholder questions"
    - "Human curator has reviewed and approved the draft question set"
  artifacts:
    - path: "backend/src/scripts/content-generation/locale-configs/biloxi-ms.ts"
      provides: "Biloxi, MS locale config with 7 topics, source URLs, voice guidance"
      exports: ["biloxiMsConfig"]
    - path: "backend/src/db/seed/collections.ts"
      provides: "Biloxi, MS seed entry (sortOrder: 15, isActive: false, themeColor: #0077A8)"
      contains: "biloxi-ms"
  key_links:
    - from: "biloxi-ms.ts"
      to: "generate-locale-questions.ts city config registration"
      via: "scaffold registers city configs in generate-locale-questions.ts"
      pattern: "biloxiMsConfig"
    - from: "generate-locale-questions.ts"
      to: "Biloxi questions in DB"
      via: "--locale biloxi-ms --fetch-sources"
      pattern: "bxl-[0-9]+"
---

<objective>
Scaffold the Biloxi, MS collection, author a complete locale config with all 7 topic categories and voice guidance, run content generation with automatic semantic dedup, and curate the draft question set.

Purpose: Establishes Biloxi as a content-rich city collection emphasizing its unique civic identity -- seafood heritage, civil rights history (wade-ins), Keesler AFB, and Gulf Coast resilience. Target 120-130 generated questions (1.4x buffer) to land at 70+ after dedup and curation.

Output: Biloxi, MS locale config file, seed entry in collections.ts, and a curated set of draft questions ready for activation.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-biloxi-ms-collection/61-RESEARCH.md
@.planning/COLLECTION-PLAYBOOK.md
@backend/src/scripts/content-generation/locale-configs/portland-or.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Biloxi, MS and author locale config</name>
  <files>
    backend/src/db/seed/collections.ts
    backend/src/scripts/content-generation/locale-configs/biloxi-ms.ts
    backend/src/scripts/content-generation/generate-locale-questions.ts
  </files>
  <action>
**Step 1 -- Run scaffold command:**

```bash
cd backend
npx tsx src/scripts/scaffold-collection.ts \
  --name "Biloxi, MS" \
  --slug biloxi-ms \
  --prefix bxl \
  --theme "#0077A8" \
  --tier city \
  --description "The Seafood Capital of the World is betting you don't know it." \
  --sort-order 15
```

**Step 2 -- Apply Scaffold Bug 2 workaround immediately:**

```bash
git diff backend/src/scripts/content-generation/generate-locale-questions.ts
```

If the diff shows a corrupted type annotation line (string injected into a TypeScript type), revert:

```bash
git checkout backend/src/scripts/content-generation/generate-locale-questions.ts
```

IMPORTANT: City configs DO need registration in generate-locale-questions.ts (unlike state configs which are auto-discovered). Only revert if the file is corrupted (extra string in a type annotation), NOT because it was modified. Check the diff carefully -- a clean import + registration is correct; a mangled type line is the bug.

**Step 3 -- Run seed:**

```bash
npx tsx src/db/seed/seed.ts
```

**Step 4 -- Verify seed entry in collections.ts.** Check that:
- `sortOrder: 15`
- `themeColor: '#0077A8'`
- `tier: 'city'`
- `isActive: false`
- `localeName` is `'Biloxi, MS'` (not expanded incorrectly)
- `iconIdentifier` is a valid frontend identifier (likely `flag-ms` -- confirm it exists; if not, use `flag-us`)

Manually correct any scaffold errors in collections.ts.

**Step 5 -- Author the locale config at `backend/src/scripts/content-generation/locale-configs/biloxi-ms.ts`.**

Model on `portland-or.ts` (city config shape -- single named export `biloxiMsConfig`). The config must include:

`biloxiMsConfig` with:
- `locale: 'biloxi-ms'`
- `name: 'Biloxi, MS'`
- `externalIdPrefix: 'bxl'`
- `collectionSlug: 'biloxi-ms'`
- `targetQuestions: 130` (1.4x buffer: 90 target x 1.4 = 126, round up to 130)
- `batchSize: 25`
- `overshootFactor: 1.4`

**`topicCategories` (7 topics):**

1. `city-government` -- "Strong Mayor-Council government adopted 1978 (effective 1981). Mayor: Andrew 'FoFo' Gilich, Jr. -- 3rd term, re-elected June 3, 2025, term expires 2029-06-01. 7-member City Council (one per ward), all elected June 2025, all expire 2029-06-01: Ward 1 Wayne Gray, Ward 2 Anthony Marshall, Ward 3 Mike Nail, Ward 4 Jamie Creel (VERIFY: one source referenced 'Fuller' -- check biloxi.ms.us), Ward 5 Paul Tisdale, Ward 6 Kenny Glavan, Ward 7 David Shoemaker. ALL officeholder questions MUST have expiresAt: 2029-06-01T00:00:00Z. Mix of structure questions (durable) and current-officeholder questions (expiring, target 8-10 expiring from this topic)."

2. `founding-history` -- "Founded 1699 by Pierre Le Moyne d'Iberville as one of the oldest European settlements in the Gulf South. Capital of French Louisiana 1720-1723 (before New Orleans). Name origin: Biloxi people (indigenous Siouan-speaking nation, 'first people'). Explored and claimed for France. Harrison County established 1841. Biloxi incorporated as a city in 1838. Key dates and founding figures."

3. `seafood-heritage` -- "Known as 'Seafood Capital of the World.' Historic seafood canneries (shrimp, oysters, crab) -- industry peak in early-to-mid 20th century. Maritime & Seafood Industry Museum (opened 1986, destroyed by Katrina 2005, rebuilt 2014 at new location on Point Cadet). Biloxi Schooner (replica oyster schooner). Vietnamese fishing community (arrived late 1970s-1980s, significant contribution to Gulf Coast fishing industry). Annual Blessing of the Fleet (since 1929, one of the oldest in the US)."

4. `landmarks-culture` -- "Biloxi Lighthouse (cast-iron tower, built 1848, survives in US-90 highway median -- one of the few lighthouses in the middle of a highway). Beauvoir (last home of Jefferson Davis, now a museum -- questions about Beauvoir as a historic site, NOT Jefferson Davis biography). George Ohr ('Mad Potter of Biloxi,' pottery pioneer, Ohr-O'Keefe Museum designed by Frank Gehry). Mardi Gras in Biloxi (predates New Orleans Mardi Gras, documented celebrations since 1699). Gulf Coast beach (26-mile man-made beach -- Harrison County Shore Protection Project; say 'Gulf Coast beach' or 'Biloxi area beach,' NOT 'within Biloxi city limits')."

5. `civil-rights` -- "Biloxi Wade-Ins (1959-1963): Dr. Gilbert R. Mason Sr. led beach desegregation protests, Bloody Sunday (April 24, 1960 -- white mob attacked Black beachgoers), resulted in beaches being closed rather than integrated. Federal court ruling (1968) ordered beach integration. Dr. Mason was the first Black physician to practice in Biloxi. Civil rights history is central to Biloxi's identity -- do not minimize or sensationalize."

6. `military-geography` -- "Keesler Air Force Base (established 1941, named for 2nd Lt. Samuel R. Keesler Jr., 81st Training Wing -- premier electronics and computer training installation for the Air Force). Ship Island (part of Gulf Islands National Seashore, accessible by ferry from Biloxi, Fort Massachusetts on West Ship Island -- Civil War era). Harrison County geography: Biloxi is on a peninsula between Biloxi Bay and the Mississippi Sound. Biloxi is NOT the county seat of Harrison County -- that is Gulfport."

7. `casino-resilience` -- "Mississippi Gaming Control Act of 1990 legalized dockside gaming along the Gulf Coast. First casinos opened 1992. Major casinos: Beau Rivage (MGM Resorts), Hard Rock, IP Casino Resort Spa, Golden Nugget. Hurricane Katrina (August 29, 2005) devastated Biloxi -- storm surge reached 28 feet, destroyed or severely damaged all 11 casinos, 90% of structures within half a mile of coast destroyed. Post-Katrina rebuild allowed casinos to build on land (previously required to be on water). Biloxi's recovery is a resilience story, not a gambling story. CAP: Maximum 9 questions on this topic -- trim during curation if overflow."

**`topicDistribution`** (must sum to 100):
- `city-government`: 20
- `founding-history`: 13
- `seafood-heritage`: 15
- `landmarks-culture`: 18
- `civil-rights`: 11
- `military-geography`: 13
- `casino-resilience`: 10

**`sourceUrls`** (Wikipedia-first, per carry-forward rule):
```
'https://en.wikipedia.org/wiki/Biloxi,_Mississippi',
'https://en.wikipedia.org/wiki/Biloxi_Lighthouse',
'https://en.wikipedia.org/wiki/Beauvoir_(Biloxi,_Mississippi)',
'https://en.wikipedia.org/wiki/Biloxi_wade-ins',
'https://en.wikipedia.org/wiki/Keesler_Air_Force_Base',
'https://en.wikipedia.org/wiki/Maritime_%26_Seafood_Industry_Museum',
'https://en.wikipedia.org/wiki/George_Ohr',
'https://en.wikipedia.org/wiki/Gulf_Islands_National_Seashore',
'https://en.wikipedia.org/wiki/Gilbert_R._Mason_Sr.',
'https://en.wikipedia.org/wiki/Biloxi_people',
'https://en.wikipedia.org/wiki/Hurricane_Katrina',
'https://en.wikipedia.org/wiki/Beau_Rivage_(Biloxi)',
```

Do NOT use biloxi.ms.us as a primary source -- government portal pages may return navigation/sitemap content (carry-forward rule from Portland Phase 58). The two biloxi.ms.us URLs from research are supplementary only -- omit from sourceUrls array.

**Voice guidance block** (add as a comment block at the top of the file, same pattern as portland-or.ts):

```
CRITICAL ACCURACY NOTES:
- Biloxi is in Harrison County; the county seat is Gulfport -- do NOT attribute county government to Biloxi
- The man-made beach (26 miles) is the Harrison County Shore Protection Project beach -- say 'Gulf Coast beach' or 'Biloxi area beach,' not 'within Biloxi city limits'
- ALL officeholder questions must have expiresAt: 2029-06-01T00:00:00Z -- verify during curation
- Casino questions cap at 9 -- trim during curation if casino content overflows
- Do NOT call Biloxi 'the gambling capital' -- primary frame is seafood heritage + civil rights history
- Beauvoir questions should be about the historic site, NOT general Jefferson Davis biography
- No addresses or phone numbers in answer options (quality rule)
- Ward 4 council member: verify 'Jamie Creel' vs. 'Fuller' on biloxi.ms.us before generation
```
  </action>
  <verify>
    1. `backend/src/scripts/content-generation/locale-configs/biloxi-ms.ts` exists and exports `biloxiMsConfig`
    2. Config has `locale: 'biloxi-ms'`, `targetQuestions: 130`, 7 topic categories, topic distribution summing to 100
    3. `generate-locale-questions.ts` has clean registration for biloxi-ms (no corrupted type annotations)
    4. Biloxi, MS seed entry in `backend/src/db/seed/collections.ts` with `slug: 'biloxi-ms'`, `themeColor: '#0077A8'`, `tier: 'city'`, `isActive: false`, `sortOrder: 15`
    5. TypeScript compiles: `cd backend && npx tsc --noEmit`
  </verify>
  <done>Biloxi, MS collection seeded and locale config complete with 7 topic categories, Wikipedia-first source URLs, and voice guidance. TypeScript compiles without errors. Scaffold Bug 2 workaround applied if triggered.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Biloxi, MS questions</name>
  <files>
    backend/src/scripts/content-generation/generate-locale-questions.ts
  </files>
  <action>
**Step 1 -- Pre-generation source spot-check:**

All primary sources are Wikipedia article URLs handled by the Phase 58-03 w/api.php fix. No biloxi.ms.us URLs in the primary list. Sources should be safe, but spot-check 2-3 by running:

```bash
cd backend
npx tsx -e "
const urls = [
  'https://en.wikipedia.org/wiki/Biloxi,_Mississippi',
  'https://en.wikipedia.org/wiki/Biloxi_wade-ins',
  'https://en.wikipedia.org/wiki/Keesler_Air_Force_Base'
];
for (const url of urls) {
  try {
    const apiUrl = url.replace('https://en.wikipedia.org/wiki/', 'https://en.wikipedia.org/w/api.php?action=query&titles=') + '&prop=extracts&explaintext=true&format=json';
    const resp = await fetch(apiUrl);
    const json = await resp.json();
    const pages = Object.values(json.query.pages);
    const extract = pages[0].extract || '';
    console.log(url.split('/').pop() + ': ' + extract.length + ' chars');
  } catch(e) { console.log(url + ': FAILED - ' + e.message); }
}
"
```

If any return 0 chars, note for post-generation assessment but proceed -- the main Biloxi article covers most topics.

**Step 2 -- Run generation:**

```bash
cd backend
npx tsx src/scripts/content-generation/generate-locale-questions.ts \
  --locale biloxi-ms \
  --fetch-sources
```

Generation takes ~30 minutes. Semantic dedup runs automatically after all batches complete. Watch for:
- Total generated count (expect ~120-130 questions before dedup -- Biloxi has rich content across 7 topics)
- Dedup archive count (expect 25-40% reduction based on prior city collections)
- Final draft question count (target: 80+ after dedup; 70+ is the phase success floor)

**Step 3 -- Log generation results:**

```bash
cd backend
npx tsx src/scripts/audit-collection-readiness.ts --slug biloxi-ms --prefix bxl
```

Note the output: total active count, expiring question ratio, any warnings. The expiring ratio will likely be 0% at this point (generator does not reliably set expiresAt) -- this is expected and will be addressed in Plan 02 with a targeted officeholder pass.
  </action>
  <verify>
    Run `npx tsx src/scripts/audit-collection-readiness.ts --slug biloxi-ms --prefix bxl` and confirm:
    - Draft/active question count is reported (target 80+, minimum 70+)
    - No unrecoverable errors (TypeScript errors, DB connection failures)
    - Semantic dedup ran automatically during generation (check generation output for dedup log lines)
  </verify>
  <done>Generation complete. Audit report printed. Question count and expiring ratio logged. Semantic dedup ran automatically during generation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Biloxi, MS question set -- generated and semantically deduped. Collection exists in the database with draft questions ready for admin panel review.

    Current status from audit: [Claude will print the active count and expiring ratio here before pausing]

    NOTE: Expiring ratio will likely be 0% at this stage. The generator does not reliably apply expiresAt. A targeted officeholder pass is planned in 61-02 to address this -- do not reject the set for low expiring ratio now.
  </what-built>
  <how-to-verify>
    1. Open the admin panel: https://civic-trivia-backend.onrender.com/admin (or local: http://localhost:3001/admin)
    2. Find the Biloxi, MS collection
    3. Review ALL draft questions. Archive any that:
       - Call Biloxi the county seat of Harrison County (Gulfport is the county seat)
       - Claim the man-made beach is within Biloxi city limits (it is the Harrison County Shore Protection Project)
       - Include addresses or phone numbers in answer options
       - Are primarily about Jefferson Davis biography rather than Beauvoir as a historic site
       - Frame Biloxi as "the gambling capital" rather than seafood heritage / civil rights / resilience
       - Reference website navigation structure ("Which section of biloxi.ms.us...")
       - Are factually wrong or misleading
       - Have weak answer distractors
    4. Count casino-resilience topic questions -- if more than 9, archive the weakest ones
    5. Verify Ward 4 council member name: is it Jamie Creel or Fuller? If any questions reference the wrong name, correct or archive them.
    6. Confirm the draft count AFTER curation:
       - Target: at least 70 draft questions remaining
       - If count is below 60 after curation: run a gap-fill generation batch before typing "approved":
         ```bash
         cd backend
         npx tsx src/scripts/content-generation/generate-locale-questions.ts \
           --locale biloxi-ms \
           --fetch-sources
         ```
         Then re-curate and re-audit.

    **Do NOT worry about expiresAt at this stage.** Officeholder pass is in Plan 02.
  </how-to-verify>
  <resume-signal>Type "approved" once draft questions are curated and at least 70 remain. Or describe the final count and any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
After the curation checkpoint is approved:
1. `npx tsx src/scripts/audit-collection-readiness.ts --slug biloxi-ms --prefix bxl` reports 70+ draft questions
2. No questions claim Biloxi is the county seat of Harrison County
3. Casino-resilience questions capped at 9
4. `biloxiMsConfig` export exists and TypeScript compiles clean
5. Biloxi, MS entry in `collections.ts` has `isActive: false` (activation happens in Plan 02)
</verification>

<success_criteria>
- Biloxi, MS locale config exists with 7 topic categories, Wikipedia-first sources, and voice guidance
- At least 70 curated draft questions in the database
- Semantic dedup ran automatically during generation
- Casino-resilience questions capped at 9
- Scaffold Bug 2 workaround applied if triggered
- Collection is NOT yet active (isActive: false)
</success_criteria>

<output>
After completion, create `.planning/phases/61-biloxi-ms-collection/61-01-SUMMARY.md` following the summary template.
</output>
