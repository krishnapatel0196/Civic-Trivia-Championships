---
phase: 59-oregon-state-collection
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - frontend/public/images/collections/oregon-state.jpg
  - .planning/COLLECTION-PLAYBOOK.md
autonomous: true

must_haves:
  truths:
    - "Oregon State collection is active and playable in production"
    - "Collection has at least 80 active questions visible to players"
    - "Banner image (Oregon State Capitol) is present at the correct path"
    - "Post-activation verify script confirms the collection is live"
    - "Oregon State retrospective is appended to COLLECTION-PLAYBOOK.md"
  artifacts:
    - path: "frontend/public/images/collections/oregon-state.jpg"
      provides: "Oregon State Capitol banner image for collection card"
    - path: ".planning/COLLECTION-PLAYBOOK.md"
      provides: "Retrospective appended for Oregon State (Phase 59)"
      contains: "Retrospective: Oregon"
  key_links:
    - from: "activate-collection.ts"
      to: "collections table isActive flag"
      via: "--slug oregon-state --prefix ore"
      pattern: "isActive.*true"
    - from: "frontend/public/images/collections/oregon-state.jpg"
      to: "Collection card UI"
      via: "static file serving"
      pattern: "oregon-state.jpg"
---

<objective>
Activate the Oregon State collection in production, add the state capitol banner image, run post-activation verification, and append the phase retrospective to COLLECTION-PLAYBOOK.md.

Purpose: Ships the Oregon State collection to players and closes the phase with documented learnings for future collections.

Output: Oregon State live in production with banner image, post-activation verification passing, and COLLECTION-PLAYBOOK.md updated.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-oregon-state-collection/59-CONTEXT.md
@.planning/phases/59-oregon-state-collection/59-01-SUMMARY.md
@.planning/COLLECTION-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add banner image and activate Oregon State collection</name>
  <files>
    frontend/public/images/collections/oregon-state.jpg
  </files>
  <action>
**Step 1 — Add the Oregon State Capitol banner image.**

The banner image must be the Oregon State Capitol building (hard rule for state collections — no exceptions). The Oregon State Capitol is visually distinctive: Art Deco/Moderne design, white Vermont marble exterior, topped by the gilded bronze "Pioneer" statue.

Source a high-quality JPEG of the Oregon State Capitol and place it at:
```
frontend/public/images/collections/oregon-state.jpg
```

Use WebSearch to find a suitable freely-licensed image if one is not already available locally. The image should show the full capitol building facade, ideally with the Pioneer statue visible.

Target dimensions: match existing collection banner images (check other `.jpg` files in `frontend/public/images/collections/` for reference dimensions). Minimum 800px wide.

**Step 2 — Dry-run activation:**

```bash
cd backend
npx tsx src/scripts/activate-collection.ts --slug oregon-state --prefix ore --dry-run
```

Review the dry-run output. Confirm:
- Question count meets minimum (80+)
- No unexpected warnings that would block activation
- External IDs all use `ore` prefix

**Step 3 — Activate:**

```bash
cd backend
npx tsx src/scripts/activate-collection.ts --slug oregon-state --prefix ore
```

**Step 4 — Run post-activation verification:**

```bash
cd backend
npx tsx src/scripts/verify-post-activation.ts
```

Confirm Oregon State collection passes all checks.

**Step 5 — Verify the collection appears in production:**

```bash
curl https://civic-trivia-backend.onrender.com/api/collections | jq '.[] | select(.slug == "oregon-state")'
```

Confirm `isActive: true` and question count is correct.
  </action>
  <verify>
    1. `frontend/public/images/collections/oregon-state.jpg` exists
    2. `npx tsx src/scripts/activate-collection.ts --slug oregon-state --prefix ore --dry-run` passes without blockers
    3. `npx tsx src/scripts/verify-post-activation.ts` passes
    4. `curl https://civic-trivia-backend.onrender.com/api/collections | jq '.[] | select(.slug == "oregon-state") | .isActive'` returns `true`
  </verify>
  <done>Oregon State collection is active in production. Banner image present. Post-activation verify passes. Collection visible in production API response with isActive: true.</done>
</task>

<task type="auto">
  <name>Task 2: Append Oregon State retrospective to COLLECTION-PLAYBOOK.md</name>
  <files>
    .planning/COLLECTION-PLAYBOOK.md
  </files>
  <action>
Read `.planning/COLLECTION-PLAYBOOK.md` in full. Then append a completed retrospective for Oregon State using the Retrospective Template from Section 6 of the playbook.

Fill in the retrospective from:
- Generation stats from the 59-01-SUMMARY.md (questions generated, after curation, expiring ratio, generation cost if logged)
- What actually happened during scaffold, locale config authoring, generation, and curation
- Any bugs encountered (Scaffold Bug 2 — expected; any new issues)
- Carry-forward rules derived from this phase's experience

The retrospective must be substantive and honest — not a template with placeholders. Minimum content per section:

**What went well:** At least 2 specific observations about what worked (e.g., Wikipedia URLs returning clean content, voice guidance preventing no-LtGov confusion, dedup behavior)

**What broke or was harder than expected:** Honest account of any generation issues, dedup rate surprises, question count shortfalls, curation effort

**Bugs encountered:** Scaffold Bug 2 status (applied workaround / not triggered / something new). Any other bugs.

**Carry-forward rules:** New conventions specific to Oregon State that should inform future state collections (e.g., civic-innovation clustering behavior, expiring ratio reality for states with fewer elected executives than Texas, voice guidance lessons)

**Final stats:**
- Questions generated: [from 59-01-SUMMARY]
- Questions after curation: [from audit]
- Expiring question ratio: [from audit]
- Generation cost: [if logged, otherwise "not logged"]
- Time to activate: [approximate]

Append the completed retrospective at the END of COLLECTION-PLAYBOOK.md, after the Portland OR retrospective, as a new `## Retrospective: Oregon (Phase 59, 2026-03-09)` section.

Do NOT modify any existing content in the file. Only append.
  </action>
  <verify>
    1. `.planning/COLLECTION-PLAYBOOK.md` contains a new `## Retrospective: Oregon` section at the end of the file
    2. All 5 retrospective sections are filled in (What went well, What broke, Bugs, Carry-forward rules, Final stats)
    3. Final stats include actual numbers (not placeholders like "[from audit]")
    4. No existing playbook content was modified
  </verify>
  <done>Oregon State retrospective appended to COLLECTION-PLAYBOOK.md with all sections filled in and actual stats from the phase execution.</done>
</task>

</tasks>

<verification>
Phase 59 complete when:
1. `curl https://civic-trivia-backend.onrender.com/api/collections | jq '.[] | select(.slug == "oregon-state")'` returns active collection with correct question count
2. `frontend/public/images/collections/oregon-state.jpg` exists (Oregon State Capitol)
3. `.planning/COLLECTION-PLAYBOOK.md` ends with a completed Oregon State retrospective
4. `npx tsx src/scripts/audit-collection-readiness.ts --slug oregon-state --prefix ore` reports 80+ active questions
</verification>

<success_criteria>
- Oregon State collection is active and playable in production with at least 80 questions
- State capitol banner image in place at correct path
- Mixed-durability pattern applied: current-officeholder questions carry expiresAt, durable facts carry null
- Expiring ratio at 15–30% (or documented below with rationale in retrospective if unavoidable)
- Retrospective appended to COLLECTION-PLAYBOOK.md — substantive, with actual stats and carry-forward rules
- REQUIREMENTS CONTENT-02 and PLAYBOOK-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/59-oregon-state-collection/59-02-SUMMARY.md` following the summary template.
</output>
