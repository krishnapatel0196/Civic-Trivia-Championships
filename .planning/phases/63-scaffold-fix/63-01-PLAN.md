---
phase: 63-scaffold-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/scripts/scaffold-collection.ts
autonomous: true

must_haves:
  truths:
    - "Running scaffold-collection.ts leaves generate-locale-questions.ts with only the expected new locale entry added — no corruption of existing lines"
    - "The brace-depth scanner starts from the object literal assignment (= {), not from the type annotation"
    - "A post-write sanity check confirms the new slug appears in both supportedLocales and configKeys"
  artifacts:
    - path: "backend/src/scripts/scaffold-collection.ts"
      provides: "Fixed step3RegisterLocale function"
      contains: "content.indexOf(' = {', supportedLocalesStart)"
  key_links:
    - from: "backend/src/scripts/scaffold-collection.ts"
      to: "backend/src/scripts/content-generation/generate-locale-questions.ts"
      via: "readFileSync + string manipulation + writeFileSync"
      pattern: "indexOf.*= \\{"
---

<objective>
Fix Scaffold Bug 2: the brace-depth scanner in `step3RegisterLocale` starts scanning from `const supportedLocales:` which includes the TypeScript type annotation braces `{ default?: LocaleConfig; [key: string]: unknown }` before reaching the actual object literal. This causes the insertion to land inside the type annotation, corrupting `generate-locale-questions.ts`.

Purpose: Eliminate the post-scaffold `git checkout` workaround so the scaffold tool is safe to run without manual intervention — prerequisite for all subsequent v2.2 collection work.
Output: A fixed `scaffold-collection.ts` that inserts locale entries at the correct position.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-scaffold-fix/63-RESEARCH.md
@backend/src/scripts/scaffold-collection.ts
@backend/src/scripts/content-generation/generate-locale-questions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix brace-depth scanner in step3RegisterLocale</name>
  <files>backend/src/scripts/scaffold-collection.ts</files>
  <action>
In `step3RegisterLocale` (starts at ~line 354), replace the supportedLocales brace-depth scanner with a version that starts from the object literal assignment, not the type annotation.

**What to change:**

1. Remove the dead-code block at lines 366-375 (the `closingBraceForLocales` / `altClose` check that finds a position but never uses it — it was an earlier attempt that got overridden by the brace scanner below it).

2. Replace the brace scanner (lines 377-394) with:
   - After finding `supportedLocalesStart`, find `' = {'` from that position: `content.indexOf(' = {', supportedLocalesStart)`
   - Error-exit if not found (matching existing error pattern)
   - Set `objectOpenPos = assignmentPos + 3` (the position of the `{`)
   - Scan brace depth starting from `objectOpenPos` — depth starts at 0, increment on `{`, decrement on `}`, break when depth returns to 0

3. The `configKeys` array scanner (lines 413-429) uses `[` and `]` counting from `const configKeys = [`. This works correctly because there is no type annotation with brackets. Leave it unchanged.

4. Add a post-write sanity check after the `writeFileSync` call at line 439:
   - Read the file back with `readFileSync`
   - Verify `args.slug` appears in the content (confirms supportedLocales entry)
   - Verify `configVarName + 'Config'` appears in the content (confirms configKeys entry)
   - If either check fails, `console.error` a clear message and `process.exit(1)`

**What NOT to change:**
- Do NOT modify `generate-locale-questions.ts` in any way — the fix is entirely in `scaffold-collection.ts`
- Do NOT change the `configKeys` insertion logic — it works correctly
- Do NOT change the locale entry format string (line 401) — trailing comma style is correct
  </action>
  <verify>Run `npx tsc --noEmit` from backend/ to confirm no type errors in the modified file.</verify>
  <done>The brace scanner starts from ` = {` (not from `const supportedLocales:`), dead code is removed, post-write sanity check is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Verify fix with test scaffold run</name>
  <files>backend/src/scripts/scaffold-collection.ts</files>
  <action>
Run a full end-to-end test of the fix:

1. Record the SHA256 hash of `generate-locale-questions.ts` before scaffolding
2. Run scaffold: `npx tsx src/scripts/scaffold-collection.ts --name "Test City, TX" --slug test-city-tx --prefix tct --theme "#123456" --description "A test collection for verification"`
3. Run `git diff src/scripts/content-generation/generate-locale-questions.ts` and verify:
   - The diff shows ONLY two additions: one new entry in `supportedLocales` and one new key in `configKeys`
   - No lines are corrupted, deleted, or modified — only pure additions
   - The type annotation line (`Record<string, () => Promise<{ default?:...`) is UNCHANGED
4. Verify: `grep "test-city-tx" src/scripts/content-generation/generate-locale-questions.ts` returns the locale entry
5. Verify: `grep "testCityTxConfig" src/scripts/content-generation/generate-locale-questions.ts` returns the config key entry
6. Clean up ALL test artifacts (do NOT leave them in the working tree):
   - `git checkout -- src/scripts/content-generation/generate-locale-questions.ts`
   - `git checkout -- src/db/seed/collections.ts` (if scaffold modified it)
   - `rm -f src/scripts/content-generation/locale-configs/test-city-tx.ts`
   - Check for any other files scaffold created and remove them

If any verification step fails, diagnose and fix before proceeding. The fix is not complete until a clean scaffold run produces only the expected diff.
  </action>
  <verify>After cleanup, `git status` shows only `scaffold-collection.ts` as modified (the fix itself). No other files changed.</verify>
  <done>Test scaffold run produces a clean, correct diff with no corruption. All test artifacts cleaned up. The `git checkout` workaround is no longer needed.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in backend/ (no type errors introduced)
2. Test scaffold run adds entries to both `supportedLocales` and `configKeys` without corrupting any existing lines
3. The type annotation line in `generate-locale-questions.ts` is byte-for-byte unchanged after scaffold
4. Post-write sanity check catches intentional corruption (if you temporarily break the insertion, the script should error-exit)
</verification>

<success_criteria>
- scaffold-collection.ts brace scanner starts from ` = {` assignment, not from type annotation
- Post-write sanity check validates both insertion sites
- A test scaffold run produces only the two expected additions in generate-locale-questions.ts
- No manual `git checkout` workaround required after scaffolding
</success_criteria>

<output>
After completion, create `.planning/phases/63-scaffold-fix/63-01-SUMMARY.md`
</output>
