---
phase: 69-game-flow-buttons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/game/components/NextStepButton.tsx
  - frontend/src/features/game/components/GameScreen.tsx
autonomous: false

must_haves:
  truths:
    - "After answering questions 1-6, a 'Next Question' button is visible and advances the game"
    - "After answering question 7, a 'Last Question' button appears instead"
    - "After answering the final question (8 / wager), a 'Game Recap' button appears and navigates to results"
    - "No tap-anywhere mechanic or tap icon remains on the game screen"
    - "Full game screen fits without scrolling on mobile (375x667) and desktop"
    - "Button text is legible and matches the game show aesthetic"
  artifacts:
    - path: "frontend/src/features/game/components/NextStepButton.tsx"
      provides: "Navigation button with conditional label"
      exports: ["NextStepButton"]
    - path: "frontend/src/features/game/components/GameScreen.tsx"
      provides: "Game screen with button replacing tap-anywhere"
      contains: "NextStepButton"
  key_links:
    - from: "frontend/src/features/game/components/NextStepButton.tsx"
      to: "GameScreen nextQuestion prop"
      via: "onAdvance callback"
      pattern: "onAdvance"
    - from: "frontend/src/features/game/components/GameScreen.tsx"
      to: "NextStepButton"
      via: "import and render during revealing phase"
      pattern: "NextStepButton"
---

<objective>
Replace the tap-anywhere mechanic in the game screen with explicitly labeled navigation buttons that change based on question number: "Next Question" (Q1-Q6), "Last Question" (Q7), and "Game Recap" (Q8/wager).

Purpose: Eliminate tap-anywhere ambiguity so players always know how to advance. The button labels also build narrative momentum (the "Last Question" label creates anticipation for the final wager).

Output: A new NextStepButton component and a modified GameScreen with tap-anywhere removed, button added, and timer shrunk during reveal to prevent mobile scroll.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/features/game/components/GameScreen.tsx
@frontend/src/features/game/components/AnswerGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NextStepButton and integrate into GameScreen</name>
  <files>
    frontend/src/features/game/components/NextStepButton.tsx
    frontend/src/features/game/components/GameScreen.tsx
  </files>
  <action>
**Create `NextStepButton.tsx`** — a new component that renders a styled navigation button during the revealing phase.

Props interface:
```typescript
interface NextStepButtonProps {
  questionIndex: number;      // 0-based current question index
  totalQuestions: number;      // total questions in game (8)
  isFinalQuestion: boolean;   // true for Q8 (wager question)
  onAdvance: () => void;       // calls nextQuestion()
  disabled: boolean;           // true when LearnMore modal is open
}
```

Label logic (derive from props, not hardcoded question numbers):
- If `isFinalQuestion` is true: `"GAME RECAP"`
- Else if `questionIndex === totalQuestions - 2` (Q7, index 6): `"LAST QUESTION"`
- Else: `"NEXT QUESTION"`

Styling — match the LOCK IN button in AnswerGrid.tsx and QUICK PLAY button in GameScreen idle state:
- `fontFamily: "'Bebas Neue', sans-serif"`
- `fontSize: '20px'`
- `letterSpacing: '0.14em'`
- `background: '#E8A020'` (amber accent)
- `color: '#0F0D09'` (dark text)
- `border: 'none'`
- `borderRadius: '2px'`
- `padding: '14px 40px'`
- `minHeight: '52px'`
- `cursor: 'pointer'` (or `'default'` when disabled)
- Hover: `background: '#C88010'` (using onMouseEnter/onMouseLeave like LOCK IN button)
- When disabled (LearnMore open): `opacity: 0.4`, no hover effect, `cursor: 'default'`
- Use `motion.button` from framer-motion with `initial={{ opacity: 0, y: 8 }}`, `animate={{ opacity: 1, y: 0 }}`, `transition={{ delay: 0.55 }}` so it fades in after the answer reveal settles
- Add `className="focus-ring-primary"` for keyboard focus visibility
- Add `aria-label` with the button text for screen reader clarity

**Modify `GameScreen.tsx`** — three surgical changes:

1. **Remove tap-anywhere root div onClick** (lines 405-409): Remove the `onClick` handler from the root `<div>` at the top of the main game return. The div should keep its className and style, just drop the onClick.

2. **Remove tap hint SVG overlay** (lines 632-646): Delete the entire `{state.phase === 'revealing' && (<motion.img src="/images/noun-tap-8166713-03B9D2.svg" .../>)}` block.

3. **Add NextStepButton during revealing phase**: Inside the `AnimatePresence` block that wraps the answer grid section (around line 573-614), AFTER the LearnMore button/tooltip section (after the closing `)}` of the `{state.phase === 'revealing' && learningContent && (` block, but still inside the `<motion.div>` wrapper), add:

```tsx
{/* Navigation button — shown during reveal phase */}
{state.phase === 'revealing' && (
  <div className="flex justify-center mt-3">
    <NextStepButton
      questionIndex={state.currentQuestionIndex}
      totalQuestions={state.totalQuestions}
      isFinalQuestion={isFinalQuestion}
      onAdvance={nextQuestion}
      disabled={isLearnMoreOpen}
    />
  </div>
)}
```

Place the button AFTER the LearnMore section so it renders below "Learn More" when learning content exists, or directly below the answer grid when no learning content exists. The button should render regardless of whether learningContent exists — it only depends on `state.phase === 'revealing'`.

4. **Shrink timer during reveal phase** to prevent mobile scroll overflow: Change the `GameTimer` `size` prop from hardcoded `80` to `{state.phase === 'revealing' ? 56 : 80}`. Also change the container `min-h-[80px]` to be conditional: use template literal className `min-h-[${state.phase === 'revealing' ? '56' : '80'}px]` — actually, Tailwind does NOT support dynamic class names. Instead, use inline style: replace `className="flex items-center justify-center min-h-[80px]"` with `className="flex items-center justify-center"` and add `style={{ minHeight: state.phase === 'revealing' ? '56px' : '80px' }}`.

5. **Update screen reader announce messages** (lines 282-289): The existing announce messages say "Press Space or tap Next to continue." — update these to reference the actual button label. Use a helper or inline: for correct answers say `Press Space or click ${buttonLabel} to continue.`, for incorrect/timeout say the same. Compute the label using the same logic as NextStepButton (or import a shared helper). Keep it simple — just inline the logic:
```typescript
const advanceLabel = isFinalQuestion ? 'Game Recap' : (state.currentQuestionIndex === state.totalQuestions - 2 ? 'Last Question' : 'Next Question');
```
Then use `Press Space or click ${advanceLabel} to continue.` in the announce messages.

6. **Overflow change**: The root div currently has `${state.phase === 'revealing' ? 'overflow-y-auto' : 'overflow-hidden'}`. Change this to ALWAYS be `overflow-hidden` since the button replaces scroll-dependent tap targets. The content must fit without scrolling. If the timer shrink + button together still overflow on 375x667, the `pt-4 md:pt-12 lg:pt-16` top padding on the question area can be reduced to `pt-2 md:pt-8 lg:pt-12`.
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit` — no TypeScript errors.
Run `cd frontend && npm run build` — build succeeds.
Visually confirm in browser: play a game, verify button appears after each answer reveal with correct label per question number.
  </verify>
  <done>
- NextStepButton.tsx exists and exports the component
- GameScreen imports and renders NextStepButton during revealing phase
- Root div onClick handler removed (no tap-anywhere)
- Tap hint SVG removed
- Timer shrinks to 56px during reveal
- Button labels: "NEXT QUESTION" for Q1-Q6, "LAST QUESTION" for Q7, "GAME RECAP" for Q8
- Button disabled (dimmed) when LearnMore modal is open
- Screen reader messages reference actual button labels
- No overflow scrolling on mobile viewport
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete game flow button replacement: tap-anywhere removed, explicit navigation buttons added with contextual labels, timer shrunk during reveal to fit mobile viewport.</what-built>
  <how-to-verify>
    1. Visit the game at http://localhost:5173 (or deployed URL)
    2. Start a Quick Play game
    3. Answer question 1 — verify "NEXT QUESTION" button appears below the answer grid after reveal
    4. Click the button — verify it advances to question 2
    5. Continue through questions 2-6 — all should show "NEXT QUESTION"
    6. Answer question 7 — verify "LAST QUESTION" button appears (different label!)
    7. Click it — should advance to the Final Question announcement, then wager screen
    8. Complete the wager question — verify "GAME RECAP" button appears
    9. Click it — should navigate to results screen
    10. Throughout: verify NO tap-anywhere behavior (tapping empty space should NOT advance)
    11. Throughout: verify NO tap hint icon (bouncing hand SVG) appears
    12. On mobile viewport (375x667 in DevTools): verify the full screen fits without scrolling — timer, question, answers, and button all visible
    13. Open Learn More during a reveal — verify button is dimmed and non-clickable
    14. Close Learn More — verify button becomes active again
    15. Test keyboard: press Space or Enter during reveal — should still advance (accessibility preserved)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes with no errors
- `cd frontend && npm run build` succeeds
- No references to `noun-tap-8166713` remain in GameScreen.tsx
- No onClick handler on root game div
- NextStepButton renders three distinct labels based on question index
- Game completes full 8-question flow using only the button (no tap-anywhere)
</verification>

<success_criteria>
- FLOW-01: "Next Question" button visible after Q1-Q6 answer reveal -- MET
- FLOW-02: "Last Question" button visible after Q7 answer reveal -- MET
- FLOW-03: "Game Recap" button visible after Q8 answer -- MET
- FLOW-04: Tap-anywhere and tap icon fully removed -- MET
- FLOW-05: Full game screen fits without scrolling on mobile and desktop -- MET
- FLOW-06: Button text legible, matches game show aesthetic -- MET
</success_criteria>

<output>
After completion, create `.planning/phases/69-game-flow-buttons/69-01-SUMMARY.md`
</output>
