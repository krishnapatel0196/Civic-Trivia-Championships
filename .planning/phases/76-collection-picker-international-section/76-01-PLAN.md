---
phase: 76-collection-picker-international-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/game.ts
  - frontend/src/features/collections/types.ts
  - frontend/src/utils/formatFreshness.ts
  - frontend/src/features/collections/components/CollectionPicker.tsx
  - frontend/src/features/collections/components/CollectionCard.tsx
autonomous: true

must_haves:
  truths:
    - "Collection picker renders an INTERNATIONAL section after Federal, visually consistent with existing section headers"
    - "Each International collection card shows a freshness indicator below the description (e.g. 'Updated 3h ago' or 'Updated Apr 9')"
    - "When no International collections are active, the International section does not appear"
    - "Search/filter (Phase 74) matches International collections by name and shows them in filtered results"
    - "Domestic collection cards do NOT show a freshness indicator"
  artifacts:
    - path: "frontend/src/utils/formatFreshness.ts"
      provides: "Freshness formatting utility"
      exports: ["formatFreshness"]
    - path: "frontend/src/features/collections/types.ts"
      provides: "CollectionSummary with latestQuestionAt field"
      contains: "latestQuestionAt"
    - path: "backend/src/routes/game.ts"
      provides: "latestQuestionAt MAX aggregate in /collections response"
      contains: "latestQuestionAt"
    - path: "frontend/src/features/collections/components/CollectionPicker.tsx"
      provides: "Four-section grouping with international last"
      contains: "international"
    - path: "frontend/src/features/collections/components/CollectionCard.tsx"
      provides: "Freshness indicator for international cards"
      contains: "formatFreshness"
  key_links:
    - from: "backend/src/routes/game.ts"
      to: "frontend/src/features/collections/types.ts"
      via: "latestQuestionAt field in /collections JSON response"
      pattern: "latestQuestionAt"
    - from: "frontend/src/features/collections/components/CollectionCard.tsx"
      to: "frontend/src/utils/formatFreshness.ts"
      via: "import formatFreshness"
      pattern: "formatFreshness"
    - from: "frontend/src/features/collections/components/CollectionPicker.tsx"
      to: "getCategory returns 'international'"
      via: "collection.tier === 'international' branch"
      pattern: "international"
---

<objective>
Add an "International" section to the collection picker UI with a freshness indicator on each International collection card.

Purpose: Players can discover and select International collections from the picker. The freshness indicator ("Updated 3h ago" / "Updated Apr 9") communicates how recently the collection's question pool was refreshed -- a key signal for news-driven content.

Output: Backend returns `latestQuestionAt` on all collections; frontend groups International collections into a new section (after Federal) with freshness display on each card.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-collection-picker-international-section/76-CONTEXT.md
@.planning/phases/76-collection-picker-international-section/76-RESEARCH.md

Key source files:
@backend/src/routes/game.ts (lines 52-103: GET /collections endpoint)
@frontend/src/features/collections/types.ts (CollectionSummary interface)
@frontend/src/features/collections/components/CollectionPicker.tsx (grouping logic + section rendering)
@frontend/src/features/collections/components/CollectionCard.tsx (card body + admin question count)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend latestQuestionAt + frontend type + formatFreshness utility</name>
  <files>
    backend/src/routes/game.ts
    frontend/src/features/collections/types.ts
    frontend/src/utils/formatFreshness.ts
  </files>
  <action>
    **1. Backend -- Add latestQuestionAt to /collections query (backend/src/routes/game.ts)**

    In the GET /collections handler (~line 59), add a new field to the `.select({})` block alongside the existing `questionCount` aggregate:

    ```typescript
    latestQuestionAt: sql<string | null>`MAX(${questions.createdAt})`.as('latestQuestionAt'),
    ```

    This computes the most recent `created_at` timestamp among active, non-expired questions per collection. The existing `groupBy(collections.id)` is compatible with this aggregate -- no changes to `.groupBy()` or `.orderBy()` needed. The field will be `null` for collections with zero matching question rows (which are filtered out by `MIN_QUESTION_THRESHOLD` anyway).

    **2. Frontend type -- Add latestQuestionAt to CollectionSummary (frontend/src/features/collections/types.ts)**

    Add to the `CollectionSummary` interface:

    ```typescript
    latestQuestionAt?: string | null;
    ```

    Place it after the existing `tier` field. The `?` makes it optional for backward compatibility; `| null` handles the SQL NULL case.

    **3. New utility -- Create formatFreshness (frontend/src/utils/formatFreshness.ts)**

    Create a new file with the following logic (adapted from existing patterns in ElectionsPage.tsx and Profile.tsx):

    - Input: `isoString: string | null`
    - Output: `string | null` (null when input is null)
    - Logic:
      - < 1 minute: `"Updated just now"`
      - < 60 minutes: `"Updated Xm ago"` (e.g., "Updated 45m ago")
      - < 24 hours: `"Updated Xh ago"` (e.g., "Updated 3h ago")
      - >= 24 hours: `"Updated Mon D"` using `Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric' })` (e.g., "Updated Apr 9")

    Use `Date.now() - new Date(isoString).getTime()` for the diff. No external dependencies (no date-fns).
  </action>
  <verify>
    - `npx tsc --noEmit` passes in both backend/ and frontend/ directories
    - `formatFreshness` file exists at frontend/src/utils/formatFreshness.ts and exports the function
    - CollectionSummary in types.ts includes `latestQuestionAt`
    - game.ts /collections query includes `latestQuestionAt` in select block
  </verify>
  <done>
    Backend /collections endpoint returns latestQuestionAt (string or null) for every collection. Frontend type includes the field. formatFreshness utility correctly formats timestamps per the spec.
  </done>
</task>

<task type="auto">
  <name>Task 2: CollectionPicker International section + CollectionCard freshness indicator</name>
  <files>
    frontend/src/features/collections/components/CollectionPicker.tsx
    frontend/src/features/collections/components/CollectionCard.tsx
  </files>
  <action>
    **1. CollectionPicker.tsx -- Extend grouping to include International section**

    Three changes in this file:

    a) Update `getCategory` function to return `'international'` for international collections. Add before the default `return 'local'`:
    ```typescript
    if (collection.tier === 'international') return 'international';
    ```
    Update the return type to `'local' | 'state' | 'federal' | 'international'`.

    CRITICAL: The `'international'` check MUST come before the default `return 'local'` -- otherwise international collections silently appear under "LOCAL".

    b) Add `'international'` entry to `GROUP_LABELS`:
    ```typescript
    international: 'International',
    ```
    The section header rendering already uses Bebas Neue with `letterSpacing: '0.18em'`, `fontSize: '13px'`, `color: '#9A8878'`, and a `#DDD5C3` horizontal rule. The label is rendered as-is by the existing JSX (`{label}`). This matches the CONTEXT.md spec for section header style.

    c) Extend the grouping array from `['local', 'state', 'federal']` to `['local', 'state', 'federal', 'international']`:
    ```typescript
    const grouped = (['local', 'state', 'federal', 'international'] as const)
    ```
    Order = Local -> State -> Federal -> International (geographic escalation per CONTEXT.md decision).

    The existing `.filter((g) => g.collections.length > 0)` already handles the "hide when empty" requirement -- no additional logic needed for SC #3.

    **2. CollectionCard.tsx -- Add freshness indicator for international cards**

    Import `formatFreshness` from `../../../utils/formatFreshness`.

    After the admin question count block (the `{isAdmin && (...)}` section, ~line 102-111), add the freshness indicator:

    ```tsx
    {collection.tier === 'international' && collection.latestQuestionAt && (
      <div style={{
        fontFamily: "'Bebas Neue', sans-serif",
        fontSize: '10px',
        letterSpacing: '0.1em',
        color: '#9A8878',
        marginTop: '6px',
      }}>
        {formatFreshness(collection.latestQuestionAt)}
      </div>
    )}
    ```

    This uses the exact same style as the admin question count row (Bebas Neue 10px, #9A8878, 0.1em letter-spacing, 6px top margin) per the CONTEXT.md decision.

    CRITICAL: Gate on `collection.tier === 'international'` -- NOT just on `collection.latestQuestionAt != null`. The backend returns latestQuestionAt for ALL collections; only international cards should display it.
  </action>
  <verify>
    - `npx tsc --noEmit` passes in frontend/
    - CollectionPicker.tsx contains `'international'` in the grouping array
    - CollectionPicker.tsx getCategory handles `'international'` tier explicitly
    - CollectionCard.tsx imports formatFreshness
    - CollectionCard.tsx gates freshness indicator on `collection.tier === 'international'`
    - No changes to existing domestic section behavior (local/state/federal order preserved)
  </verify>
  <done>
    Collection picker renders four sections: Local, State, Federal, International. International section appears last. Each International card shows a freshness timestamp below the description. Domestic cards are unchanged. Empty International section is automatically hidden.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` passes in both backend/ and frontend/ with zero errors
2. Backend response shape: The /collections endpoint JSON includes `latestQuestionAt` field on each collection object
3. Frontend grouping: CollectionPicker groups into exactly 4 categories in order: local, state, federal, international
4. Section visibility: The `.filter((g) => g.collections.length > 0)` logic ensures empty sections (including International when no international collections exist) are hidden
5. Freshness display: CollectionCard shows freshness only when `tier === 'international'` AND `latestQuestionAt` is non-null
6. Search compatibility: The filtered search path (`collections.filter(c => c.name.toLowerCase().includes(...))`) propagates `latestQuestionAt` automatically since it operates on the full `CollectionSummary` objects
7. Section header style: International uses the same Bebas Neue / #9A8878 / horizontal rule treatment as Local/State/Federal -- no special styling
</verification>

<success_criteria>
- The collection picker renders an "International" section after Federal with consistent Bebas Neue header styling
- International collection cards display a freshness indicator ("Updated Xh ago" or "Updated Mon D") in small muted text below the description
- When no International collections are active (current production state), the International section does not appear -- zero visible change for users today
- The search/filter input matches International collections by name and displays them in the flat filtered list
- Domestic cards (local/state/federal) show no freshness indicator
- TypeScript compiles cleanly in both backend and frontend
</success_criteria>

<output>
After completion, create `.planning/phases/76-collection-picker-international-section/76-01-SUMMARY.md`
</output>
