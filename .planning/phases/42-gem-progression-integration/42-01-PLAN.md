---
phase: 42-gem-progression-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260301000001_add_player_stats_columns.sql
  - backend/src/db/schema.ts
  - backend/src/types/database.types.ts
autonomous: true

must_haves:
  truths:
    - "trivia.player_stats table has current_streak, best_streak, and lifetime_gems columns"
    - "Drizzle schema exports include the three new columns with correct types"
    - "TypeScript types are regenerated and the build passes"
  artifacts:
    - path: "supabase/migrations/20260301000001_add_player_stats_columns.sql"
      provides: "ALTER TABLE adding 3 new columns"
      contains: "current_streak"
    - path: "backend/src/db/schema.ts"
      provides: "Updated playerStats Drizzle definition"
      contains: "lifetimeGems"
    - path: "backend/src/types/database.types.ts"
      provides: "Regenerated Supabase types including new columns"
      contains: "lifetime_gems"
  key_links:
    - from: "supabase/migrations/20260301000001_add_player_stats_columns.sql"
      to: "backend/src/db/schema.ts"
      via: "Column names must match between SQL and Drizzle"
      pattern: "current_streak.*best_streak.*lifetime_gems"
---

<objective>
Add current_streak, best_streak, and lifetime_gems columns to trivia.player_stats, update the Drizzle schema, and regenerate TypeScript types.

Purpose: Plans 02 and 03 need these columns available in the Drizzle ORM and TypeScript types to write game stats and track confirmed gem awards.
Output: Migration file, updated schema.ts, regenerated database.types.ts, clean build.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-gem-progression-integration/42-RESEARCH.md

@backend/src/db/schema.ts
@supabase/migrations/20260228000001_create_trivia_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration and update Drizzle schema</name>
  <files>
    supabase/migrations/20260301000001_add_player_stats_columns.sql
    backend/src/db/schema.ts
  </files>
  <action>
    1. Create a new Supabase migration file `supabase/migrations/20260301000001_add_player_stats_columns.sql` with:
       ```sql
       ALTER TABLE trivia.player_stats
         ADD COLUMN IF NOT EXISTS current_streak integer NOT NULL DEFAULT 0,
         ADD COLUMN IF NOT EXISTS best_streak integer NOT NULL DEFAULT 0,
         ADD COLUMN IF NOT EXISTS lifetime_gems integer NOT NULL DEFAULT 0;
       ```
       IMPORTANT: Use `ADD COLUMN IF NOT EXISTS` for each column to prevent migration failure if columns were previously added manually on Supabase.

    2. Update `backend/src/db/schema.ts` — add three new columns to the `playerStats` table definition, after `totalQuestions` and before `updatedAt`:
       - `currentStreak: integer('current_streak').notNull().default(0)`
       - `bestStreak: integer('best_streak').notNull().default(0)`
       - `lifetimeGems: integer('lifetime_gems').notNull().default(0)`

    3. Push the migration to the linked Supabase project:
       ```bash
       cd supabase && SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase db push
       ```
       If SUPABASE_ACCESS_TOKEN is not set in environment, check `.env` or `backend/.env` for it. The project is already linked (ref: kxsdzaojfaibhuzmclfq).
  </action>
  <verify>
    - `supabase db push` completes without errors
    - `grep -c 'current_streak\|best_streak\|lifetime_gems' backend/src/db/schema.ts` returns 3 (one per column)
  </verify>
  <done>Migration applied to Supabase, Drizzle schema has all 3 new columns defined.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate TypeScript types and verify build</name>
  <files>
    backend/src/types/database.types.ts
  </files>
  <action>
    1. Regenerate types using the established pattern (stderr redirect required to avoid diagnostic pollution):
       ```bash
       SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase gen types --linked --lang typescript --schema public,trivia 2>/dev/null > backend/src/types/database.types.ts
       ```

    2. Verify the generated types include the new columns by grepping for `lifetime_gems` in the output file.

    3. Run TypeScript build to confirm no type errors:
       ```bash
       cd backend && npx tsc --noEmit
       ```
  </action>
  <verify>
    - `grep 'lifetime_gems' backend/src/types/database.types.ts` finds the column
    - `grep 'current_streak' backend/src/types/database.types.ts` finds the column
    - `grep 'best_streak' backend/src/types/database.types.ts` finds the column
    - `npx tsc --noEmit` exits 0
  </verify>
  <done>database.types.ts includes all 3 new player_stats columns and TypeScript build passes cleanly.</done>
</task>

</tasks>

<verification>
- Migration file exists at expected path
- Drizzle schema has `currentStreak`, `bestStreak`, `lifetimeGems` on playerStats
- Generated types reflect the 3 new columns
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
The trivia.player_stats table has current_streak, best_streak, and lifetime_gems columns on the live Supabase database. The Drizzle schema and TypeScript types are in sync. Build is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/42-gem-progression-integration/42-01-SUMMARY.md`
</output>
