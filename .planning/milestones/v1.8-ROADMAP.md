# Milestone v1.8: Empowered Identity

**Status:** ✅ SHIPPED 2026-03-01
**Phases:** 40–46
**Total Plans:** 16

## Overview

Replace the custom auth/gem/admin system with the shared Empowered Accounts platform. Civic Trivia becomes a fully integrated feature of the Empowered Vote ecosystem: Supabase JWT for identity, shared gem ledger, Connected tier guards, platform-level admin roles, and complete removal of the legacy local auth stack.

## Phases

### Phase 40: Database Migration

**Goal**: Trivia tables exist under the `trivia` schema on the shared Supabase project with UUID user FKs, all 953 questions migrated, RLS policies in place, and TypeScript types regenerated.
**Depends on**: Phase 39 (v1.7 complete)
**Plans**: 3 plans

Plans:

- [x] 40-01: Schema creation, RLS policies, and player_stats/player_prefs table definitions
- [x] 40-02: Content migration (questions, collections, tags) from current Postgres to shared Supabase
- [x] 40-03: Type generation and build verification

**Details:**
- Deployed 9 trivia tables under `trivia` schema on shared Supabase project (kxsdzaojfaibhuzmclfq)
- UUID user_id FKs referencing `public.users(id)` on all user-scoped tables
- 953 questions, 7 collections, 80 collection_topics migrated
- 14 RLS policies across all tables; service role bypasses, user-scoped ownership enforcement
- `supabase gen types` with `--schema public,trivia` → 1950-line database.types.ts; tsc --noEmit passes
- `player_stats` and `player_prefs` tables deployed with UUID user_id keys
- Migration history repaired via `supabase migration repair --status reverted` to clear 24 pre-existing remote entries

---

### Phase 41: Auth & Tier Integration (Backend)

**Goal**: The backend validates Supabase JWTs, extracts UUID user identity, enforces Connected tier guards, and checks admin status via the platform `admin_users` table — all existing admin routes working under the new guards.
**Depends on**: Phase 40
**Plans**: 2 plans

Plans:

- [x] 41-01: Middleware foundation: install jose + supabase-js, create supabaseAdmin config, replace auth.ts with requireAuth/optionalAuth/requireConnected/requireAdmin
- [x] 41-02: Caller migration: update all route handlers from req.user to req.userId, fix sessionService plausibility, fix progression guard, verify build

**Details:**
- `requireAuth`: jwtVerify (jose ^6.1.3) against SUPABASE_JWT_SECRET; returns 401 on missing/invalid token
- `optionalAuth`: accepts both authenticated (req.userId populated) and anonymous requests
- `requireAdmin`: queries `public.admin_users` by UUID via supabaseAdmin service role client
- `requireConnected`: queries `connect.connected_profiles` for tier verification
- All route files migrated from `req.user.userId` (integer) to `req.userId` (UUID string)
- `supabaseAdmin` singleton client in `backend/src/config/supabase.ts`
- `authenticateToken` backward-compat alias created (removed in Phase 46)

---

### Phase 42: Gem & Progression Integration

**Goal**: Gem awards flow through the platform `award_gems` RPC, gem balance is read from the accounts API, persistent stats are stored in `trivia.player_stats` for Connected users only, and the local `total_gems` column is removed.
**Depends on**: Phase 41
**Plans**: 3 plans

Plans:

- [x] 42-01: Schema: add current_streak, best_streak, lifetime_gems to player_stats; update Drizzle schema; regenerate types
- [x] 42-02: Services: extend GameSession with tier/suspension fields; build awardPlatformGems, upsertPlayerStats, checkAccountContext
- [x] 42-03: Routes: wire account context into session start, gem award + stat write into results; GEMS-03 deprecation markers

**Details:**
- `awardPlatformGems`: calls Supabase `award_gems` RPC with `p_gem_type: 'yellow'`, `p_source: 'civic_trivia'`, `p_reason: 'game_completed'`
- `upsertPlayerStats`: writes games_played, best_score, total_correct, total_questions to `trivia.player_stats` for Connected tier only
- `checkAccountContext`: fetches tier and account_standing at session start
- Suspended accounts blocked from gem earning and stat writes
- Anonymous play preserved — no gem/stat writes for unauthenticated users
- `current_streak`, `best_streak`, `lifetime_gems` columns added to `player_stats`
- `@deprecated` JSDoc markers on total_gems references (resolved by Phase 44 cleanup)

---

### Phase 43: Frontend Auth & Profile

**Goal**: Frontend login, signup, logout, and token refresh all call the Empowered Accounts API; the profile page shows trivia-specific stats plus accounts-sourced gem balance, tier badge, and display name, with identity management actions linking out to the accounts platform.
**Depends on**: Phase 42
**Plans**: 3 plans

Plans:

- [x] 43-01: Auth infrastructure: types, accountsApi.ts, authService rewrite, authStore update, api.ts interceptor, AuthInitializer
- [x] 43-02: Login/Signup pages rewire with return-to navigation, ProtectedRoute update
- [x] 43-03: Profile page redesign: dual-API data, tier badge, remove identity management, accounts link

**Details:**
- `AccountsUser` type with UUID `id` and `tier` field replacing integer User
- `accountsApi.ts`: dedicated fetch wrapper for Empowered Accounts API (no credentials:include)
- `exchangeRefreshToken()`: Supabase native `/auth/v1/token?grant_type=refresh_token`
- `fetchAccountProfile()`: GET /api/account/me with Bearer token
- `authService.ts` rewritten for accounts API login/signup/logout
- `authStore`: tier and displayName fields; clearAuth removes ev_refresh_token
- `api.ts` auto-attaches Bearer token and refreshes on 401
- `AuthInitializer`: skips network call when no ev_refresh_token in localStorage
- Profile.tsx: trivia stats (gamesPlayed, bestScore, accuracy) + gem balance + tier badge + display name
- Open redirect guard in Login.tsx (from param validated to start with /)
- "Manage Account" link points to VITE_EMPOWERED_ACCOUNTS_WEB_URL

---

### Phase 44: Deprecation & Cleanup

**Goal**: All legacy local auth infrastructure is removed — no bcrypt, no custom JWT utilities, no local users table, no orphaned auth routes, no stale env vars — leaving the codebase clean with only Supabase-based auth remaining.
**Depends on**: Phase 43
**Plans**: 2 plans

Plans:

- [x] 44-01: Delete legacy auth files (9 files), uninstall dead packages (10 packages), surgically edit 7 remaining files to remove all dead imports and integer-user branches
- [x] 44-02: Update env.ts and .env.example, create users table drop migration, full build verification and stale reference sweep

**Details:**
- 9 files deleted: routes/auth.ts, controllers/authController.ts, middleware/auth.ts (old), models/User.ts, utils/jwt.ts, utils/tokenUtils.ts, and 3 others
- 10 packages removed: bcrypt, jsonwebtoken, @types/bcrypt, @types/jsonwebtoken, and related
- `POST /api/auth/*` routes removed; 404 on all legacy auth endpoints
- `users` table dropped via migration (20260301000002_drop_legacy_users_table.sql)
- `JWT_SECRET`, `JWT_REFRESH_SECRET`, `ADMIN_EMAIL` removed from .env.example and env.ts
- All 5 Supabase/accounts vars in .env.example: SUPABASE_JWT_SECRET, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, EMPOWERED_ACCOUNTS_URL
- `GameSession.userId` typed as string throughout — no integer user path
- `storageFactory.getRawClient()` added to provide Redis access without legacyRedis export
- TypeScript build passes with zero errors

---

### Phase 45: Auth State Hardening

**Goal**: Tier and admin status flow reliably from the accounts API to the auth store — fix the critical admin gate mismatch, tier loss after token refresh, and profile sync gap identified in the v1.8 audit. Formally verify Phase 43's frontend requirements.
**Depends on**: Phase 44
**Gap Closure**: Closes audit gaps from v1.8-MILESTONE-AUDIT.md (initial)
**Plans**: 2 plans

Plans:

- [x] 45-01: Code fixes: AuthInitializer tier fetch, AdminGuard backend check, Profile.tsx store sync, accessToken null guard
- [x] 45-02: Phase 43 formal verification (VERIFICATION.md) + ADMIN-01 requirement text update

**Details:**
- `tierResolved` flag added to authStore (default false, set true in all 4 AuthInitializer exit paths)
- `setLoading(false)` moved into per-branch finally blocks — prevents AdminGuard flash before accounts API call
- `AdminGuard` uses store-level `tier` field (not stale JWT metadata `user.tier`)
- `Profile.tsx` calls `setTier(profile.tier)` after `fetchAccountProfile` — syncs authoritative tier
- accessToken null guard: early-return redirect to /login rather than sending `Bearer ` empty header
- `setAuth` updated to set `tierResolved: true` — fixes admin access after expired-session re-login
- Phase 43 VERIFICATION.md created: 8/8 requirements confirmed (AUTH-04–07, PROF-01–04)
- ADMIN-01 requirement text corrected to reference `public.admin_users` (not `user_roles`)

---

### Phase 46: Auth Cleanup

**Goal**: Remove residual tech debt from the v1.8 auth migration — rename the `authenticateToken` alias to `requireAuth` in all remaining callers, remove the unused `requireConnected` export, and fix a stale JSDoc comment.
**Depends on**: Phase 45
**Gap Closure**: Closes low-severity tech debt items from v1.8 audit
**Plans**: 1 plan

Plans:

- [x] 46-01: Rename authenticateToken → requireAuth in 3 files, remove requireConnected export, fix stale JSDoc, verify build

**Details:**
- `authenticateToken` alias removed from auth.ts; all 3 callers (profile.ts, admin.ts, feedback.ts) use `requireAuth` directly
- `requireConnected` removed from auth.ts exports — no active Express consumer; frontend handles tier gates
- `sessionService.ts` JSDoc corrected: userId described as "UUID string for authenticated users, or 'anonymous'"
- `auth.ts` now exports exactly three functions: `requireAuth`, `optionalAuth`, `requireAdmin`
- TypeScript build passes with zero errors post-cleanup

---

## Milestone Summary

**Key Decisions:**

- Auth: `jwtVerify` (jose) against `SUPABASE_JWT_SECRET` — per accounts integration guide
- Admin: `admin_users` table UUID lookup (not user_roles join) — correctly typed in database.types.ts
- Gems: `award_gems` RPC, `p_gem_type: 'yellow'`, `p_source: 'civic_trivia'` — replaces local total_gems
- Stats: `trivia.player_stats` is trivia-specific (UUID user_id); not shared platform XP
- Anonymous play: preserved — only Connected tier earns gems and persistent stats
- Suspended accounts: blocked from gem earning and stat writes
- Identity management: links out to accounts platform — no in-trivia editing
- `tierResolved` gate pattern: store flag defaults false, set true in all init exit paths, guards render after resolution
- `requireConnected` removed from Express — tier enforcement is frontend-only for trivia routes
- `storageFactory.getRawClient()`: replaces legacyRedis export for rate limiter Redis access
- Token refresh: Supabase native /auth/v1/token (no custom /api/auth/refresh route)

**Issues Resolved:**

- Phase 43 no VERIFICATION.md → Closed by Phase 45-02
- Admin gate mismatch (user.tier from JWT vs admin_users table) → Closed by Phase 45-01
- Tier lost after token refresh → Closed by Phase 45-01 (tierResolved flag)
- Profile page not syncing tier → Closed by Phase 45-01 (setTier after fetchAccountProfile)
- `accessToken ?? ''` empty-string fallback → Closed by Phase 45-01 (null guard with /login redirect)
- `requireConnected` dead export → Closed by Phase 46-01
- `authenticateToken` alias (3 files) → Closed by Phase 46-01
- Stale JSDoc in sessionService.ts → Closed by Phase 46-01
- EMPOWERED_ACCOUNTS_URL missing from .env.example → Closed by Phase 44-02
- Admin access after expired-session re-login → Closed by Phase 45-01 (setAuth sets tierResolved: true)

**Issues Deferred:**

- `currentStreak`/`bestStreak` always written as 1 — real day-streak accumulation logic deferred to future phase
- Profile.tsx: commented-out inform-tier redirect (TODO — intentionally deferred until Connected accounts available)

**Technical Debt Incurred:**

- `(supabaseAdmin as any).rpc()` type bypass for `award_gems` RPC — cross-schema RPC not in typed Database interface; type-safe workaround possible in future

---

_For current project status, see .planning/ROADMAP.md_
